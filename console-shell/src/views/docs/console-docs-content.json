[
  {
    "key": "getting-started",
    "title": "Getting started",
    "guided": "# Getting started\n\nWelcome. This page walks you through what Console is, how it runs on your machine, how to open your first project, and how to find your way around. You don't need to be an experienced developer to follow it. Where a software-engineering idea comes up, there's an info callout that explains it honestly, so you finish this page knowing a little more than when you started.\n\n## What Console is\n\nConsole is a desktop app that sits on top of a code project you have on your computer and helps you understand it and ship it. You point it at a folder, and it reads the project the way an experienced engineer would on their first day: what language and frameworks it uses, how the pieces fit together, what's healthy and what's broken, and what you'd need to do to get it live on the internet.\n\nIt does a few distinct things:\n\n- **Reads your project** and writes you a plain report: the stack, the structure, a health score, and what to fix first.\n- **Maps the architecture** as a diagram of which parts of the code depend on which other parts.\n- **Connects to the services** your project uses or could use, such as GitHub (where code lives), Vercel (where you deploy), Sentry (which catches errors after launch), and others.\n- **Advises you** through a built-in AI assistant that can read your actual code to ground its answers.\n- **Deploys** to Vercel with no configuration, so \"make it live\" is a button rather than an afternoon.\n\n> [!INFO] What \"deploy\" means\n> To **deploy** is to take the code on your computer and put a running copy of it on a server somewhere on the internet, so other people can use it at a real web address. Until you deploy, your project only runs on `localhost` — your own machine. Deploying is the step that turns \"it works for me\" into \"anyone can visit it.\"\n\n> [!INFO] What \"a repo\" is\n> A **repository** (repo for short) is just the folder that holds your project, tracked by a tool called Git. Git records every change you make as a \"commit,\" so you have a full history and can undo mistakes. When Console asks you to \"open a project,\" it's asking for the folder that is your repo.\n\n## Local-first: it runs on your machine, not in the cloud\n\nConsole is **local-first**. That's an important promise, so it's worth being precise about what it means.\n\n- The app runs **on your computer**. It reads the files in the folder you open, directly from your disk.\n- Console does **not** upload your code to a Console server. There is no Console cloud that stores a copy of your project.\n- Everything you see in a panel is **derived live from files already in your repo** — your source files, your `.github/workflows`, your release checklists, your run records. Console reflects the truth on disk; it doesn't keep a second copy.\n\n> [!INFO] Why \"local-first\" matters\n> A lot of developer tools work by uploading your project to their servers and showing you a website. That's convenient but it means your code leaves your machine. **Local-first** means the opposite: the tool comes to your code, not your code to the tool. Your source never has to leave your computer for Console to do its job, which matters a great deal when the code is private or unfinished.\n\nA couple of consequences fall out of this design, and they're good ones to understand early:\n\n- **An empty panel is usually a signal, not a bug.** If the Architecture panel is blank, it generally means Console didn't find the files it draws from yet — not that it crashed. Add the file, and the panel fills in on the next read.\n- **Your keys stay private.** When you connect a service that needs a secret token, that secret is held by Console's background process (the part of the app that talks to your operating system), never handed back to the visible part of the app, and never sent to the AI. The screen only ever sees a yes/no \"Connected\" status and a redacted preview.\n\n> [!INFO] What \"a token\" or \"a key\" is\n> Many services give you a long secret string — a **token** or **API key** — that proves you're allowed to use them, a bit like a password that programs use instead of you typing one. Because anyone holding it can act as you, it must be kept secret. Console treats these tokens carefully: it stores them in your operating system's secure keychain and never shows them back on screen.\n\n## Opening a project\n\nThe first thing Console needs is a project folder to look at.\n\nThe very first time you launch Console, you'll see a short **onboarding flow** (covered below). One of its steps asks you to pick your project folder. After that first run, you can open or switch projects at any time from the top bar.\n\nTo open or switch a project later:\n\n1. Look at the **project chip** in the top bar — the little button with a folder icon. If you have a project open, it shows the folder's name; if not, it says \"No project, click to pick.\"\n2. Click it. A menu drops down with:\n   - **Recent projects** — up to six folders you've opened before, each showing its name and full path. Click one to switch to it.\n   - **New project in a new window** — opens a fresh Console window. Use this when you want two projects open side by side; each window remembers its own active project.\n   - **Open a project in this window…** — opens a folder picker and switches the current window to whatever folder you choose.\n   - **Open in file explorer** — reveals the current project folder in your operating system's file browser (this is greyed out until you have a project open).\n\n> [!INFO] Why several windows?\n> Console scopes the \"active project\" to each **window**, while the recent-projects list is shared across all of them. So you can have your main app open in one window and a side project in another, working on both at once, without them stepping on each other. A project you open in one window still shows up in another window's \"Recent\" list.\n\nWhen you pick a folder, Console reads it and the panels fill in. Naming follows the folder: the operating-system window title becomes \"`your-project` - Console\" so you can tell windows apart on the taskbar.\n\n## The first-run onboarding\n\nThe first time you launch (or any time you choose \"Run onboarding again\" from the command palette — see below), Console walks you through a short setup wizard. You don't have to get everything right; most steps are skippable, and you can change every choice later in Settings. The stages are, roughly, in order:\n\n1. **Pick a pace.** A welcome screen — \"You built it. Let's ship it.\" — offers two buttons: **Jump in** (the Operator view) or **Guide me through it** (the Guided view). This sets your *mode*, explained in the next section. Don't overthink it; you can switch any time.\n2. **A quick tour** of the main features (skippable).\n3. **Sign in** and **set up AI** — both optional, both skippable. You can add an AI provider now or later.\n4. **Connect your project** — pick the folder. This one is required, because everything else reads from it.\n5. **Study** — Console reads the project and builds its profile.\n6. **Report** — it shows you what it found, and you confirm \"Looks good.\"\n7. **Goals** — tell it what you're trying to do.\n8. **Services** — optionally connect things like GitHub now (skippable, \"Finish\").\n9. **Done** — you land on the Overview.\n\n## The two modes: Guided and Operator\n\nConsole has two *modes*, and the whole app changes shape depending on which one you're in. They show the same real data — they differ in how much they explain and how densely they pack it.\n\n- **Guided** is the teaching view. It uses plain-language names, a shorter and journey-ordered menu, more breathing room, and explanatory notes. It's the view you're reading these docs in. Start here if you're new to building software or new to Console.\n- **Operator** is the dense view for experienced engineers. Same data, fewer words, a fuller menu with technical names, tighter spacing. Switch to it once you know your way around and want to move fast.\n\nYou're not locked in. To switch:\n\n- Click the **mode pill** in the top bar (it shows \"Guided\" or \"Operator\"). One click flips it.\n- Or open the command palette (next section) and choose \"Switch mode.\"\n- Or go to **Settings → Display → Mode**.\n\n> [!INFO] Why two modes?\n> Good tools meet you where you are. A beginner and a ten-year veteran need different things from the same screen: one wants the concept explained, the other wants the number and nothing else. Rather than compromise on a middle that serves neither, Console ships both and lets you choose. Switching is instant and the whole UI re-renders, so feel free to flip back and forth as you learn.\n\n## Light and dark theme\n\nIndependently of mode, you can pick a **theme** — how the app looks.\n\n- Click the **sun/moon button** in the top bar to flip between light and dark instantly.\n- In **Settings → Display → Theme** you get a third option, **System**, which makes Console follow whatever your operating system is set to (so it goes dark when your OS goes dark).\n\nYour choice is remembered between launches.\n\n## The command palette (Ctrl+K)\n\nThe fastest way to get anywhere in Console is the **command palette**. Press **Ctrl+K** (or click the `Ctrl+K` button in the top bar) and a search box opens in the middle of the screen.\n\nStart typing what you want. The palette searches three kinds of things:\n\n- **Panels** — jump straight to any screen (Overview, Workboard, Architecture, and so on), even ones hidden from your current menu.\n- **Actions** — do something without hunting for a button: toggle the theme, switch mode, connect GitHub or Vercel, deploy, open the assistant, run onboarding again, copy the eval command, open your project folder, and more.\n- **Docs** — open any page of these docs.\n\nPress **Enter** to run the highlighted result, or **Esc** to close. The palette is smart about matching: it finds results by the words in their name or description, then re-ranks them by meaning, so a rough description of what you want usually surfaces the right thing.\n\n> [!INFO] Why a command palette?\n> As an app grows, hunting through menus gets slow. A **command palette** is a single search box that reaches everything — every screen and every action — by name. It's a pattern borrowed from code editors and modern tools because, once you know it exists, it's almost always faster than the mouse. If you only learn one shortcut in Console, make it Ctrl+K.\n\nThere's also a set of quick keyboard jumps if you like them: press **`g`** then a letter to jump to a panel (for example `g` then `o` goes to Overview, `g` then `s` to Services). Press **`?`** anywhere to see the full list. These are entirely optional — Ctrl+K does everything they do.\n\n## The overall layout\n\nOnce you're past onboarding, every Console window has the same frame. Knowing the four regions means you always know where you are.\n\n**1. The top bar** runs across the top. Left to right it holds:\n- The **Console** wordmark and \"by Refringence.\"\n- The **project chip** (open and switch projects — covered above).\n- On the right: a **mode pill** (Guided/Operator), an **Assistant** button (opens the chat, covered below), the **Ctrl+K** button, and the **theme** toggle. If an app update has downloaded, a \"Restart to update\" button appears here too.\n\n**2. The sidebar** runs down the left. It's your menu of panels, grouped sensibly for your mode. In Guided mode the groups are plain-language: **Your project** (Overview, Project report, Your code, What to fix), **Ship it** (Connect & deploy, Release, Monitor), and **Help** (Tutorials, Docs, and \"Read project files like a book\"), plus a **More** disclosure for the rest, and Settings always at the bottom. You can collapse the whole sidebar to a thin strip of icons using the **Collapse** control at its bottom — the icons stay, and hovering one shows its name.\n\n> [!INFO] What these panels are, briefly\n> Each sidebar entry is a different lens on your project. **Overview** is the dashboard. **Project report** is the deep read. **Your code** browses the files. **What to fix** is your task board. **Connect & deploy** wires up services. **Release** answers \"is it ready to ship?\" **Monitor** watches for errors after you go live. You'll meet each in its own doc page; for now, just know the sidebar is how you move between them.\n\n**3. The main area** in the middle is the panel you've selected. This is where the actual work and information live, and it's the part that changes as you click around the sidebar.\n\n**4. The assistant dock** opens on the right when you click the **Assistant** button (or choose \"Open assistant\" from the palette). It's a chat panel where you can ask questions; if you've connected an AI provider and have a project open, the assistant can read your real code to answer accurately. Opening it widens the window so it doesn't squeeze the main area. Close it with the × in its header.\n\n> [!INFO] \"Bring your own key\"\n> Console doesn't bundle an AI for you; instead you connect your own account with one of several providers (OpenAI, Anthropic, Google, a local Ollama model, or Kimi) in **Settings → AI**. This is called **bring-your-own-key**. It means you control which model you use and what it costs, and your key stays on your machine. A locally-running Ollama model needs no key at all.\n\nThat's the whole frame: top bar, sidebar, main area, and the optional assistant dock. With those four regions and Ctrl+K, you can reach everything Console does. The next pages walk through each panel in turn.",
    "operator": "# Getting started\n\nA precise reference to Console's shell: what the app is, where its data comes from, how to open and scope projects, the persona/theme/density/layout axes, the command palette and keymap, and the window frame. Source of truth: `console-shell/src/views/_shell/`, `console-shell/src/lib/`, and `console-shell/src/views/welcome/`.\n\n## What Console is\n\nLocal-first developer command-center, Electron main + React 19 / Vite / Tailwind v4 / Radix / cmdk renderer. It reads a local repo and derives every panel from files on disk; it does not maintain a server-side copy of project state. The renderer talks to the main process through a single typed IPC surface, `window.refringenceConsole`, wrapped by `lib/bridge.ts`. In browser dev (no main process) bridge calls fall back to throwing stubs. Raw secrets/keys/tokens are held main-side and never returned to the renderer — the renderer receives boolean status, redacted previews, and metadata only.\n\n## Local-first data model\n\n- Every \"data read\" in a panel is a bridge call, usually wrapped in a TanStack-Query hook under `lib/queries/*`.\n- Derived sources include: repo files, GitHub issues via the `gh` CLI, `.github/workflows`, release checklists under `docs/release-checklists/<version>.yaml`, run records under `.refringence-qa/`, project-scoped state under `.refringence-console/` (prompts, architecture overlay).\n- An empty panel signals a missing underlying file/connection, not a crash. Panels re-read on next fetch; freshly added files/connections surface without restart.\n- Secret handling: provider keys and service tokens are encrypted in the OS keychain main-side. Service `.env` values are read transiently main-side to test/store, then dropped — never shown back, never sent to AI.\n\n## Personas (Guided / Operator)\n\n`lib/persona.ts`, `lib/usePersonaMode.ts`. localStorage key `refringence-console-persona`, values `newbie` | `seasoned` (internal enum; user-facing labels `PERSONA_LABEL = { newbie: 'Guided', seasoned: 'Operator' }`, the raw words never render).\n\n- **No persona stored → route to `/welcome`** (onboarding). Once stored, `usePersonaMode` returns the value; UI defaults treat a missing value as `newbie` for label/render fallbacks.\n- Persona drives: explainer hovers, default sidebar collapse, density default, palette placeholder, onboarding-banner visibility, and per-panel Guided/Operator variants (panels branch `if (isNewbie) return <XNewbie/>; return <XSeasoned/>`).\n- Flip broadcasts `console-persona-change`; all mounted views re-render instantly. Flip surfaces: TopBar persona pill (`topbar-persona`), Sidebar persona chip (`sidebar-persona-chip`), CommandPalette \"Switch mode\", Settings → Display → Mode.\n\n## Theme\n\nlocalStorage key `refringence-console-theme`. TopBar toggle (`topbar-theme-toggle`) cycles **light ↔ dark only**; Settings → Display → Theme adds **System** (`prefers-color-scheme`). Toggling writes the key, toggles `.dark` on `<html>`, and dispatches `console-theme-change` for cross-component sync. `WindowControls` and all theme-aware components listen on that event. TopBar reads stored value as `'dark' | 'light'` (anything not `dark` → `light`).\n\n## Density\n\n`lib/density.ts`, key `refringence-console-density`, values `compact` | `roomy`, default `compact` (Guided surfaces `roomy`). Editable in Settings → Display → Density.\n\n## Console layout presets\n\n`lib/consoleLayout.ts`, key `refringence-console-layout-v1`. Two bits: `railCollapsed` (sidebar shows icon rail only) + `chatOpen` (right chat dock open). This module is the single owner of the rail bit. Presets:\n\n| Preset | railCollapsed | chatOpen |\n| --- | --- | --- |\n| Standard (default) | false | false |\n| Focus | true | false |\n| Chat | true | true |\n\n`useConsoleLayout` exposes `setPreset`, `toggleRail`, `setRailCollapsed`, `toggleChat`, plus `detectPreset` (returns `custom` when bits match no preset). Cross-instance/cross-window sync via `console-layout-change` custom event + `storage` event. Set from CommandPalette (\"Layout: Standard/Focus/Chat\", \"Toggle sidebar rail\") and the Sidebar Collapse control.\n\n## Opening and scoping projects\n\n`lib/activeProject.ts`. The **active project is scoped per window** via the `?wid=` query the main process appends (survives reloads); keys `refringence-console-active-project:<wid>` and `…-pickedAt:<wid>`. The **recent list is global**: key `refringence-console-recent-projects-v1`, cap 6. Changes broadcast `console-active-project-change` (+ `storage` event for cross-window).\n\nTopBar project chip (`topbar-project-chip`) dropdown:\n\n- **Recent projects** — up to 6, name + full path; select → `setProject(path)`.\n- **New project in a new window** (`topbar-new-window`) → `bridge.window.newWindow()` (fresh window starts at onboarding).\n- **Open a project in this window…** → `bridge.project.pickFolder()` → on non-cancel, `setProject(res.path)`.\n- **Open in file explorer** → `bridge.openPath(project.path)`; disabled when no active project.\n\nOn project change the OS window title is set via `bridge.window.setTitle` to `\"<basename> - Console\"` (or `\"Console\"` when none).\n\n## First-run onboarding\n\n`views/welcome/OnboardingWizard.tsx`, state machine `lib/onboarding/machine.ts`. Window-scoped via `?wid=`; frameless with its own drag strip + `WindowControls` (no TopBar). Stages and gating:\n\n1. **persona** — `PersonaLanding`, \"You built it. Let's ship it.\" Buttons: **Jump in** (`persona-seasoned`) → writes `seasoned`; **Guide me through it** (`persona-newbie`) → writes `newbie`. Then → tour.\n2. **tour** — skippable carousel (`TourStep`) → signin.\n3. **signin** (`SignInStep`, skippable) → **ai** (`AiSetupStep`, skippable) → **connect** (`ConnectRepoStep`, **required**: Next disabled until `projectRoot` set; writes active project) → **study** (`StudyStep`, footer hidden; `bridge.intel.mountStart`) → **report** (`ReportStep`, Next label \"Looks good\") → **intent** (`IntentStep`) → **services** (`ServicesStep`, skippable, Next label \"Finish\") → **done** (`DoneStep`, footer hidden).\n\nFooter (when shown): Back (`onb-back`) / Skip (`onb-skip`, on signin/ai/services) / Continue (`onb-next`). Finish writes both onboarded flags (`writeOnboarded` global + `writeOnboardedForWindow`) → navigate `/overview`. CommandPalette \"Run onboarding again\" clears `refringence-console-onboarded` + the per-window flag → `/welcome`.\n\n## Command palette (Ctrl+K)\n\n`views/_shell/CommandPalette.tsx`, Radix Dialog + `cmdk` (testid `command-palette`). Opened by the `console-open-palette` window event, dispatched by TopBar's `topbar-cmdk` button and by `CommandPaletteHotkey` (registers Ctrl+K / Cmd+K to toggle). Input placeholder: \"Jump to a panel, run an action, or open a doc…\". Footer: Esc to close, Enter to select.\n\nMatching: `shouldFilter={false}`. Own substring match on `label + description`, then re-rank by semantic similarity (`rankBySemanticSimilarity`, `lib/cmdk/semantic-rank`), debounced 150ms; empty query → alphabetical. Three groups:\n\n- **Panels** — flattened from `GROUPS` (the canonical Operator set in `Sidebar.tsx`). Each row shows its group badge; a **Hidden** badge appears when the path is in the saved sidebar `hidden` set.\n- **Actions** — Toggle theme; Switch mode (Guided/Operator); Run onboarding again; Connect GitHub / Connect Vercel / Connect repo (→ `/services`); Run eval (→ `/observability`); Deploy (→ `/release`); Open assistant (`toggleChat`); Open prompt library (→ `/prompts`); Toggle sidebar rail; Layout: Standard / Focus / Chat; Copy eval command (`npm run eval:promptfoo` → clipboard + toast); Open project folder (`bridge.openPath` active project, toast on no-project or failure); Open repo on GitHub (`github.com/Refringence-AI/console`); Keyboard shortcuts (opens help dialog).\n- **Docs** — from `CONSOLE_DOC_PAGES` (`console-docs-index.ts`).\n\nSelecting a panel/doc navigates; an action runs its `run()`. The palette also hosts `useKeymap({ enabled: !open })` and the Shortcuts help dialog (`shortcuts-help`).\n\n## Keyboard shortcuts\n\n`lib/useKeymap.ts`. Global handler; yields while target is INPUT/TEXTAREA/SELECT/contentEditable, while any modifier (meta/ctrl/alt) is held, or while the palette is open. g-prefix chord buffer clears after 1500ms.\n\ng-prefix navigation chords (`NAV_CHORDS`):\n\n| Chord | Route | Chord | Route |\n| --- | --- | --- | --- |\n| `g o` | /overview | `g l` | /release |\n| `g w` | /issues (Workboard) | `g y` | /observability |\n| `g r` | /repo | `g c` | /activity |\n| `g a` | /arch | `g t` | /tutorials |\n| `g p` | /pipeline | `g d` | /docs |\n| `g s` | /services | `g ,` | /settings |\n| `g b` | /library | | |\n\nOther: **`?`** opens the shortcuts help dialog; **Ctrl+K / Cmd+K** toggles the palette. In ChatComposer: Enter sends, Shift+Enter newline. In Prompts (Operator) detail: ↑/↓ navigate, ⌘C copy filled.\n\n## Window frame\n\n`views/_shell/ConsoleShell.tsx`: `TopBar` (full width) over `Sidebar | <Outlet/> | ConsoleChatDock`. Mounts `CommandPalette` + `CommandPaletteHotkey`; \"Skip to content\" a11y link; each route wrapped in `PanelErrorBoundary` keyed by pathname. Frameless window — `WindowControls` (minimize/maximize/close) rendered in React.\n\n### TopBar (`top-bar`)\n\nLeft: `console.svg` + \"CONSOLE\" wordmark + \"by Refringence\"; then the project chip. Right cluster (`ml-auto`): `UpdatePill` (`topbar-update`, shown only when `useUpdate().status === 'downloaded'`, click → `update.install()`); persona pill (`topbar-persona`); Assistant toggle (`topbar-ai`, `aria-pressed=chatOpen`, on open calls `bridge.window.growForDock(360)`); Ctrl+K button (`topbar-cmdk`); theme toggle (`topbar-theme-toggle`). Whole bar is an OS drag region except interactive controls.\n\n### Sidebar (`sidebar`, `data-rail-collapsed`)\n\nPersona-aware IA. Width animates between `FULL_WIDTH=224` and `RAIL_WIDTH=56`; rail keeps icons + tooltips, active row keeps a 2px accent bar (`nav-active-bar-<panel>`). Nav testids are route-canonical (`NAV_TESTID`) regardless of display label.\n\n- **Operator** renders `GROUPS`: **Project** (Overview, Report, Prompts, Workboard `/issues`, Repo, Architecture, Pipeline, Services, Library, Release, Observability, Activity) and **Help** (Tutorials, Docs, Settings). Order / hide / group-collapse come from saved sidebar layout (`lib/sidebarLayout.ts`, key `…sidebar-layout-v1`). `SOFT_HIDDEN = {/activity, /library, /docs, /tutorials}` default off the rail (palette-only) until the layout is explicitly customized. LOCKED-never-hideable: Overview, Release, Pipeline.\n- **Guided** renders `GUIDED_GROUPS` (plain-language, same routes): **Your project** (Overview, \"Project report\", \"Your code\" `/repo`, \"What to fix\" `/issues`); **Ship it** (\"Connect & deploy\" `/services`, Release, \"Monitor\" `/observability`); **Help** (Tutorials, Docs, \"Read project files like a book\" `/library`). `GUIDED_MORE` disclosure (Architecture, Pipeline, Activity, Prompts) + Settings always shown.\n- Proactive nav badges: danger dot on `/observability` when `useObsCounters().errors_last_24h > 0`; warning dot on `/release` when latest release summary is blocked/red. One OS notification (`bridge.notify`) per mount when 24h errors cross 0→positive.\n- Footer: Collapse toggle (`sidebar-rail-toggle`, drives `toggleRail`), persona chip (`sidebar-persona-chip`), version chip \"v0.1\" (`sidebar-version-chip`).\n\n### ConsoleChatDock\n\nRight-hand 360px AnimatePresence column, open state = `consoleLayout.chatOpen`. Header \"Assistant\" + close (`console-chat-dock-close`). Embeds `<ChatPanel embedded/>`. The assistant is the dock, not a nav panel; passing `projectRoot` to `bridge.ai.chat.start` gives it read-only tools + project context.\n\n## At a glance\n\n| Surface | Storage key | Owner |\n| --- | --- | --- |\n| Persona | `refringence-console-persona` | `lib/persona.ts` |\n| Theme | `refringence-console-theme` | TopBar / Settings |\n| Density | `refringence-console-density` | `lib/density.ts` |\n| Layout (rail + chat) | `refringence-console-layout-v1` | `lib/consoleLayout.ts` |\n| Sidebar layout | `…sidebar-layout-v1` | `lib/sidebarLayout.ts` |\n| Active project (per window) | `…active-project:<wid>` | `lib/activeProject.ts` |\n| Recent projects (global) | `…recent-projects-v1` | `lib/activeProject.ts` |\n| AI tier | `refringence-console-ai-tier` | `lib/ai/registry.ts` |\n| Onboarded flags | `refringence-console-onboarded` (+ per-window) | `lib/onboarded*.ts` |"
  },
  {
    "key": "overview",
    "title": "Overview",
    "guided": "# Overview\n\nThe Overview panel is the first thing Console shows you after you open a project. It is your project's front page: one screen that answers the single most useful question a builder can ask — *\"What is the state of my project right now, and what should I do next?\"*\n\nEverything on this page is read live from your actual project (your git history, your config files, the services you have connected). Nothing here is invented or filled in with placeholder data. If a number looks empty, it is because that thing genuinely has not happened yet.\n\n> [!INFO] **What \"live state\" means.** Console does not store a copy of your project and show you a stale snapshot. Each time this panel loads, it re-reads the real files and re-runs the checks. A few numbers refresh on their own roughly once a minute. So when you fix something and come back, the page reflects it.\n\nThe page is laid out **most-urgent-first**, top to bottom. The closer to the top something is, the more it wants your attention.\n\n---\n\n## The top line: \"Needs attention\" or \"All systems healthy\"\n\nThe very first thing under the title is a single strip. It is the page's headline.\n\n- **Green strip — \"All systems healthy. Nothing needs your attention right now.\"** Everything Console checks is in good shape. You can relax or keep building.\n- **Amber/red strip — \"Needs attention:\"** followed by a short list of dots. Each dot is one real problem, and clicking it jumps you straight to the panel where you can deal with it.\n\nThe problems that can appear here:\n\n- **\"N gates failing\"** — one of your release checks failed. Jumps to **Release**.\n- **\"N gates blocked\"** — a release check is waiting on you to do something. Jumps to **Release**.\n- **\"N errors in 24h\"** — your running app reported errors in the last day. Jumps to **Observability**.\n- **\"N eval failures\"** — your AI-quality tests failed. Jumps to **Observability**.\n\n> [!INFO] **Why \"most urgent first\" is a real engineering habit.** Good dashboards do not show you everything at equal weight — they triage. A pilot's cockpit puts the \"engine on fire\" light where you cannot miss it and the cabin temperature in a corner. This strip is your \"engine on fire\" light. When it is green, you have permission to stop scanning the rest of the page for hidden trouble.\n\n---\n\n## \"Is it ready to ship?\" — the ship card and the gate ring\n\nBelow the headline strip sit two cards side by side. Together they answer the one question that matters before a release: *can I ship this?*\n\n### The ship card (left)\n\nA plain-English verdict in a big sentence, plus one button:\n\n- **\"Ready to ship.\"** — every release check passed. The button reads **Ship release** and takes you to the Release panel to cut it.\n- **\"Almost ready to ship.\"** — just a couple of checks left. The button reads **Open Release**.\n- **\"Not ready to ship.\"** / **\"Checks pending.\"** — there is failing or unfinished work. The button reads **Open Release**.\n\nThe card never repeats the exact problem count (the strip at the top already said that). Instead it gives you the verdict and a forward-looking next move.\n\n### The release-gate ring (right)\n\nA donut chart that breaks your release checks into three coloured slices, each with a matching labelled row beside it:\n\n- **Passing** (green) — checks that are done and good.\n- **Blocked** (amber) — checks waiting on your input. (Failing checks are folded in here too, because both mean \"you need to act.\")\n- **Pending** (grey) — checks not yet run or reviewed.\n\nThe middle of the ring reads something like **\"7/12 passing\"** so you can see progress at a glance. Clicking anywhere on this card opens the **Release** panel.\n\n> [!INFO] **What a \"release gate\" actually is.** A *gate* is a checkpoint that a release must pass before it is allowed out the door — \"tests pass\", \"no known security holes\", \"the changelog is updated\", and so on. The set of gates is your *release checklist*. Professional teams gate releases so that shipping is a deliberate decision backed by evidence, not a hopeful \"looks fine to me.\" Console reads your checklist (a file in `docs/release-checklists/`) and shows you, honestly, how many gates are green.\n>\n> A **health score** or a **pass ratio** like \"7 of 12\" is engineering's favourite shape for this: it turns a yes/no question (\"am I done?\") into a progress bar you can chip away at.\n\n---\n\n## Vitals — the four number cards\n\nNext is a row of small cards, each a single important number. Think of them as your project's pulse, blood pressure, and temperature. Each card is clickable and takes you to the panel with the full story.\n\n### Tests\n\nHow many test runs finished in the last 24 hours, and whether any of them had errors.\n\n> [!INFO] **What \"tests\" are.** A *test* is a small piece of code whose only job is to check that your real code behaves correctly — it runs your function with a known input and asserts the output is what you expected. *Running your tests* means executing all of them at once and counting how many passed. A project with passing tests is one you can change without holding your breath, because the tests will shout if you broke something. This card counts how often that safety net has run recently.\n\n### Build\n\nWhether your latest **CI** run passed, failed, or is still running. If your project has no CI set up, this reads **\"idle\"** with **\"No CI runs found\"** — which is not a failure, just an absence.\n\n> [!INFO] **What \"CI\" (Continuous Integration) is.** CI is a robot that watches your code repository. Every time you push a change, it automatically downloads your project on a fresh machine, installs everything, builds it, and runs your tests — then reports pass or fail. The point is to catch \"it works on my machine but nowhere else\" problems immediately, and to keep the main branch always in a working state. The config lives in a `.github/workflows/` folder; Console reads the result of the most recent run.\n\n### Cost today\n\nHow much you have spent on AI through Console today, in dollars. If you have not used any paid AI yet, it reads **\"No AI spend tracked yet.\"**\n\n> [!INFO] **Why a tool tracks AI cost.** AI models charge by the amount of text they read and write. It is easy to lose track and get a surprising bill. Showing the running daily total — the way a taxi meter shows the fare as you ride — keeps the cost visible while it is still small enough to act on.\n\n### Evals\n\nYour AI-quality test results, shown as a pass-rate percentage with a \"X pass · Y fail\" breakdown, plus a count of components in your SBOM if one exists. If you have never run them, it reads **\"No runs\"**.\n\n> [!INFO] **What an \"eval\" is.** An *eval* (short for evaluation) is a test, but for AI behaviour instead of ordinary code. Normal tests check exact answers; AI gives fuzzy, varied answers, so an eval runs a set of prompts through your AI and grades whether the responses are good enough against expected outcomes. It is how you stop your AI feature from silently getting worse. **SBOM** stands for *Software Bill of Materials* — a complete list of every third-party package your project depends on, like an ingredients label on food. It matters for security: when a popular package is found to have a hole, you check your SBOM to see if you are affected.\n\n### A note on empty cards\n\nWhen you have run **no** tests **and** no evals, Console does not show you two sad blank cards. It collapses them into one friendly card — **\"No test or eval runs yet\"** with a **Run a test** button that takes you to Observability to start your first run. This is deliberate: a clean, honest \"you have not done this yet, here is how to start\" beats two hollow zeroes.\n\n### Deployment (only when connected)\n\nA fifth card appears **only if you have connected Vercel** (a hosting service) and deployed at least once. It shows the status of your last deployment (e.g. **READY**) and the live URL, which you can click to open your site in a browser.\n\n> [!INFO] **What \"deploying\" means.** *Deployment* is the act of taking the code on your computer and putting a running copy on the public internet so other people can visit it. \"Zero-config\" deployment, which Console supports through Vercel, means Console figures out the build settings for you so you do not have to learn them first. This card is your receipt: it tells you the latest version is live and gives you the link.\n\n---\n\n## \"Next\" — what to do now\n\nBelow the vitals is a short list (one to three items) of concrete next steps, computed from the real state of your project. Each is a single clickable line that takes you where the work happens.\n\nThese are not chatbot answers and not generic advice — they are rules reading your actual signals. For example: if a release gate is blocked, it suggests opening Release; if you have never run evals, it suggests running them. If everything is genuinely fine, this section quietly says so.\n\n> [!INFO] **Why a \"next step\" list helps you learn.** The hardest part of a new project is not doing the work — it is knowing *which* work to do first. By always surfacing the single highest-value next action, Console teaches you the order experienced engineers work in: clear blockers, keep your safety nets fresh, then build. Follow the suggestions for a few weeks and the priorities start to feel obvious on their own.\n\n---\n\n## \"Recent activity\" — your project's history\n\nAt the bottom is a feed of your most recent git commits: the actual changes saved to your project, newest first, with a short description, a relative time (\"3 hours ago\"), and a short code (the commit hash). **View all** opens the full history in the Activity panel.\n\nIf this is empty, it usually means the folder is not a git repository yet, or git is not installed.\n\n> [!INFO] **What a \"commit\" is.** *Git* is the system that records the history of your project. A *commit* is a single saved snapshot — a checkpoint with a message describing what changed (\"fix login bug\", \"add dark mode\"). The string of commits is your project's complete, undo-able timeline. Reading this feed is like flipping through a logbook: it tells you what has been happening and reassures you that the project is alive and moving.\n\n---\n\n## The Guided Overview, specifically\n\nYou are reading the **Guided** version of this panel (set by your persona toggle in the top bar). It is intentionally calmer and more narrative than the Operator version:\n\n- A friendly headline — **\"How your project is doing\"** — and one plain-English sentence describing what your project *is* (e.g. \"A Node.js web app. Start with `npm start`.\").\n- A single bold **Next step** line — the one thing most worth doing now.\n- An **\"Is it ready to ship?\"** card with the big gate number and a plain explanation.\n- **The numbers that matter**: AI spend today, Progress (commits landed), and Tested (test runs in 24h) — and if all three are zero, one quiet line instead of three empty cards.\n- A **\"Lately\"** activity list (your last few commits).\n- An **Explore** row pointing you at Workboard, Read the repo, and Tutorials.\n\nIt deliberately leaves out the denser details (SBOM counts, gate-by-gate breakdowns, raw line counts) — those mean little before you know the project. Flip the persona toggle to **Operator** any time to see the full cockpit.",
    "operator": "# Overview\n\nRoute `/overview`. Landing surface. Persona-branched at the top of `OverviewPanel`: `usePersonaMode().isNewbie` → `OverviewNewbie`, else `OverviewSeasoned`. This reference documents the **Operator** (`seasoned`) cockpit; the Guided variant is summarized at the end.\n\n`data-testid=\"overview-panel\"`. Vertically scrolling single column. Layout order, top to bottom: PanelHeader → AlertStrip → focal row (ShipCard + ReleaseGatesCard) → Vitals grid → Next (WhatsNextTile) → Recent activity → 32px tail spacer.\n\n## Data sources\n\nAll reads are TanStack-Query hooks over `bridge.*`, keyed on the active project path (`useActiveProject().project.path`); each is window-scoped via the active-project query.\n\n| Hook | Bridge call | Feeds | Cadence |\n|---|---|---|---|\n| `useMetricsSummary` | `metrics.summary(root)` | Build, Cost today, Evals, SBOM | `staleTime 30s`, `refetchInterval 60s` |\n| `useReleaseList` | `release.list(root)` | resolves `[0].version` | `staleTime 30s` |\n| `useReleaseSummary(version)` | `release.summary(root)` | ShipCard, ReleaseGatesCard, AlertStrip, WhatsNextTile | `staleTime 30s` |\n| `useObsCounters` | `obs.counters(root)` | Tests vital, AlertStrip errors, WhatsNextTile | — |\n| `useRepoSummary` | `repo.summary(root)` | `total_loc` for shape subtitle | — |\n| `useRecentCommits(6)` | `activity.recentCommits` | Recent activity feed | — |\n| `useProjectShape(path)` | `repoIntrospect.shape` | header subtitle (`shapeSubtitle`) | — |\n| `useConnections` | `connections.list` | `vercel.connected` gate | — |\n| `useVercelDeployments(enabled)` | `connections.vercel.deployments` | DeploymentCell (enabled only when Vercel connected) | — |\n| `getLastDeploy(path)` | `lib/deployStore` (local, per-project) | scopes DeploymentCell to this project | synchronous |\n\n`ReleaseSummary` shape: `{ version, overall_status: GateStatus, green, amber, red, blocked, gate_count }` where `GateStatus = 'green'|'amber'|'red'|'blocked'`. `MetricsSummary` relevant fields: `ci{configured,status:'passing'|'failing'|'running'|'none',conclusion,workflow,ranAt}`, `sbom{present,components,spec_version,size_bytes}`, `promptfoo{present,last_run,passed,failed,errors}`, `cost_today_usd`, `cycle_log{commits_landed,cycles_completed}`. `ObsCounters`: `{runs, runs_last_24h, errors, errors_last_24h}`.\n\n## PanelHeader\n\nIcon `LayoutDashboard`, title **\"Overview\"**, `testid=\"overview-panel-header\"`. Subtitle = the project **shape line** from `shapeSubtitle(shape.data, repo.data?.total_loc)` (`data-testid=\"overview-shape-line\"`), format `Type · startCommand · N packages · N LOC` (each segment conditional). Fallback when shape unresolved: **\"Live state of the project, most urgent first.\"**\n\n## AlertStrip\n\n`px-6 pt-4`. Aggregated banner, built in this push order from real signals:\n\n1. `release.red > 0` → `\"{n} gate(s) failing\"`, tone `rose`, → `/release`.\n2. `release.overall_status === 'blocked' && release.blocked > 0` → `\"{n} gate(s) blocked\"`, tone `amber`, → `/release`.\n3. `obs.errors_last_24h > 0` → `\"{n} error(s) in 24h\"`, tone `rose`, → `/observability`.\n4. `metrics.promptfoo.present && metrics.promptfoo.failed > 0` → `\"{n} eval failure(s)\"`, tone `rose`, → `/observability`.\n\nStates:\n- **Loading** (`!release.data && release.isLoading`): 36px pulse skeleton.\n- **Empty** (no alerts): success-tinted row, `CircleCheck` + **\"All systems healthy.\"** + \"Nothing needs your attention right now.\"\n- **Populated**: warning-tinted row, `TriangleAlert` + **\"Needs attention:\"** + alerts joined by `·` separators. Each alert = tone dot (rose=`bg-danger`, amber=`bg-warning`) + label, wrapped in a `Link` to its `to` (hover underline).\n\nThe strip's first alert route (`alerts[0]?.to`) is passed to WhatsNextTile as `suppressTopTo` to de-dupe the headline against the Next list.\n\n## Focal row\n\n`px-6 pt-5`, `grid lg:grid-cols-2 gap-4`. ShipCard + ReleaseGatesCard. Both render loading skeletons while `!release.data && release.isLoading`.\n\n### ShipCard\n\nVerdict + single primary action. Verdict computed by `shipVerdict(release)` over `open = amber + red + blocked`:\n\n- `open === 0` → **\"Ready to ship.\"** / \"Every release gate is passing. You are clear to cut a release.\" / `ready=true`.\n- `green >= gate_count - open && open <= 2` → **\"Almost ready to ship.\"** / \"Clear the last few gates from the release checklist, then ship.\" / `ready=false`.\n- else → headline **\"Not ready to ship.\"** (when `red > 0`) or **\"Checks pending.\"** (otherwise) / \"Open the release checklist to see what each gate needs.\" / `ready=false`.\n\nThe detail line is intentionally forward-looking and never restates the alert count. Button: `variant=\"default\"` (neutral-inverted), `size=\"lg\"`, no chevron; label **\"Ship release\"** when `ready`, else **\"Open Release\"**; both `Link to=\"/release\"`.\n\n### ReleaseGatesCard\n\nEntire card is a `Link to=\"/release\"`. `Donut` (size 84, stroke 9), `ariaLabel=\"{green} of {gate_count} gates passing\"`, three segments:\n\n- `green` → `emerald`\n- `blocked = red + blocked` → `amber` (failing gates fold into blocked; no fourth arc)\n- `amber` → `slate`\n\nDonut center: **`{green}/{gate_count}`** + caption **\"passing\"**. Beside it: `SectionLabel` \"Release gates\", `{green} of {gate_count} passing`, then three `LegendRow`s mapped 1:1 to segments — `passing` (`bg-success`, count `green`), `blocked` (`bg-warning`, count `red+blocked`), `pending` (`bg-muted-foreground`, count `amber`).\n\n## Vitals\n\n`Section title=\"Vitals\"`, grid `sm:grid-cols-2 xl:grid-cols-4`, gap 3. Each cell is a `VitalCell` = `Link` to a route with hover border. Cells render `CellSkeleton` (48px pulse) until their query resolves.\n\n**Combined-empty collapse**: `testsAndEvalsEmpty = obs.data.runs_last_24h === 0 && !metrics.data.promptfoo.present` (both loaded). When true, the Tests cell and the Evals cell are replaced by a single dashed `EmptyTestsEvalsPrompt` spanning `sm:col-span-2`: label \"Tests + Evals\", \"No test or eval runs yet\", \"See past runs and start the first one in Observability.\", **Run a test** button → `/observability`. It carries the `overview-eval-passrate` and `overview-sbom-count` testids so queries still resolve; the standalone Evals cell is suppressed in this state.\n\n### Tests — `VitalCell to=\"/observability\"`\nIcon `FlaskConical`. Value = `obs.runs_last_24h`.\n- `> 0`: `NumberFlow` value + hint `\"all passing in 24h\"` (when `errors_last_24h === 0`) or `\"{n} error(s) in 24h\"`.\n- `=== 0`: muted `0` + hint \"No test runs in 24h\".\n\n### Build — `VitalCell to=\"/pipeline\"`\nIcon `Hammer`. Value = `ciLabel(metrics.ci.status)` where `'none'` maps to **\"idle\"** (a CI-less repo is not a failure); other values render verbatim (`passing`/`failing`/`running`). Hint: `metrics.ci.workflow ?? 'latest CI run'` when `ci.configured`, else **\"No CI runs found\"**.\n\n### Cost today — `VitalCell to=\"/observability\"`\nIcon `Coins`. Value = `metrics.cost_today_usd` via `NumberFlow` formatted as USD currency. Hint: \"No AI spend tracked yet\" when `0`, else \"AI spend today\".\n\n### Evals — `VitalCell to=\"/observability\"` (suppressed when `testsAndEvalsEmpty`)\nIcon `CheckCircle2`. `data-testid=\"overview-eval-passrate\"`.\n- `promptfoo.present`: `evalPassRate = round(passed / (passed+failed+errors) * 100)` rendered as `{n}%`; hint `\"{passed} pass · {failed} fail\"` (+ `· {errors} err` when `errors > 0`) + `overview-sbom-count` span showing `· {components} SBOM` when `sbom.present`.\n- not present: muted **\"No runs\"**, hint \"No eval runs yet\"; hidden `overview-sbom-count` span carries the component count (or 0).\n\n### Deployment — `DeploymentCell`, mounted only when `vercelConnected`\n`Link to=\"/services\"`, `data-testid=\"overview-deployment-cell\"`, icon `CloudUpload`. Scoping: `getLastDeploy(path)` (per-project local store) → matched against `useVercelDeployments` data by `id` or `url`; if not found in the live list a synthetic `{state:'READY', ...stored}` is shown. A never-deployed project shows nothing here (the account's global latest deploy is deliberately NOT surfaced).\n- Status `Badge`: variant from `deploymentStatusVariant(state)` — `READY`→success; `BUILDING`/`QUEUED`/`INITIALIZING`→warning; `ERROR`/`CANCELED`→danger; else secondary. Label = raw uppercased state.\n- URL: a button (stops nav propagation) → `bridge.openExternal('https://'+url)` with trailing `ExternalLink`; \"No URL yet\" when absent. \"No deployments yet.\" when `deployment` undefined.\n\n## Next\n\n`Section title=\"Next\"`. Renders `WhatsNextTile` with `metrics`, `release`, `obs`, `suppressTopTo={alerts[0]?.to}`, `hideHeader` (Operator owns the section header). `data-testid=\"ov-whats-next\"`.\n\nSuggestions from `suggestNext(state)` (rule engine `lib/ai/rules.ts`, pure/no-network). `toOverviewState` maps panel data: release counts (forcing ≥1 on blocked/red when `overall_status` matches), promptfoo last-run/failed/errors + `daysSince`, sbom present/components, and `obs.errors_last_24h → issues.openCritical` / `obs.runs → issues.openTotal`. Output: filtered (drops `all-clear` unless it is the only one), sorted by severity (`critical < warning < info`), the top row dropped when `sorted[0].to === suppressTopTo`, then sliced to 3.\n\nEach row: severity dot (`critical`=`bg-danger`, `warning`=`bg-warning`, else muted) + `cleanLabel(label)` + trailing `ChevronRight` when the suggestion has a route; row is a `Link to={s.to}` when routed, else a static div. Loading → 3 pulse bars; empty → \"Nothing to recommend right now.\" HARD RULE enforced in source: no chatbot widget, no textarea, no \"Ask AI\" here.\n\nRelevant rules (`suggestNextRule`): `release-blocked` (critical → `/release`), `release-red` (critical → `/release`), `evals-stale` (>7d warning / >14d critical → `/observability`), `evals-never` (warning → `/observability`), `evals-failed` (warning → `/observability`), `issues-critical` (from obs errors, critical → `/issues`), `sbom-missing` (warning → `/release`), `all-clear` (info, no route).\n\n## Recent activity\n\n`Section title=\"Recent activity\"`, action **\"View all\"** `Link to=\"/activity\"` (the one accent affordance in the section). `Card` p-0. Source: `useRecentCommits(6)` — real chronological git history.\n- Loading: three pulse bars.\n- Populated: `<ul>` of rows, each — `GitCommit` icon, `humanizeCommitSubject(c.subject)` (neutral body text, hover underline; not an accent link), mono short hash, right-aligned `relativeTime`.\n- Empty: \"No commits found. This isn't a git checkout, or git isn't on PATH.\"\n\nNote: the 90-day commit heat-strip is intentionally omitted — the activity bridge caps history at 50 commits and exposes no per-day count series, so a real strip cannot be drawn (per source comment).\n\n## Navigation map (Operator)\n\n| Surface | Target |\n|---|---|\n| AlertStrip \"gates failing/blocked\" | `/release` |\n| AlertStrip \"errors in 24h\" | `/observability` |\n| AlertStrip \"eval failures\" | `/observability` |\n| ShipCard button | `/release` |\n| ReleaseGatesCard | `/release` |\n| Tests / Cost / Evals vitals | `/observability` |\n| Build vital | `/pipeline` |\n| DeploymentCell card | `/services`; URL button → external browser |\n| EmptyTestsEvalsPrompt button | `/observability` |\n| WhatsNextTile rows | per-suggestion route |\n| Recent activity \"View all\" | `/activity` |\n\nNo panel-local keyboard shortcuts; global g-prefix `g o` navigates here. No edge interactions beyond hover/click. Empty-state behavior is exhaustive above (combined Tests+Evals collapse, CI idle, no-deploy suppression, no-commits message).\n\n## Guided variant (`OverviewNewbie`, `data-testid=\"overview-newbie\"`)\n\nSingle centered column (`max-w-[760px]`). Same data hooks, different framing:\n- Header **\"How your project is doing\"** + one `ProjectLede` line = standing lede + `describeShape(shape, total_loc)` (e.g. \"A Node.js web app. Start with `npm start`.\"). Folder-not-picked fallback prompts to pick from the top bar.\n- `NextStepCallout`: single top suggestion from `suggestNext` (ranked by severity), severity dot + label + hover arrow → suggestion route (default `/repo` when none). `release-blocked` rendered amber (awaiting input), not rose.\n- `ReleaseStatusCard` (`newbie-release-status`, → `/release`): \"Is it ready to ship?\" + version badge; big number = `gate_count` when green / `amber` when pending / `blocked` otherwise; status `Badge` (green→Ready, amber→Pending, red→Failing, blocked→Blocked[warning]); plain-English `liveBlurb`.\n- `NumbersThatMatter` (`newbie`): three MetricCards → `/observability` — **AI spend today** (`cost_today_usd`), **Progress** (`cycle_log.commits_landed`), **Tested** (`runs_last_24h`); each shows a zero-caption instead of a headline zero. When all three loaded and zero → single muted line \"No automation activity in the last 24h.\" (`newbie-numbers-quiet`).\n- `RecentActivityCard` (`newbie-recent-activity`): `useRecentCommits(3)`, \"Lately\" list; empty → \"No commits in the last 24h…\".\n- `Explore` row: Workboard (`/issues`), Read the repo (`/library`), Tutorials (`/tutorials`).\n- Deliberately omits SBOM counts, gate-by-gate breakdown, LOC."
  },
  {
    "key": "report",
    "title": "Report",
    "guided": "# Report\n\n> [!INFO] **What this panel is**\n> The **Report** panel (in Guided mode it is labelled **Project report**) reads the project you have open and writes up what it is, what it is built with, what it connects to, and how healthy it is. Almost all of it comes from *reading your files directly* - no internet, no AI required. Think of it as the page a careful new teammate would write after spending an afternoon walking your codebase.\n\nOpen it from the sidebar (**Project report**, under *Your project*) or jump there with the command palette (`Ctrl+K`).\n\n---\n\n## What you are looking at\n\nWhen you open the Report, Console does a **deterministic scan** of your project. \"Deterministic\" just means *predictable*: the same files always produce the same answer. There is no guessing and no model involved - it walks your folders, opens the files it understands, counts things, and looks for tell-tale signs of how the project is set up.\n\n> [!INFO] **Why \"deterministic\" matters**\n> A lot of developer tools dress up a guess as a fact. Console keeps the two separate. Everything in the top of the Report is *measured* from your files, so you can trust it and re-run it. The only part that involves an AI model is the optional narrative at the end, and it is clearly labelled as such.\n\nThe subtitle under the title summarises the whole project in one line, for example:\n\n```\nTypeScript · 48,210 LOC · 3 services\n```\n\nThat is your main language, your total **lines of code**, and how many outside **services** Console found wired in.\n\n> [!INFO] **What \"LOC\" means**\n> LOC = *lines of code*. It is the rough size of your project measured in lines. It is not a quality measure - more lines is not better - it is just a sense of scale. A weekend project might be a few thousand LOC; a mature app, hundreds of thousands. Console counts lines only in files written in a programming language (it skips images, lock files, and so on).\n\n---\n\n## The identity card\n\nThe first card is the project's **identity**: a health ring on the left, then the title, a one-line description (taken from your README or your `package.json`), the notable frameworks it found, your licence, your current git branch, how active the project is, and its size on disk.\n\n> [!INFO] **What a licence is, and why Console shows it**\n> A **licence** is the legal text that says what other people are allowed to do with your code - copy it, sell it, change it. If there is no licence file, the law's default is \"nobody may reuse this\", which is usually *not* what an open project wants. Console reads your `LICENSE` file and tries to name it (MIT, Apache-2.0, GPL-3.0, and so on). If it says \"no LICENSE\", that is worth fixing before you share the project.\n\n> [!INFO] **What a git branch is**\n> **Git** is the system that records the history of your code as a series of saved snapshots called *commits*. A **branch** is one line of that history you are currently working on - usually `main`. Console shows the branch so you know which version of the project it is describing.\n\nThe health ring shows a number out of 100 and a word: **Healthy** (80+), **Needs work** (50-79), or **At risk** (below 50). We will come back to how that number is built.\n\n---\n\n## Languages and size\n\nIn the Tech-stack area you will see a thin coloured **language bar**. Each colour is one programming language, sized by how much of your code is written in it. Hover any segment to see the exact percentage.\n\n> [!INFO] **Why one project has several languages**\n> Most real projects mix languages. A web app might be mostly TypeScript for the logic, with some CSS for styling and a little Python for a script. The bar shows that mix at a glance. The biggest segment is your **primary language**, the one Console names in the subtitle.\n\nConsole figures this out by opening every code file it recognises, counting its lines, and tallying them per language. It deliberately *ignores* configuration files (like `.json` and `.yaml`) and documentation (`.md`) when measuring \"what this is written in\", so a few large config files don't drown out your actual code.\n\nThe at-a-glance strip near the top gives you four plain numbers: **Files**, **Lines of code**, **Packages**, and **Commits**.\n\n> [!INFO] **What a \"package\" is**\n> A **package** is a self-contained unit of code with its own definition file (like a `package.json`). A small project is one package. A larger project might be split into several - a front-end package, a back-end package, a shared-utilities package. When a project holds many packages in one repository, that arrangement is called a **monorepo**, and Console will say so.\n\n---\n\n## How to run it, and where to start\n\nTwo cards answer the questions every newcomer asks first.\n\n- **How to run** lists the commands that start, build, test, and deploy the project, pulled straight out of your `package.json` scripts (and any `Makefile`). If there is an obvious start command, Console highlights it.\n- **Start here** is a short, numbered reading order: the entry point, the files the most other code depends on, the file that changes most often, and the largest file. It is Console's suggested path through an unfamiliar codebase.\n\n> [!INFO] **Why \"most depended-upon\" is a good place to start**\n> Console builds a **dependency graph** of your project (more on that next). If lots of files rely on one particular file, that file is probably important - changing it affects everything downstream. Reading the heavily-relied-upon files first is the fastest way to understand how a project hangs together.\n\n---\n\n## The dependency graph\n\nA **dependency graph** is a map of *what relies on what*. When one part of your code imports or uses another part, that is a dependency - an arrow from the user to the thing being used.\n\n> [!INFO] **Why this map is useful**\n> Software is mostly about how the pieces connect, not the pieces themselves. The dependency graph turns those invisible connections into something you can see and reason about. Console builds it by reading your `import`/`require` statements and following the trail between packages. (You can explore the full visual version in the **Architecture** panel; the Report uses the same graph quietly, under the hood.)\n\nThe Report uses this graph in three places:\n\n1. **Start here** - to find the most-depended-upon files.\n2. **Risk hotspots** (Operator view) - files that are large, change often, *and* have lots of code depending on them. That combination is where bugs tend to hide.\n3. **The health score** - if the graph contains a **cycle** (package A depends on B, and B depends back on A), that is a design smell, and it pulls the score down.\n\n> [!INFO] **What a dependency \"cycle\" is and why it is bad**\n> A cycle is a loop in the map: A needs B, B needs A. It means the two pieces can't be understood, built, or tested in isolation - they are tangled together. A few cycles are common; many of them make a codebase hard to change safely. Console counts them and warns you.\n\n---\n\n## Detected services, with evidence\n\nA **service** is an outside product your project talks to - GitHub for code hosting, Vercel for deploys, Sentry for error tracking, Stripe for payments, a database, an AI provider, and so on.\n\nConsole finds these *without you connecting anything*, by looking for clues your project already contains:\n\n- a matching dependency installed (for example `@sentry/node`),\n- a known config file present (like `vercel.json`),\n- a server named in your `.mcp.json`,\n- or an environment-variable **name** that matches a known pattern (like `STRIPE_SECRET_KEY`).\n\n> [!INFO] **What an environment variable is - and why Console only reads its name**\n> Secrets like API keys are not written into your code (that would be unsafe). Instead they live in a `.env` file as **environment variables** - `NAME=value` pairs the app reads at runtime. Console reads only the **names** on the left of the `=`, never the values on the right. Seeing a variable called `STRIPE_SECRET_KEY` is enough to say \"this project uses Stripe\" without ever touching the secret itself.\n\nEach detected service shows the **evidence** that found it, and a confidence badge:\n\n- **in use** (high confidence) - a real dependency, config file, or MCP server was found. Strong proof.\n- **maybe** (medium confidence) - only an environment-variable *name* matched. A name in `.env.example` doesn't prove the service is actually wired up, so Console hedges.\n\nIn Guided mode the services are shown as a calm single line (\"GitHub, Vercel, Sentry.\"). You connect them for real over in the **Services** panel.\n\n---\n\n## The health score (0-100)\n\nThe number in the ring is a **health score**: a rough, honest read of how well-tended the project is. It is *not* a grade of your code's cleverness - it measures the engineering hygiene that makes a project safe to work on and ship.\n\n> [!INFO] **Why a health score exists**\n> Good practices are easy to skip when you are moving fast - tests, a licence, a lockfile, continuous integration. Each one you skip is a small future risk. The health score gathers those into one number so you can see, at a glance, what your project is missing. It is a *checklist with teeth*, not a vanity metric.\n\nConsole builds the score by starting at zero and **adding points for good practices it finds**, then **subtracting for real risks**. The good practices and their weights:\n\n| Check | Points | What it means |\n|---|---|---|\n| README (with real content) | 16 | A file that explains the project to a human |\n| Tests present | 16 | Automated checks that catch breakage |\n| Licence file | 12 | Clear reuse terms (see above) |\n| CI configured | 12 | See the callout below |\n| Lockfile | 8 | Pins exact dependency versions so builds are repeatable |\n| Is a git repository | 8 | The project's history is tracked |\n| Test depth (decent ratio) | +8 | Tests are a healthy fraction of the code, not token |\n| `.gitignore` | 6 | Keeps junk out of version control |\n| Docs beyond the README | 6 | Extra documentation |\n| Two or more main authors | +4 | The project isn't dependent on one person |\n\nAnd the risks that **subtract**:\n\n- **Dependency cycles** pull points off (the more loops, the bigger the hit).\n- A **thin test ratio**, a **bus factor of 1**, or a **dormant/abandoned** project each raise a warning (and miss the points a healthy version would earn).\n\n> [!INFO] **What \"CI\" is**\n> **CI** stands for *continuous integration*. It is an automation that runs your tests (and other checks) automatically every time you push code, so a mistake is caught by a robot in minutes rather than by a user in production. Console looks for a CI setup like a `.github/workflows` folder. Having CI is one of the strongest signals that a project is maintained seriously.\n\n> [!INFO] **What \"bus factor\" means**\n> The **bus factor** is a slightly grim industry term: *how many people would have to be hit by a bus before the project is in trouble?* A bus factor of 1 means a single person wrote almost everything - if they leave, nobody else understands it. Console reads your git history to estimate this. It is not about your code's quality; it is about how spread-out the knowledge is.\n\nThe score is capped between 0 and 100. Below the ring, each contributing check is listed with a tick, an info dot, or a warning, and a one-line explanation - so the number is never a mystery. In Guided mode, Console shows **only the checks that need your attention**, to keep the page calm.\n\n---\n\n## Reading the project \"as a book\"\n\nConsole also lays out the project as a readable document: the at-a-glance numbers, a plain \"Built with ...\" sentence, the services as a line, and only the health issues that actually need fixing. The goal is one calm story you can read top to bottom, not a wall of dials.\n\nIf you ever want to read the underlying files themselves, the **Library** panel (\"Read project files like a book\") opens them one at a time, formatted nicely.\n\n---\n\n## The optional AI narrative\n\nEverything above is measured. The Report can *also* ask a connected AI model to read the project and write a richer story - but only if you ask it to, and only if you have set up an AI provider.\n\nIn the **What this is about** card you will see one of two things:\n\n- If no AI is connected: a note explaining that the summary above came from your files, and a **Read deeper with AI** button.\n- If AI is connected: a one-line **tagline**, a **narrative** paragraph, a **What to fix first** list (each item with the file it points at), and a **read by `<model name>`** badge so you know exactly which model wrote it.\n\n> [!INFO] **Why the AI part is separate and optional**\n> An AI model can write a fluent summary, but it can also be confidently wrong. By keeping the measured facts and the AI narrative in separate parts of the page - and labelling which model produced the narrative - Console lets you treat each appropriately. The facts you can rely on; the narrative is a helpful read, clearly attributed. The AI is also handed your real file structure, so its \"what to fix first\" suggestions point at actual paths in your project.\n\n> [!INFO] **\"Bring your own key\"**\n> Console does not ship its own AI. You connect a provider you control (OpenAI, Anthropic, Google, a local Ollama, and so on) in **Settings → AI**. Your key is held by Console's background process and never shown back to the screen. If nothing is connected, **Read deeper with AI** will tell you so - the measured Report still works completely without it.\n\nOnce you have an AI narrative, **Re-read with AI** regenerates it.\n\n---\n\n## Caching and re-studying\n\nThe first scan of a large project takes a few seconds. Console then **caches** the result (keyed to a signature of your file tree) so re-opening the Report is instant. If your files change, the signature changes and Console re-scans automatically.\n\n> [!INFO] **What \"caching\" means**\n> A **cache** is a saved copy of a slow-to-compute answer, kept so you don't have to recompute it. Console saves the Report into a hidden `.refringence-console/` folder in your project. Because it is keyed to your file tree, an out-of-date cache is detected and rebuilt on its own - you don't have to think about it.\n\nThe footer tells you exactly what just happened, e.g. *\"Read 1,240 files in 820 ms, all from the project itself.\"* Two buttons let you force a fresh pass:\n\n- **Re-study** - rebuild the whole measured Report from scratch (ignore the cache).\n- **Re-read with AI** - regenerate just the AI narrative.\n\n---\n\n## Quick recap\n\n- The Report **reads your files** and reports facts: languages, size, packages, run commands, services, and health.\n- The **health score** rewards good engineering hygiene and penalises real risks - it is a checklist, not a vanity number.\n- **Detected services** are found from real evidence, labelled **in use** or **maybe**.\n- The **AI narrative** is optional, clearly separated, and attributed to the model that wrote it.\n- Everything is cached and re-runnable; nothing leaves your machine unless *you* connect an AI provider.",
    "operator": "# Report\n\nRoute `/report`. Sidebar label **Report** (Operator) / **Project report** (Guided). Title `Project report`, icon `ScanSearch`, testid `panel-report`. Source: `views/intel/ProjectReport.tsx`, branching `mode = isNewbie ? 'guided' : 'operator'` inside `ReportBody`. Backed by the **Project Intelligence Engine** (`console-electron/src/main/intel/`): a deterministic Layer-1 profiler plus an optional Layer-2 AI enrichment.\n\n## Data source\n\n| Hook | Bridge call | Returns |\n|---|---|---|\n| `useProjectProfile(root)` | `bridge.intel.profile(root)` | `ProjectProfile \\| null` |\n| `useReprofile(root)` | `bridge.intel.profile(root, { force: true })` | rebuilt `ProjectProfile`, written into the query cache |\n| `useEnrich(root)` | `bridge.intel.enrich(root, { model? })` | `{ ok, intel?: ProjectIntel, error? }`; on `ok` merges `intel` into `profile.ai` |\n\n`useProjectProfile` keys `['intel','profile',root]`, `staleTime` 10 min, `retry: false`, `enabled` only when `root.length > 0`. `root = activeProject.path ?? ''`.\n\nSubtitle (memoised on `profileQuery.data`): `` `${stack.primaryLanguage} · ${metrics.totalLoc.toLocaleString()} LOC · ${N} service(s)` ``; before data loads: `\"A deep read of the project you have open\"`.\n\n### Render states\n- **No project** (`!projectRoot`): `EmptyState` icon `FolderGit2`, \"No project open\".\n- **Loading**: skeletons (one h-28, four h-24 tiles, one h-40) + \"Studying the project…\".\n- **Null profile** (resolved but failed): `EmptyState` icon `ScanSearch`, \"Could not study this project\" + **Try again** (`profileQuery.refetch()`).\n- **Loaded**: `ReportBody`.\n\n## The deterministic scan (`intel/profiler.ts → buildProfile`)\n\nOne bounded tree walk (`scanProject`) + a set of derive-* passes. No AI, no network. Every sub-step degrades to an empty/honest value rather than throwing. Staged build emits 8 mount steps (used by onboarding's streamed mount, not the `/report` fast path): `scan → deps → meta → stack → services → graph → git → health`.\n\n**Walk caps & skips:**\n- `MAX_FILES = 40000`, `MAX_DEPTH = 24`; hitting either sets `metrics.truncated = true`.\n- Per-file LOC/TODO read only when size `<= MAX_LOC_BYTES` (1.5 MB).\n- `SKIP_DIRS`: `node_modules, .git, dist, build, .refringence-qa, .refringence-console, __pycache__, .pio, .cache, .venv, tools-src, vcpkg, src-qt, out, .next, .parcel-cache, target, vendor, .turbo, .svelte-kit, coverage, .nuxt, .output`.\n- Dotfiles skipped except allow-list `.github, .gitignore, .cursor, .mcp.json, .npmrc` and any `.env*`.\n\n## Languages / LOC (`metrics`, `stack.languages`)\n\n`CODE_LANG` maps ~50 extensions to a language (TS/JS variants, Python, Rust, Go, Java/Kotlin, C/C++, C#, Ruby, PHP, Swift, Scala, Dart, Elixir, Shell, PowerShell, Lua, Julia, R, Vue, Svelte, HTML, CSS/SCSS/etc., SQL, Solidity, shaders). LOC = `body.split('\\n').length` per matched file. Config exts (`.json/.yaml/.yml/.toml/.ini/.xml/.env`) counted as `configFiles` only; `.md/.mdx/.rst` counted as `docsCount`/`docsLoc`; neither contributes to the language histogram. Test vs source LOC split by `TEST_PATH_RE`.\n\n`LanguageStat = { language, files, loc, bytes, share }`, `share = loc / totalLoc`, sorted desc; `primaryLanguage = languages[0] ?? 'Unknown'`. `metrics = { fileCount, totalBytes, totalLoc, sizeLabel, truncated, ratios }`. `sizeLabel` is human bytes (B/KB/MB/GB). `ratios = { sourceLoc, testLoc, docsLoc, configFiles, testToSource, docsToSource }` (ratios rounded to 2 dp).\n\n**UI:** `LanguageBar` shows top 6 languages, segment width `max(1, share*100)%`, fixed muted palette `#60a5fa #38bdf8 #2dd4bf #fbbf24 #94a3b8 #64748b`, hover title = exact %. `metrics.truncated` is carried but the Report does not surface a banner for it.\n\n## Stack detection (`deriveStack`, deps name-only)\n\nDeps collected name-only from every `package.json` (monorepo-aware, `findPackageJsons` cap 60, depth ≤6), `requirements.txt`, `[project]`/deps in `pyproject.toml` (first 8 KB), `[dependencies]` in `Cargo.toml`. Versions resolved by `collectDepVersions` (declared ranges, overridden by `package-lock.json` installed versions).\n\n- **frontend / backend**: anchored regex over lowercased dep names (Next.js, React, Vue/Nuxt, Svelte/SvelteKit, Angular, SolidJS, Astro, Remix, React Native, Tailwind; Express, Fastify, NestJS, Koa, Hono, Flask, Django, FastAPI, Rails, Gin, Axum/Actix).\n- **runtimes**: manifest presence **or** ≥8% LOC share (Node.js, Deno, Bun, Python, Go, Rust) + Electron/Tauri from deps.\n- **buildTools**: Vite, Webpack, Rollup, esbuild, Turborepo, Nx, Cargo, Make, electron-builder.\n- **packageManager**: from lockfile precedence (pnpm > yarn > bun > npm > poetry > cargo > go modules).\n- **notableFrameworks**: `FrameworkVersion[]` from a curated `NOTABLE_FRAMEWORKS` table joined to the resolved version map; UI chip shows `name + major`.\n\n**UI (Operator only)** — *Tech stack* card: `LanguageBar` + columns Frontend / Backend / Runtimes / Build tools / Package manager; second row (when present) Data layer (`detail.dataLayer.orm + engines`), API style (`detail.apiStyle`), Testing + quality (`detail.testing.frameworks/linters/formatters/typecheck`). Empty columns render \"None detected\". **Guided** collapses this to one \"Built with\" sentence (`frontend ∪ backend ∪ buildTools`, runtimes) + the bar.\n\n`detail.dataLayer` (Prisma/Drizzle/TypeORM/Mongoose/Sequelize/Knex/Kysely/SQLAlchemy/Alembic/Diesel/sqlx; Postgres/MySQL/SQLite/Redis/MongoDB), `detail.testing`, `detail.apiStyle` (tRPC/GraphQL/gRPC/OpenAPI/WebSocket/REST) all derived from dep names (+ relFiles for OpenAPI).\n\n## Dependency graph\n\n`depGraph` is computed via the **shared arch-graph cache** — `getGraph(root, { includeExternal: false, allLanguages: false }, false)` — the same options as the Architecture panel's default, so the profiler warms the cache the panel reads. On failure it degrades to `{ nodes:[], edges:[], cycles:[], truncated:false, fileCount:0 }`.\n\n`DependencyGraph = { nodes: DependencyNode[], edges: DependencyEdge[], cycles: string[][], truncated, fileCount }`. Package→package edges from JS/TS imports (Python/Rust/Go/Java/Kotlin only when `allLanguages` — off here). The graph is **not rendered** in the Report; it feeds three derived products:\n\n- **`detail.readingOrder`** (`ReadingStep[]`, → *Start here*): entry point (shell/app package), top-3 most depended-upon nodes (in-degree), `hotFiles[0]` (\"changes most often\"), largest node by LOC; deduped, cap 8.\n- **`detail.hotspots`** (`Hotspot[]`, → Operator *Build + risk*): non-external nodes scored `round(log2(loc+1) · (1+churn/200) · (1+inDeg) · 10)/10`, top 6. Title attr shows `loc / churn / depended-on`.\n- **`health.cycles`** signal: `graph.cycles.length` subtracts `min(10, cycles*3)` from the score and adds a `risk` signal.\n\n`detail.commands` (`RunCommand[]`) parsed from every `package.json` `scripts` block (cap 80) + `Makefile` targets, grouped run/build/test/quality/data/deploy/other by name regex. `detail.workflows`, `detail.containers`, `detail.envGroups`, `detail.todoCount`, `detail.release` (latestTag/tagCount/hasChangelog from git + scan) round out the detail block.\n\n## Detected services with evidence (`intel/serviceCatalog.ts`)\n\n`detectServices({ envNames, deps, configFiles, mcpNames })` is a pure function over a curated `SERVICE_CATALOG` (~21 defs). It **never reads env values, only names**. Each `ServiceDef` declares `envKeyPatterns` (anchored, whole-name), `depPatterns` (substring), `configFiles`, `mcpNames`, plus `category / powers / pricing / docsUrl`.\n\nCatalog (id → category → powers): github→repo→*Repo + Workboard*; vercel/netlify→hosting-frontend→*Release*; railway/fly/render→hosting-backend→*Release*; supabase/upstash→database; sentry→observability→*Observability (errors)*; langsmith→observability→*Evals*; posthog/plausible→analytics→*Overview counters*; stripe→payment; slack→comms→*Workboard*; openai/anthropic/google-ai/openrouter/elevenlabs→ai-model→*AI provider*; exa→search.\n\n**Confidence rule:** any of `mcp / config / dep` evidence ⇒ `high` (\"in use\"); env-name-only ⇒ `medium` (\"maybe\"). `via` ordered strongest-first `mcp → config → dep → env`; `evidence` strings (e.g. `dep: @sentry/node`, `env: STRIPE_SECRET_KEY`) capped at 6. Sort: GitHub first → panel-powering → high-confidence → alphabetical.\n\n**`DetectedService = { id, name, category, confidence, via, evidence[], powers, pricing, docsUrl }`.**\n\n**UI:** Operator *Services wired* card — count badge + `ServiceCard` per service: name, `in use`/`maybe` badge (success/warning), category label, `· powers <x>` when set, evidence joined `·`. Empty: \"No external services detected. This looks like a services-light project.\" **Guided** *Services* card collapses to `services.map(name).join(', ')` or a connect-from-Services prompt. (Connecting for real lives in `/services`; detection here is read-only.)\n\n## Health score 0-100 (`deriveHealth`)\n\nDiscriminator, not a presence checklist. Starts at 0, **adds** weights for practices, **subtracts** for risks, clamps `[0,100]`. Each check pushes a `HealthSignal { id, label, severity: good|info|warn|risk, detail }`.\n\n| id | label | weight | bad severity |\n|---|---|---|---|\n| `readme` | README (`present && wordCount>60`) | 16 | warn |\n| `license` | License | 12 | warn |\n| `tests` | Tests (`inv.hasTests`) | 16 | **risk** |\n| `ci` | CI | 12 | info |\n| `gitignore` | .gitignore | 6 | warn |\n| `lockfile` | Lockfile | 8 | warn |\n| `docs` | Docs (beyond README) | 6 | info |\n| `git` | Version control (`.git` present) | 8 | **risk** |\n\nConditional signals:\n- `test-ratio` (only if `hasTests`): `+8` when `ratios.testToSource ≥ 0.1` (good), else `warn` \"coverage likely thin\".\n- `bus-factor`: `+4` when `busFactor ≥ 2` (good); `warn` when `busFactor === 1 && commitCount > 20`.\n- `cycles`: `-min(10, cycles*3)`, `risk`, when `graph.cycles.length > 0`.\n- `activity`: `warn` when git activity is `dormant`/`abandoned` (no points either way).\n\n`hasTests = testing.frameworks.length>0 || scan.testLoc>0 || cap.hasTests` (monorepo-aware, nested tests count). `hasEvals` from eval frameworks / promptfoo / capability. Activity bucket from last-commit recency: `<30d active, <90d slowing, <365d dormant, else abandoned`. Bus factor = authors needed to reach 50% of commits (`git shortlog -sn`).\n\n**UI band (`healthTone`):** ≥80 `Healthy`/success, ≥50 `Needs work`/warning, else `At risk`/danger. `HealthRing` is an SVG donut (r=26, stroke = tone colour) with the score centred. Operator *Project health* card lists every signal (icon by severity: `good`→CheckCircle2, `info`→Info, `warn`→AlertTriangle, `risk`→XCircle) with a header line `<CI provider|no CI> · N docs · README (M sections)|no README`. Guided *Health* card shows the `score/100 · label` badge and **only** `severity !== 'good'` signals (or \"Every basic check passes. Nice.\").\n\n## Optional AI narrative (`ProjectIntel`, Layer 2)\n\n`profile.ai: ProjectIntel | null`. Triggered only by **Read deeper with AI** / **Re-read with AI** via `useEnrich` → `bridge.intel.enrich` → `enrichProfile(profile, model?, nowIso)`. On success `intel` is merged into `profile.ai` and persisted; returns `{ ok:false, error }` when no provider is connected (rendered as a connect prompt). Provider is bring-your-own-key (Settings → AI); keys live main-side, never returned to renderer.\n\n`ProjectIntel = { narrative, tagline, systemDiagram, suggestions[{title,detail,priority}], packageNotes[{path,oneLiner}], changeFirst[{title,rationale,evidencePath}], runGuide, model, generatedAt }`.\n\n**`AboutSection` (\"What this is about\"):**\n- No `ai`: explanatory note + **Read deeper with AI** button (outline; pulse icon while pending). `enrichError` shown in warning text.\n- With `ai`: `tagline` (bold), `narrative`, then **What to fix first** (`changeFirst`, each with `evidencePath` basename) — falling back to **Suggestions** (`suggestions`, priority badge high/medium/low → danger/warning/info) when `changeFirst` empty; `read by <model>` badge in the header; **Re-read with AI** (ghost) button.\n\n`ai` also feeds: `runGuide` → *How to run* card prose; `packageNotes` → per-package one-liners in *Packages* and *Start here*; `systemDiagram` → Architecture **Systems** view (not the Report). Enrichment is preserved across deterministic/version rebuilds (`getProfile`/`runMount` carry `cached.profile.ai` forward) so it is never silently wiped.\n\n## Operator-only sections (beyond shared cards)\n\nShared by both personas: identity card, `AboutSection`, *How to run*, *Packages* (`PackagesCard`, kind badge app/lib/test/tooling/docs/unknown, AI one-liner or description or `role · frameworks`), *Start here*. Operator adds:\n- **Stat tiles** (4): Files (`fileCount`, hint `sizeLabel`), Lines of code (`totalLoc`, hint primary language), Packages (`shape.packageCount || packages.length || 1`, hint monorepo/single), Commits (`git.commitCount` or n/a, hint contributors).\n- **Tech stack** card (above).\n- **Services wired** + **AI tooling** (`aiTooling`: MCP servers from `.mcp.json`/`.cursor`/`.vscode`, AI SDKs, eval frameworks, agent configs `.claude`/`CLAUDE.md`/`AGENTS.md`/`.cursor`/copilot).\n- **Project health** card.\n- **Build + risk** (`InfraCard`, rendered only when any of hotspots/workflows/dockerfiles/todoCount present): Risk hotspots (top 3), CI workflows (file · N jobs · deploys), Containers (deduped base images), Markers (`todoCount` TODO/FIXME · tags · CHANGELOG).\n\n## Footer / caching / re-study\n\nFooter: `Read <fileCount> files in <durationMs> ms, all from the project itself.` + **Re-study** (outline, `useReprofile`, spinner while pending).\n\n**Cache** (`ipc/intel.ts`): written to `<root>/.refringence-console/intel-profile.json` as `{ v: PROFILE_VERSION (=4), profile }`, keyed to `cacheSignature(root)` (mtime-based). `getProfile` returns cache only when `version === 4 && signature` matches and `force !== true`; otherwise rebuilds and carries `cached.profile.ai` forward. `PROFILE_VERSION` bump invalidates all caches (signature alone would miss a profiler code change). `resolveRoot` traversal-guards the path; every handler returns `null`/`{ok:false}` rather than throwing.\n\n## Keyboard / navigation\n\nNo panel-local chord. Reached via sidebar, command palette (Panels group), or onboarding's **report** step (`ReportStep`, which renders this profile and offers \"Looks good\"). Re-running onboarding re-mounts the streamed profiler via `bridge.intel.mountStart`."
  },
  {
    "key": "workboard",
    "title": "Workboard",
    "guided": "# Workboard\n\nThe Workboard is where the open work for your project lives. It pulls the issues from your GitHub repository and arranges them so you can see, at a glance, what is blocking a release and what can wait. You can also flip it to read issues that came in over Slack.\n\n> [!INFO] What is an \"issue\"?\n> In software, an *issue* is a single tracked piece of work or a problem: a bug to fix, a feature to build, a question to answer. GitHub gives every repository an issue tracker, and each issue gets a number (like `#42`), a title, a description, optional labels, and a comment thread. A *workboard* (or *issue board*) is just a visual way to look at all those issues at once instead of reading a flat list. Think of it as a wall of sticky notes, grouped by how urgent each one is.\n\nYou reach it from the sidebar (in Guided mode it is called **What to fix**) or with the keyboard chord `g` then `w`.\n\n---\n\n## Where the issues come from\n\nConsole does not store your issues. It reads them live from GitHub every time you open the panel, using a small command-line program called the **GitHub CLI** (the `gh` command).\n\n> [!INFO] What is the GitHub CLI?\n> `gh` is GitHub's official command-line tool. It talks to GitHub for you and, importantly, it reuses the login you already set up once with `gh auth login` — so Console never has to ask you for a password or token of its own. When Console needs your issues, it runs `gh issue list` behind the scenes and reads back the result. This is why everything you see here matches github.com exactly: it *is* GitHub, just shown inside Console.\n\nConsole figures out *which* repository to read by looking at the project folder you opened. Every git project remembers a remote address (its \"origin\") that points at GitHub; Console reads that address and asks GitHub for the issues of that exact repo. So the Workboard always shows your project's issues, never someone else's.\n\n> [!INFO] What is a \"remote\" / \"origin\"?\n> When you clone a project from GitHub, your local copy keeps a bookmark back to the GitHub copy. That bookmark is called a *remote*, and the default one is named `origin`. It is just a URL like `github.com/you/your-project`. Console reads that URL to know which repo to load.\n\n### If you see a warning banner\n\nTwo yellow/red banners can appear at the top:\n\n- **\"gh CLI not available\"** — the `gh` program is not installed on your computer. The banner links to **cli.github.com**; install it, then come back. Nothing is broken; Console simply has no way to reach GitHub yet.\n- **\"gh not authenticated\"** — `gh` is installed but you have not logged in. Open a terminal and run `gh auth login`, follow the prompts, and reload the panel.\n\n> [!INFO] Why a separate login step?\n> Console deliberately does *not* invent its own GitHub login. Reusing `gh`'s login means there is no extra password or access token for you to manage, store, or accidentally leak. One login, used everywhere.\n\n---\n\n## The two views: board and table\n\nAt the top right you will find a **GitHub / Slack** source toggle and, for GitHub, a **Kanban / Table** view toggle and a search box. Start with GitHub.\n\n### Kanban board (the default)\n\nThe board lays your issues out in six columns, ordered by urgency:\n\n**Critical · High · Medium · Low · Phase trackers · Other**\n\n> [!INFO] What is a Kanban board?\n> *Kanban* (a Japanese word for \"signboard\") is a way of managing work where every task is a card and every card sits in a column that represents its status or priority. You move a card from one column to another as its situation changes. It makes the shape of your work visible: a tall \"Critical\" column instantly tells you that you have a problem. Console uses the columns for *severity* (how bad / how urgent), not for progress.\n\nEach column shows a count and a coloured dot so you can read severity without reading words:\n\n- **Critical** — a solid red dot. Things that block shipping. Look here first.\n- **High** — a solid amber dot. Important, but not blocking a release today.\n- **Medium** — a hollow ring. Worth doing, no rush.\n- **Low** — a solid green dot. Minor.\n- **Phase trackers** — a small square. These are big \"umbrella\" issues that track a whole phase of work rather than one fix.\n- **Other** — anything Console could not sort into a severity.\n\nThe two low-traffic columns on the right (**Phase trackers** and **Other**) start *collapsed* into a narrow strip to keep the board calm. Hover over one, or start dragging a card, and it opens up.\n\n> [!INFO] How does Console decide the severity?\n> It reads the issue's *labels*. A label is just a tag you attach to an issue on GitHub — like `bug`, `documentation`, or `severity:critical`. Console looks for labels named `severity:critical`, `severity:high`, `severity:medium`, and `severity:low` and drops the card into the matching column. Issues whose labels start with `phase:` go to **Phase trackers**. Anything with no severity label lands in **Other**. So the board is only as organised as your labels are — which is the whole point of the next section.\n\n### Table view\n\nClick **Table** to see the same issues as a plain spreadsheet: number, title, labels, author, comment count, and when it was last updated. This is handy when you just want to scan a lot of issues quickly or sort them in your head by date. Clicking any row opens the issue, exactly like clicking a card.\n\n---\n\n## Ranking and re-prioritising: drag a card\n\nHere is the part that actually changes things. On the board you can **drag a card from one column into another** to change its severity.\n\nSay a bug you marked \"Low\" turns out to be blocking your release. Grab its card (use the small grip handle that appears on hover at the left of the card) and drop it into the **Critical** column. Empty columns even say *\"Drop here to mark as Critical\"* so you know where to aim.\n\n> [!INFO] What just happened underneath?\n> When you drop a card into a new severity column, Console does the same thing you would do by hand on github.com: it *removes* the old `severity:low` label and *adds* the new `severity:critical` label, using `gh issue edit`. The change is real and permanent — refresh github.com and you will see it. This is called *relabelling*. The board is not a private scratchpad; it is a direct, two-way window onto your repo's labels.\n\n> [!INFO] What does \"optimistic\" mean here?\n> The card jumps to its new column *instantly*, before GitHub has actually confirmed the change. That snappy, no-waiting feel is called an *optimistic update* — the app assumes the change will succeed and shows it immediately. If GitHub then rejects it (you are offline, you lack permission, the label does not exist), Console quietly slides the card back to where it was and shows a small error message at the bottom of the screen. So you never have to wonder whether it really worked: success stays put, failure undoes itself.\n\nIf you would rather not drag, every card has a **⋮ menu** (top-right corner on hover) with **Move to → Critical / High / Medium / Low / Other**. Same effect, no dragging.\n\nA couple of honest details:\n\n- Moving a card *into or out of* **Phase trackers** or **Other** does not change any `severity:` label (those columns are not severities), so Console tells you \"Moved, no severity label changed.\" The card stays where you put it for this session, but nothing is written to GitHub.\n- You can also drag a card up or down *within* a column to reorder it. That reordering is just for your eyes during this session — GitHub has no concept of \"card order\", so it is not saved.\n\n> [!INFO] What does \"drag-rank\" really buy me?\n> Two things. First, triage: pulling the genuinely urgent issues up into Critical/High makes the rest of Console honest — the Overview and Release panels count \"Critical\" issues, so your labels drive the whole app's sense of \"are we ready to ship?\". Second, shared truth: because the change lands on GitHub, your teammates see the new priority too. You are not maintaining a second, private to-do list that drifts from reality.\n\n---\n\n## Opening an issue\n\nClick anywhere on a card (or a table row) and a panel slides in from the right with the full issue:\n\n- the number, who opened it, and when;\n- the title;\n- all of its labels;\n- the full description, with formatting, code blocks, and links rendered properly;\n- every comment, with author and timestamp;\n- a **View on GitHub** button to jump to the issue in your browser.\n\n> [!INFO] Why read it inside Console instead of on GitHub?\n> Console fetches the issue with `gh issue view`, which means you can read the whole thread — description and comments — without leaving the app or losing your place on the board. When you *do* want to act on it (close it, comment, edit), **View on GitHub** takes you to the real page where GitHub's own tools live. Console reads and relabels; GitHub remains the place you write.\n\nThe description and comments are written in **Markdown**, and Console renders it for you.\n\n> [!INFO] What is Markdown?\n> Markdown is a simple way to format plain text: `**bold**`, `# headings`, `- bullet lists`, and so on. GitHub issues are written in it. Console turns that raw text into formatted, readable output — the same way GitHub does — so you do not have to read the raw symbols.\n\n---\n\n## Searching and filtering\n\nIn GitHub view, the search box at the top filters the board and table live as you type. It matches on three things at once:\n\n- the issue **title**,\n- the issue **number** (type `42` to find `#42`),\n- and any **label** name.\n\nThe subtitle next to the panel title keeps a running count — \"12 of 37 open\" — so you always know how much you are looking at versus how much exists. The Workboard fetches up to 100 open issues at a time.\n\n---\n\n## Slack: issues that arrived as messages\n\nFlip the source toggle from **GitHub** to **Slack** and the board re-reads from your Slack workspace instead.\n\n> [!INFO] Why would issues come from Slack?\n> In a lot of teams, the first report of a bug is not a tidy GitHub issue — it is someone typing \"the login page is broken again\" into a Slack channel. Console can watch the channels you choose and surface those messages here as a second source of \"work to look at\", so nothing important gets lost in the chat scroll.\n\nThe Slack view groups messages by **team**: **Tech** (engineering reports — bugs, blockers, regressions), **Non-tech** (product, design, ops), and **Test** (QA findings). Each row shows the channel it came from, the message title, a severity dot, the date, and an **Open** button that jumps straight to that message in Slack.\n\nIf Slack is not connected yet, you will see a calm empty state with an **Open Services** button. Head there, paste a Slack bot token, and tag each channel as Tech, Non-tech, or Test. After that, reports flow into this view.\n\n---\n\n## Guided vs Operator\n\nYou are reading the Guided layout, which simplifies the board into three plain buckets — **Critical**, **High**, and **Other** — with a one-line explanation under each and a big, calm row for every issue. There is no dragging here; the goal is to read and understand your work, not to micro-manage labels.\n\nWhen you are ready for the full six-column board with drag-to-reprioritise, the per-card move menu, and the table view, switch to **Operator** mode (there is a **Switch to Operator view** link at the bottom of the board, or use the persona pill in the top bar).\n\n---\n\n## Quick reference\n\n| You want to… | Do this |\n|---|---|\n| Open the Workboard | Sidebar **What to fix**, or press `g` then `w` |\n| Read an issue in full | Click the card or row |\n| Change an issue's priority | Drag its card to another column (Operator mode) |\n| Jump to GitHub | Open an issue, then **View on GitHub** |\n| Find one issue | Type its title, number, or label in the search box |\n| See Slack reports | Toggle the source from **GitHub** to **Slack** |",
    "operator": "# Workboard\n\nRoute `/issues`. Sidebar label: **Workboard** (Operator) / **What to fix** (Guided). Nav chord `g w`. Source file: `views/issues/IssuesPanel.tsx`, with `WorkboardNewbie.tsx` (Guided) and `SlackBoard.tsx` (shared Slack source). Panel `data-testid=\"issues-panel\"`.\n\nA live read of the open project's GitHub issues, rendered as a six-column severity kanban (Operator) or three-bucket list (Guided), plus a Slack source. All GitHub data is fetched on demand through the `gh` CLI subprocess in the Electron main process — Console persists no issue state of its own. Cross-column drag writes back to GitHub via `gh issue edit`.\n\n## Persona branch\n\n`IssuesPanel()` reads `usePersonaMode()`: `isNewbie` → `<WorkboardNewbie/>`, else `<IssuesPanelSeasoned/>`. Both share the Slack source (`SlackBoard`) and the right-side `IssueDetailSheet` (each persona has its own copy; identical IPC source, Guided's omits the labels row).\n\n## Data sources\n\nAll via `lib/queries/issues.ts` over `bridge.issues.*` (`lib/bridge.ts`), backed by `console-electron/src/main/ipc/issues.ts`.\n\n| Hook | Bridge / IPC | gh command | Notes |\n|---|---|---|---|\n| `useIssuesHealth()` | `issues.health()` → `console:issues.health` | `gh --version`, `gh auth status` | `staleTime` 5 min. Returns `{ghAvailable, ghVersion, repo, authStatus:'ok'\\|'no-auth'\\|'unknown', error?}`. `repo` is the literal string `(from the open project)` unless `REFRINGENCE_CONSOLE_REPO` is set. |\n| `useIssuesList({state,limit,…})` | `issues.list(opts)` → `console:issues.list` | `gh issue list --repo <nwo> --state <s> --limit <n> --json number,title,state,url,createdAt,updatedAt,author,assignees,labels,milestone,comments [--label <l>]` | Panel calls with `{state:'open', limit:100}`. `staleTime` 60s. `enabled` only when a `projectRoot` or explicit `repo` exists. |\n| `useIssueDetail(num)` | `issues.detail(num, projectRoot)` → `console:issues.detail` | `gh issue view <num> --repo <nwo> --json …,body` | `staleTime` 60s. `enabled` when `num !== null && num > 0`. Returns `IssueDetail` (`IssueRow` + `body` + `comments[]`) or `null`. |\n| `useIssueRelabel()` | `issues.relabel({number,addLabels,removeLabels})` → `console:issues.relabel` | `gh issue edit <num> --repo <nwo> [--add-label L]… [--remove-label L]…` | Mutation. `onSuccess` invalidates `['issues','list']`. Returns `{ok, error?}`. No-op guard returns `{ok:true}` when both label arrays empty. |\n\n**Repo derivation** (`deriveRepo`): runs `git -C <projectRoot> remote get-url origin`, parses owner/name via `/github\\.com[/:]([^/]+)\\/(.+?)(?:\\.git)?$/i`. Resolution order for every call: explicit `opts.repo` → derived from origin → `REFRINGENCE_CONSOLE_REPO` env → if none, `list`/`detail` return `[]`/`null`, `relabel` returns `{ok:false, error:'No GitHub repo for this project (no origin remote).'}`. `projectRoot` is injected from `useActiveProject().project.path` by the query hooks.\n\n**gh runner**: `spawn('gh', args, {shell:false})`, timeouts — version/auth 5s, list/detail 30s, relabel 20s. Non-zero exit → `list`/`detail` log + return empty; `relabel` returns first 3 stderr/stdout lines joined by ` / `. `MAX_ISSUES_PER_FETCH = 100` (clamps `limit`).\n\n## Header (`PanelHeader`)\n\n- Icon `KanbanSquare`, title **Workboard**, `testid=\"workboard-panel-header\"`.\n- Subtitle (GitHub source): repo chip (`issues-repo`, `health.data.repo`) + `\"{filtered.length} of {issues.data.length} open\"`. Slack source: `\"Pulled from Slack\"`.\n- Controls: `SourceToggle` (always). GitHub only: filter input + Kanban/Table `ViewToggle`.\n\n### SourceToggle (`SlackBoard.tsx`)\n`data-testid=\"workboard-source-toggle\"`, buttons `workboard-source-github` / `workboard-source-slack`, `role=\"tablist\"`, `aria-pressed`. State `WorkboardSource = 'github'|'slack'`, local to the panel, default `'github'`.\n\n### Filter input (GitHub only)\n`data-testid=\"issues-filter\"`, `type=\"search\"`, placeholder `\"title, #num, label\"`. `useMemo` over `issues.data`: case-insensitive substring match against `title`, `String(number)`, or any `label.name`. Empty filter → all rows. Drives the header count.\n\n### ViewToggle (GitHub only)\n`issues-view-kanban` / `issues-view-table`, `aria-pressed`. State `ViewMode = 'kanban'|'table'`, default `'kanban'`.\n\n## Banners & states (GitHub source)\n\n- `gh` unavailable (`health.isSuccess && !ghAvailable`): error banner (`issues-banner-error`) with install link to `https://cli.github.com` (via `bridge.openExternal`) + `gh auth login` hint.\n- Authed-but-not-logged-in (`ghAvailable && authStatus !== 'ok'`): warn banner (`issues-banner-warn`), run `gh auth login`.\n- `issues.isLoading` → `KanbanSkeleton` (six-column silhouette, fixed greyed counts critical2/high3/medium2/low2/phase3/other2, `issues-kanban-skeleton`).\n- `issues.isError` → `Empty tone=\"error\"`: `\"Failed to fetch issues via gh: {error}\"`.\n\n## Severity model\n\n`severity(issue)` reads lowercased label names:\n- `severity:critical` → `critical`\n- `severity:high` → `high`\n- `severity:medium` → `medium`\n- `severity:low` → `low`\n- any label `startsWith('phase:')` → `phase`\n- else → `other`\n\n`severityLabel(s)` maps back to the `severity:*` string for `critical/high/medium/low`, and `null` for `phase`/`other` (so moves to/from those columns write no label).\n\n`SEVERITY_PRIORITY = ['critical','high','medium','low','phase','other']`. Dots (`SEVERITY_DOT`): critical solid `bg-danger`, high solid `bg-warning`, medium hollow neutral ring, low solid `bg-success`, phase `rounded-[2px]` muted square, other solid muted dot.\n\n## Kanban view (`KanbanView`)\n\n`data-testid=\"issues-kanban\"`. `@dnd-kit/core` + `@dnd-kit/sortable`, `collisionDetection={pointerWithin}`.\n\n### Columns (`COLUMNS`, all six always rendered)\n| id | label | collapsed `short` | lean |\n|---|---|---|---|\n| critical | Critical | — | no |\n| high | High | — | no |\n| medium | Medium | — | no |\n| low | Low | — | no |\n| phase | Phase trackers | Phase | yes |\n| other | Other | — | yes |\n\nWidths: severity columns `w-[244px] shrink-0`; lean-expanded `w-[240px]`; lean-collapsed `w-[140px]`. Each `Column` is a `useDroppable({id:'col:<id>'})` with its own `SortableContext`. Headers: dot + label + count. Empty columns render dashed `emptyDropCopy(id)` (\"Drop here to mark as Critical/High/Medium/Low\", \"Drop here for phase trackers\", \"Drop here for Other\"). `isOver` highlights `border-ring bg-card/70`.\n\n**Lean collapse**: `phase`/`other` collapse to the 140px compact column when `!hovered && !dragActive && !isOver`; remain valid droppables while collapsed (ref retained). Hover or any active drag expands them. Collapsed header uses `short ?? label`; tall spacer keeps drop zone full-height.\n\n**Overflow**: horizontal scroll container, `snap-x snap-mandatory`. `ResizeObserver` + scroll listener set `hasOverflow` when `scrollWidth - clientWidth > 8` and not at end → right-edge mask gradient + `Drag right →` hint pill (`issues-kanban-scroll-hint`).\n\n### Grouping & sync\n`groups: Record<SeverityKey, number[]>` is local optimistic state (per-column ordered issue numbers), seeded by `initialGroups(issues)`. `useSyncGroups` re-seeds from upstream whenever the set of issue numbers changes shape (filter change / refetch) — compares membership, not order, so optimistic reorders survive.\n\n### Cards (`SortableIssueCard` / `CardBody`)\n`useSortable({id: issue.number})`. `data-testid=\"issue-card-<num>\"`. Structure:\n- Drag handle (`GripVertical`), appears on hover/focus, `aria-label` documents arrow-key column movement (keyboard sensor + `sortableKeyboardCoordinates`).\n- Full-card open button (`aria-label=\"Open issue #<num>\"`) → `onOpenIssue(number)`.\n- Body: `cleanIssueTitle(title)` (`line-clamp-2`, `text-body-strong`), then `#<num>` / author / comment count (`MessageCircle` when `commentCount>0`). `area:*` labels render as up-to-4 chips (prefix stripped, `title` = label description).\n- `⋮` menu (`issue-card-menu-<num>`, Radix `DropdownMenu`): \"Move to\" → `MOVE_TARGETS` = Critical/High/Medium/Low/Other (items `issue-card-move-<num>-<id>`; current column disabled). Selecting calls `moveToColumn`.\n\n`DragOverlay` renders `DragGhost` (`CardBody` in a `bg-popover` ring) with a 180ms spring drop animation; the source card goes `opacity-30`.\n\n### Drag lifecycle\n- `onDragStart` → `activeId = active.id`.\n- `onDragOver` → if over a different column (drop-zone id `col:<id>`, or the column of the hovered card), optimistically move the number to the end of the target column.\n- `onDragEnd`:\n  - **Same column**: `arrayMove` reorder (session-only; never persisted to GitHub).\n  - **Cross column**: `commitRelabel(num, targetCol)`.\n\n### `commitRelabel` (shared by drag + menu)\nComputes `original = severityLabel(severity(row))`, `target = severityLabel(targetCol)`. `addLabels = target ? [target] : []`; `removeLabels = original && original!==target ? [original] : []`. If both empty (move to/from phase/other) → no IPC, `toast('Moved #<num>, no severity label changed')`, visual move stays. Else `relabel.mutate`:\n- success + `res.ok` → `toast.success('Relabeled #<num> to <label>')`.\n- `res.ok === false` or `onError` → `revertMove(num, originalSeverity)` + `toast.error(\"Couldn't relabel #<num>: <error>\")`.\n\n`moveToColumn` (menu): optimistically removes the number from all columns, appends to target, then `commitRelabel`. `revertMove` re-buckets to the original severity.\n\n> Note: same-column reorder and any phase↔other move are **not** written to GitHub — GitHub has no card ordering, and phase/other are not `severity:*` labels.\n\n## Table view (`TableView`)\n\n`data-testid=\"issues-table\"`, `min-w-[720px]`. Sticky header. Columns: **#** (mono), **Title** (`cleanIssueTitle`, truncated `max-w-[420px]`), **Labels** (first 4 as chips), **Author** (`?? '-'`), **Comments** (right-aligned `commentCount`), **Updated** (`updatedAt` → `toLocaleDateString`). Rows `issue-row-<num>`, click → `onOpenIssue`. Operates on the same `filtered` array.\n\n## Issue detail sheet (`IssueDetailSheet`)\n\nRadix `Dialog`, right-anchored `max-w-[640px]`, `data-testid=\"issue-detail-sheet\"`. Open when `openIssue !== null`. Fetches via `useIssueDetail`.\n- Header: `#<num>` · `opened by <author|unknown>` · `createdAt` (locale date) · title (`cleanIssueTitle`). Loading → \"Loading issue...\"; null → \"Issue not found. It may have been closed or transferred.\"\n- Body (Operator): labels row (all labels as chips, `title`=description), then `body` rendered via `marked.parse(...,{async:false})` into `prose-issue` (`issue-body`), else \"No description.\" Comments section (`issue-comments`): count header + per-comment card (author, `Calendar` + `createdAt` locale string, markdown body).\n- Footer: **View on GitHub** (`Button variant=\"outline\"`) → `bridge.openExternal(detail.url)`.\n\nGuided sheet is identical minus the top-level labels row.\n\n## Guided board (`WorkboardNewbie`)\n\n`data-testid=\"workboard-newbie\"`. Header subtitle: `\"{n} open right now\"` / `\"Loading...\"` (GitHub) or `\"Pulled from Slack\"`. Same `SourceToggle`. No filter, no view toggle, no drag, no relabel.\n\nThree buckets via `groupBySeverity`: **Critical** (`severity:critical`), **High** (`severity:high`), **Other** (everything else, sorted by `updatedAt` desc). Each `Bucket` (`Card`): dot + title + item count + one-line blurb; rows are full-width click targets (`workboard-newbie-row-<num>`: `#num` + `cleanIssueTitle` + hover chevron) opening the detail sheet. `BUCKET_ROW_CAP = 6` with a \"Show N more\" / \"Show less\" expander (`workboard-newbie-bucket-expander-<title>`). Loading → `BucketSkeleton` ×3. Footer link **Switch to Operator view** (`workboard-newbie-switch-power`) → `setPersona('seasoned')`.\n\n## Slack source (`SlackBoard`)\n\n`data-testid=\"slack-board\"`, `density` prop `'compact'` (Operator) / `'roomy'` (Guided, adds blurbs + wider gaps). Reads `useConnections()` (`connections.data.slack.connected`) and `useSlackIssues(connected)`.\n- Not connected → `EmptyState` (`slack-board-empty`) \"Connect Slack to pull issues\" → **Open Services** (`slack-board-connect`, navigates `/services`).\n- Connected → groups by `SlackTeam` via `TEAM_META`: **Tech** (`bg-danger`), **Non-tech** (`bg-warning`), **Test** (`bg-success`). Each `TeamSection` (`slack-team-<id>`): header dot + label + item count, blurb only when roomy. Rows (`slack-issue-<id>`): severity dot (`critical`→danger else warning, when present), channel (`MessageSquare` + name), title (truncated), date (from Slack `ts`, integer seconds × 1000), **Open** → `bridge.openExternal(permalink)`. Loading → `TeamSkeleton`.\n\n## Keyboard / a11y\n\n- `g w` opens the panel (`lib/useKeymap.ts`).\n- Card drag handle: pointer (4px activation distance) or keyboard sensor (arrow keys move between columns; `aria-label` on the grip documents it).\n- Detail sheet: Radix focus trap, Esc closes, `aria-label=\"Close\"` on the X.\n- \"Skip to content\" + per-route `PanelErrorBoundary` from the shell.\n\n## Edge cases\n\n- No `origin` remote / non-GitHub remote → empty board (list returns `[]`); relabel surfaces the \"no origin remote\" error.\n- gh subprocess timeout (30s list / 20s relabel) → treated as failure (empty result / revert + toast).\n- Optimistic moves: success persists, any failure reverts and toasts the first 3 lines of gh stderr.\n- Moving to/from **Phase trackers** / **Other**: visual-only, no GitHub write, neutral toast.\n- Same-column drag reorder: session-only, never persisted.\n- Up to 100 open issues fetched per load (`--limit 100`, hard-clamped).\n- `REFRINGENCE_CONSOLE_REPO` env var overrides repo derivation globally (escape hatch; no baked-in default repo)."
  },
  {
    "key": "architecture",
    "title": "Architecture",
    "guided": "# Architecture\n\nThe Architecture panel draws a map of your project: a box for each package, and lines between the boxes showing which package uses which. It reads this straight from your code — nothing is connected, nothing is sent anywhere. Open it from the sidebar, or press `g a`.\n\n> [!INFO] What is a \"package\" here?\n> Console treats each **top-level folder** in your repo as a package — for example `console-shell`, `docs`, `scripts`. A package is just a unit of code that lives together and usually does one job (the UI, the docs, the build scripts). For a few common container folders — `packages/`, `apps/`, `services/`, `libs/`, `bundled-tools/` — it goes one level deeper, so `packages/design-tokens` and `bundled-tools/my-tool` each get their own box instead of all collapsing into one.\n\n## The two views: Code and Systems\n\nAt the top right there's a toggle: **Code** and **Systems**.\n\n- **Code** is the real, measured map. Console reads your files and draws the packages and the import lines between them. No AI involved — it's just what your code actually says.\n- **Systems** is an AI-drawn map of the *idea* of your project (\"the frontend\", \"the auth service\", \"the database layer\") mapped onto real folders. It needs an AI provider connected and only appears once you generate it. The rest of this page is about the **Code** view, which is the heart of the panel.\n\n> [!INFO] Why have both?\n> The Code view answers \"what literally imports what.\" The Systems view answers \"what are the big moving parts, conceptually.\" A beginner usually wants the Code view first — it can't be wrong, because it's measured, not guessed.\n\n## What the graph is reading\n\nWhen you open the Code view, Console walks every source file in your project and looks at the **import lines** at the top of each file — the `import ... from '...'` and `require('...')` statements.\n\n> [!INFO] What is an import?\n> When one file needs code from another file, it says so at the top with an import. `import { Button } from '../ui/Button'` means \"this file uses the `Button` from that other file.\" Imports are how a codebase wires itself together — they're the single best evidence of what depends on what. A **dependency graph** is just a picture of all those \"uses\" arrows, rolled up from individual files to whole packages.\n\nBy default it reads **JavaScript and TypeScript** only (`.ts`, `.tsx`, `.js`, `.jsx`, and friends), because that's the clean, common case. If your project mixes languages, there's an **All languages** button (more on that below) that also reads Python, Rust, Go, Java, and Kotlin imports.\n\nIt skips the folders that aren't your code — `node_modules`, `.git`, `dist`, `build`, `target`, `.venv`, and similar — so the map shows *your* architecture, not your tooling.\n\nEach box also shows two numbers: **LOC** and **files**.\n\n> [!INFO] What is \"LOC\"?\n> LOC means **lines of code** — literally how many lines of source live in that package. It's a rough size signal. A package with 12,000 LOC is doing a lot more (and is riskier to change) than one with 200. Console counts lines while it reads each file, so the number is real, not estimated.\n\n## Reading the boxes: tiers and the one blue colour\n\nEach box sits in a **tier**, shown by a small coloured dot and listed in the **legend** in the bottom-left corner. The tiers are:\n\n- **Shell** — the outer process / entry point (an Electron main process, a server, a backend).\n- **Presentation** — the UI: screens, components, the things a user sees.\n- **Domain** — the middle of the app: logic that both uses other packages and is used by them.\n- **Data** — packages that get used but don't use much themselves (token sets, data layers, leaf libraries).\n- **Infra** — build scripts, config, tooling, design tokens.\n- **Test** — test suites, e2e, evals, fixtures.\n- **External** — a third-party dependency from outside your repo (only shown if you turn it on).\n\n> [!INFO] How does Console decide a package's tier?\n> Two ways, in order. First it looks at the **name** — a folder called `scripts` is Infra, one called `tests` is Test, one with `ui`/`web`/`frontend` in the name is Presentation, one with `electron`/`server`/`backend` is Shell. If the name gives no hint, it uses the **shape of the connections**: a package that's used by others but uses nothing is treated as a leaf (**Data**); one that only points outward is a top layer (**Presentation**); one in the middle is **Domain**. This is a sensible guess, not a law — and you can override it (see \"Make it yours\").\n>\n> Notice the colours stay restrained: one blue accent plus neutral greys and a couple of status colours. That's deliberate — the legend should help you read structure, not turn into a rainbow.\n\nThe legend only lists tiers that are actually on screen, so you won't see an \"External\" row unless you've turned external dependencies on.\n\n## The lines (and what a thick line means)\n\nA line from box A to box B means **A imports from B** — A depends on B. The lines flow generally **top-to-bottom**: things that depend on a lot sit higher, things that are depended-on sit lower.\n\nLines vary in thickness. A **thicker line means more imports** between those two packages — they're tightly coupled. A thin line is a light, occasional dependency.\n\nWhen nothing is selected, every line rests at a calm neutral grey so you can take in the whole shape. **Click a box** and Console lights up just that box and its direct neighbours in blue, fading everything else back — so you can isolate \"what touches this one thing.\" Click empty space (or the box again) to clear it.\n\n> [!INFO] Why highlight neighbours instead of the whole chain?\n> When you're trying to understand one package, the question is almost always \"what does *this* talk to directly?\" Showing one hop in each direction answers that without drowning you in the entire graph. It's the same instinct as clicking a person in an org chart to see their manager and reports.\n\n## Cycles — and why they're worth a warning\n\nIf Console finds a **dependency cycle**, a warning card appears in the bottom-right corner (\"N cycles\") listing the loop, like `a -> b -> c -> a`.\n\n> [!INFO] What is a dependency cycle, and why is it bad?\n> A cycle is when packages depend on each other in a loop: A needs B, B needs C, and C needs A again. The problem is there's no clean starting point — you can't fully build, test, or reason about A without already having C, which needs A. Cycles make code harder to change, harder to test in isolation, and harder to split apart later. They're one of the classic \"this will hurt you eventually\" signals in software architecture, which is why Console calls them out specifically.\n>\n> Console finds them using a standard algorithm (Tarjan's strongly-connected-components) — it's a well-known, reliable way to spot every loop in a graph, so if there's a cycle, it will be found.\n\nA small number of cycles isn't an emergency, but each one is a thread worth pulling: it usually means two packages should be merged, or a shared piece should be pulled out into a third package they both depend on.\n\n## When there are no lines\n\nSometimes you'll see boxes but no lines, with a small note explaining why. That's honest, not broken: it means your packages genuinely **don't import one another** (they might each stand alone, or they might talk through a published package name rather than a direct file path that the default scan follows). If that happens, the **All languages** and **External deps** toggles often reveal connections the default JS/TS-only scan didn't look for.\n\n## The toolbar (top-right of the canvas)\n\nThese buttons let you widen or reshape the map:\n\n- **`N` standalone / Hide standalone** — by default the map hides packages that have no connections at all (so a repo of independent folders doesn't render as a wall of lonely boxes). This button reveals them when you want the complete picture.\n- **All languages** — also read Python, Rust, Go, Java, and Kotlin imports, not just JS/TS. Turn this on for mixed-language projects.\n- **External deps** — add boxes for third-party libraries your code imports (the stuff from `node_modules`, `pip`, etc.). Off by default to keep the focus on *your* code.\n- **Recompute** — re-read the project from scratch. Console caches the map for speed, so use this if you've edited code and want to be sure the map is fresh.\n- **Reset layout** — throw away any manual box-dragging and let Console auto-arrange again.\n- **Edit layout** — let you drag boxes around (see below).\n\n> [!INFO] What does \"Recompute\" actually do?\n> Reading every file takes a moment, so Console saves the result and reuses it next time you open the panel. It notices when files change and refreshes on its own — but **Recompute** forces a fresh read right now, bypassing the saved copy. Think of it as \"reload.\"\n\n## Making it yours\n\nIn the **Edit layout** mode you can drag boxes to arrange the map the way *you* think about the project. Click any box (whether editing or not) to open a small side panel where you can:\n\n- **Re-assign its tier** — if Console's guess is wrong, set the right one by hand.\n- **Add a note** — a sentence on why this package matters, for the next person (or future you).\n- **Hide it** — drop noisy packages out of the map.\n\n> [!INFO] Where does my arrangement go? Will a recompute wipe it?\n> No. Your positions, tier overrides, notes, and hidden boxes are saved in a file inside your project (`.refringence-console/architecture.json`), completely separate from the auto-drawn map. When Console re-reads your code, it lays your saved choices back on top — so your curation survives a recompute. You can even commit that file so your whole team sees the same map. (The auto-map itself is cached separately in `.refringence-console/arch-graph-cache.json`, which is just a speed cache and safe to delete.)\n\n## The list view\n\nBelow the graph there's a collapsible **List view** — the same packages as a plain, scrollable list with short plain-English descriptions of what each one does. Hover a row to highlight what it imports and what imports it. It's the same information as the graph, in reading order, for when you'd rather scan text than a diagram. Clicking a row takes you to the **Repo** panel for that package.\n\n---\n\n**In short:** the Architecture graph reads your real import statements, rolls them up into a tiered package map, draws the dependencies between them, flags any cycles, and lets you curate the result without losing it. Start in the Code view, click a box to focus it, and watch for the cycle warning in the corner.",
    "operator": "# Architecture\n\nRoute `/arch` · nav chord `g a` · sidebar icon `Workflow`. Persona-branched at `ArchPanel`: `isNewbie` → `ArchitectureNewbie`, else `ArchSeasoned`. Source: `views/arch/{ArchPanel,ArchitectureGraph,SystemsView,ArchitectureNewbie}.tsx`. Extraction + persistence: `console-electron/src/main/ipc/architecture-graph.ts`.\n\nHeader view toggle (`arch-view-toggle`): **Code** (`Boxes`) / **Systems** (`Network`), local state `viewMode: 'code' | 'systems'`, default `code`. Subtitle swaps per mode. Body capped at `max-w-[1100px]`.\n\n## Data sources (bridge / queries)\n\n| Hook | Bridge call | Returns |\n|---|---|---|\n| `useArchitectureGraph(root, includeExternal, allLanguages)` | `bridge.arch.graph(root, {includeExternal, allLanguages})` | `DependencyGraph`; `queryKey ['arch','graph',root,includeExternal,allLanguages]`; `staleTime 5min`; `enabled: !!root` |\n| `useRecomputeArchitecture(root, includeExternal, allLanguages)` | `bridge.arch.recompute(root, opts)` | mutation; `onSuccess` seeds the graph queryKey with the fresh result (no refetch) |\n| `useArchOverlay(root)` | `bridge.arch.overlayRead(root)` | `ArchOverlay \\| null`; `queryKey ['arch','overlay',root]`; `staleTime 5min` |\n| `useSaveArchOverlay(root)` | `bridge.arch.overlayWrite(root, overlay)` | mutation; `onMutate` optimistically sets the overlay cache (no bounce) |\n| `useRepoSummary()` | `bridge.repo.summary` | drives the List view + Newbie buckets (`total_packages/total_files/total_loc`, `packages[]`) |\n| `useProjectProfile(root)` / `useEnrich(root)` | `bridge.intel.*` | Systems view (`profile.ai.systemDiagram`) |\n\n`includeExternal` and `allLanguages` are passed as `mode === 'operator' && flag` — i.e. **forced false in guided mode**, so the newbie graph is always internal-only JS/TS. Both flags are part of the query key and are cached separately main-side.\n\n`DependencyGraph = { nodes: DependencyNode[]; edges: DependencyEdge[]; cycles: string[][]; truncated: boolean; fileCount: number }`. `DependencyNode = { id; label; tier: ArchTier; loc; fileCount; external? }`. `DependencyEdge = { source; target; weight }`. `ArchTier = 'shell'|'presentation'|'domain'|'data'|'infra'|'test'|'external'`. `ArchOverlay = { positions: Record<id,{x,y}>; tierOverrides: Record<id,string>; notes: Record<id,string>; hidden: string[] }`.\n\n## Main-process extractor (`architecture-graph.ts`)\n\n**Package mapping.** A file's package id = its first path segment under the repo root, except for `TWO_SEGMENT_CONTAINERS` (`bundled-tools`, `packages`, `apps`, `services`, `libs`, `app`, `plugins`, `modules`, `examples`) which keep two segments (`bundled-tools/my-tool`). `pkgForRelPath()`.\n\n**Walk** (`walkRepo`). Iterative DFS. `SKIP_DIRS` = `node_modules, .git, dist, build, .refringence-qa, .refringence-console, __pycache__, .pio, .cache, .venv, tools-src, vcpkg, src-qt, out, .next, .parcel-cache, target`; also skips any dotfile/dotdir. Caps: `MAX_DEPTH=24`, `MAX_FILES=30000`, per-file `MAX_FILE_BYTES=1_000_000` (oversize files skipped). Hitting a cap sets `truncated=true`. LOC = `readFileSync(...).split('\\n').length` per file, summed per package. Config files collected during the walk for resolution: `tsconfig.json`, `go.mod`, `Cargo.toml`, `package.json`, `requirements.txt`, `pyproject.toml`.\n\n**Languages.** Default (`allLanguages` off) recognises **JS/TS only**: `.ts .tsx .js .jsx .mjs .cjs`. With `allLanguages` on, also `.py/.pyi`, `.rs`, `.go`, `.java/.kt/.kts`. Unknown extensions are skipped (never counted).\n\n**Import extraction (regex, one pass/file).**\n- **JS/TS**: `import … from '…'`, `export … from '…'`, `require('…')`, dynamic `import('…')`. Relative specifiers resolved against `dirname` probing `'' .ts .tsx .js .jsx .mjs .cjs` then `index.*`; bare specifiers resolved through **tsconfig `paths` aliases** (`baseUrl` + longest-prefix-first, wildcard substitution); anything still unresolved and non-relative → external (`bareModuleName`, scope-preserving).\n- **Python**: `from a.b import c`, `import a.b.c` (comma-split). Dotted module probed as `a/b/c.py|.pyi` and `a/b/c/__init__.py|.pyi`; leading-dot relative resolved against the importer's dir.\n- **Rust**: `use crate::…` and `mod foo;` stay intra-crate (no edge). Cross-crate `other::x` maps to `other/src/lib.rs|main.rs` if walked; `cargoCrateDir` guards against same-crate false positives. `self/super/std/core` dropped.\n- **Go**: grouped + single `import` specs; intra-module when prefixed by a `go.mod` `module` path (stripped to subdir, matched by `dirname`); third-party (dotted host) → external via `goExternalName`; stdlib dropped.\n- **JVM**: `import a.b.C;` resolved to the repo package whose file declared `package a.b` (longest-prefix match over `jvmPackageToPkg`).\n\n**Edges.** `buildEdges` accumulates `\"source target\" → weight` from imports (self-edges dropped). `addManifestEdges` recovers workspace edges the import scan misses: reads each internal `package.json` `dependencies/devDependencies/peerDependencies`, resolving `file:`/`link:`/`workspace:` specs to a sibling dir and otherwise matching a declared dep **name** to an internal package's `name`; same for `pyproject.toml`/`Cargo.toml` declared deps (PEP-621/poetry/cargo tables). This is what gives an npm-`--prefix`/workspaces graph any edges at all. `seedManifestPackages` adds zero-source nodes for manifest-only packages (e.g. an empty `design-tokens`).\n\n**Tiers** (`tierFor`). Name signal first (`tierByName`, checked in order): `RE_TEST` (qa/tests/e2e/specs/fixtures/__tests__/evals/benchmarks) → `test`; `RE_INFRA` (scripts/infra/config/setup/install/build/tooling/tokens/design-tokens/ci/devops) → `infra`; `RE_SHELL` (electron/main/server/host/daemon/backend/gateway/api) → `shell`; `RE_PRESENTATION` (shell/renderer/chrome/web/ui/frontend/client/app/site/view/pages/components/docs) → `presentation`. No name hit → **topology**: `inbound 0 & outbound>0` → presentation; `inbound>0 & outbound>0` → domain; `inbound>0 & outbound 0` → data; else domain. External nodes (only with `includeExternal`) are tier `external`, `external: true`, `loc` = import-site count, synthesised from imported specs **plus** declared-but-unimported manifest deps (`collectDeclaredExternals`).\n\n**Cycles.** Iterative **Tarjan SCC** over internal (non-external) nodes only; SCCs of size > 1 are returned as `cycles: string[][]`. Self-edges never emitted, so practically: multi-node SCCs.\n\n**Caching.** Graph cached at `<root>/.refringence-console/arch-graph-cache.json`, keyed on `{version: GRAPH_VERSION(=3), signature, includeExternal, allLanguages}`. `cacheSignature` is **directory-only** (roll-up of dir `mtimeMs` + dir count + entry count; no per-file stat) — a content edit bumps the containing dir mtime, an add/delete changes entry count. Cache hit requires version + signature + both flags to match. `recompute` forces `force=true`, bypassing the read. `bumping GRAPH_VERSION` invalidates older caches on unchanged trees.\n\n**Hardening.** `resolveProjectRoot` resolves + `statSync.isDirectory()` guards; `isInside` blocks path traversal on the overlay write target. Every handler is total — returns `EMPTY_GRAPH`/`null` on any error rather than throwing into the renderer. Overlay write returns `{ok:false,error}` on failure.\n\n## Code view — `ArchSeasoned`\n\nHeader `Dependency graph` label + `total_packages · total_files · total_loc` (from `useRepoSummary`). States: `noProject` → `NoProjectEmptyState` (`arch-pick-folder` → `bridge.project.pickFolder`); `isLoading` → `LoadingSkeleton` (`arch-loading`); `isError` → `arch-error-state` card with **Retry** (`repo.refetch`).\n\n### ArchitectureGraph (`mode=\"operator\"`)\n\n`@xyflow/react` `ReactFlow` on a `h-[640px]` canvas (`architecture-graph`). Local state: `includeExternal`, `allLanguages`, `selectedId`, `editLayout`, `showStandalone`, `elkPositions`, `flowNodes`.\n\n**Node visibility.** `hidden` = overlay `hidden` set. `connectedIds` = every node in any edge. Default `visibleNodes` = non-hidden ∩ connected; `showStandalone` (or zero connected) → all non-hidden. `standaloneCount` = non-hidden, non-connected count (drives the toggle label). `visibleEdges` = edges with neither endpoint hidden.\n\n**Layout (ELK).** `layoutWithElk` runs `elkjs` `elk.layered`, `direction DOWN`, `nodeNodeBetweenLayers 64`, `spacing.nodeNode 40`, fixed node box `NODE_W=240 × NODE_H=58`. Re-runs on `[graph, visibleNodes.length, hidden]`. ELK failure → 4-column grid fallback. **Overlay `positions[id]` win over ELK** per node, so a curated layout sticks. `FitOnInit` calls `fitView({padding:0.2})` only once `useNodesInitialized()` is true (and re-fits when visible node count changes) — fixes the right-edge clipping from fitting before measurement.\n\n**Node card** (`ArchNodeCard`): tier dot + tint + border from `TIER_STYLE` (one blue accent `accent-solid` for shell, `info` presentation, `foreground` domain, `warning` data, `muted` infra, `success` test, dashed `border` external), `font-mono` label, `loc · files`. `target` handle Top, `source` handle Bottom. Dimmed to `opacity-25` when a selection excludes it; `ring-2 ring-accent-solid` when selected.\n\n**Edges** (`flowEdges`): `strokeWidth = min(1.2 + weight*0.5, 4)`. No selection → all neutral `var(--muted-foreground)` at `opacity 0.6`. Selection (`neighbourIds`, the selected node ± one hop both directions) → in-subgraph edges `var(--accent-solid)` `opacity 0.95`, others `opacity 0.1`.\n\n**Interaction.** Click node toggles `selectedId` (re-click clears); pane click clears. Pan/zoom/scroll/pinch/dblclick enabled (operator is not fixed viewport). `Controls` shown (no interactive lock toggle). `Background gap=20`. `proOptions.hideAttribution`.\n\n**Operator toolbar** (top-right, gated `mode==='operator'`):\n- `arch-standalone-toggle` — only when `standaloneCount>0`; label `N standalone` / `Hide standalone`.\n- `arch-all-languages-toggle` (`Languages` icon) — toggles `allLanguages` (re-keys the query; also extracts Py/Rust/Go/JVM).\n- `arch-external-toggle` (`Package` icon) — toggles `includeExternal`.\n- `arch-recompute` (`RefreshCw`, spins while `recompute.isPending`) — `recompute.mutate()`.\n- `arch-reset-layout` (`RotateCcw`) — `patchOverlay({positions:{}})` + clears `elkPositions` to re-run ELK. **Keeps** tierOverrides/notes/hidden (layout reset ≠ curation wipe).\n- `arch-edit-layout-toggle` (`Pencil`) — toggles `editLayout`; label `Edit layout` / `Done editing`. Nodes draggable only when `operator && editLayout`.\n\n**Drag persistence.** `onNodesChange` applies changes locally; if `operator && editLayout` and any `position` change, a **400 ms** debounce writes all current `{x,y}` into `overlay.positions` via `saveOverlay.mutate` (no per-pixel writes).\n\n**Operator side panel** (`arch-node-panel`, on node select): `node.label`; **Tier** chips = `MANUAL_TIERS` (`shell/presentation/domain/data/infra/test` — **`external` not manually assignable**, and hidden entirely for `external` nodes) → `patchOverlay({tierOverrides})`; **Note** textarea, persisted on blur when changed → `patchOverlay({notes})`; **Hide/Show node** button → toggles overlay `hidden` then clears selection.\n\n**Banners / cards.**\n- `Legend` (`arch-legend`, bottom-left) — only `presentTiers` (tiers actually on screen), ordered `TIER_ORDER`.\n- `CycleWarning` (`arch-cycle-warning`, bottom-right) — when `graph.cycles.length>0`; warning badge `N cycles`, first 4 cycles as `a -> b -> c` (`shortName`).\n- `arch-truncated-banner` (top-left) — when `graph.truncated`; \"Showing X packages; tree truncated at `fileCount` files.\"\n- `arch-no-edges-hint` (top-left) — when not truncated, `connectedIds.size===0`, `visibleNodes>0`; explains the missing lines and (operator) points at All languages / External deps.\n- `architecture-graph-empty` — when `graph.nodes.length===0`; \"Showing the list view instead.\"\n- `arch-graph-loading` — `GraphSkeleton` while `graphQuery.isLoading`.\n\n### Footer note + List view\n\nNote under the graph: edges extracted live; Edit layout to drag; per-node tier/notes/hide save to `.refringence-console/architecture.json`.\n\n**List view** (`arch-list-toggle` chevron, collapsed by default). Packages from `repo.data.packages` sorted by `total_loc` desc. Source of descriptions is the **hand-authored `PACKAGE_META`** in `ArchPanel.tsx` (NOT the live graph) — `role` string + curated `importsFrom`; `importedBy` derived at runtime in `buildEdgeMap` (only edges where both ends exist in the repo's package set are kept). Each `PackageRow`: `name` (mono) + `LOC · files`, role line, **Imports** / **Imported by** edge chips (`shortName` strips `bundled-tools/`, `packages/`). Hover highlights the row + its import/imported-by neighbours (others dimmed `opacity-40`, active gets a 2px `accent-solid` left bar). Whole row is a `<Link to=\"/repo\">`.\n\n## Systems view — `SystemsView` (`mode === 'systems'`)\n\nAI-generated **semantic** map (distinct from the measured Code graph). Reads `useProjectProfile(root).data.ai.systemDiagram` (`SystemDiagram = { nodes: SystemNode[]; edges }`; `SystemNode = { id, label, kind, paths[], summary? }`). No diagram → `EmptyState` with **Generate with AI** (`useEnrich(root).mutate()`, \"Reading the project…\" pending); surfaces `enrich` error text.\n\nRendered with ReactFlow + ELK (`elk.layered DOWN`, `nodeNodeBetweenLayers 72`, `nodeNode 44`, box `230×64`). Nodes **not draggable** (no overlay). `KIND_STYLE` maps `frontend/backend/data/service/infra/shared/docs/tests` to the same one-accent palette (unknown kind → `shared`). Card shows `kind · N paths`. Click toggles selection; same neighbour-highlight edge model as Code view; edges may carry a `label`. Bottom-left card: `N systems · M links · AI-generated`. Selection sidebar (`systems-sidebar`): label, `kind` badge, `summary`, and a **Maps to (N)** list of real repo paths — each a button calling `bridge.openPath(`${root}/${rel}`)` to open real code (paths validated main-side).\n\n## Guided variants\n\n**`ArchitectureNewbie`** (`arch-newbie`): single column, `max-w-[820px]`. Embeds `ArchitectureGraph mode=\"guided\"` (`arch-newbie-graph`) — **fixed viewport** (no pan/zoom, no `Controls`, no toolbar, no operator side panel), `includeExternal`/`allLanguages` forced off, nodes never draggable. Clicking a node opens `GuidedSidebar` (`arch-guided-sidebar`): prose (\"`label` sits in the `Tier` tier; N files, M LOC\"), optional overlay note, and **It depends on** / **It is used by** chip lists derived from `graph.edges`. Below the graph: packages bucketed by `ROLE_RULES` into **Shell / UI / Data / Tools / Workspace** (`classify`, plus static `docs`/`scripts` workspace entries) with one-sentence explainers; no LOC, no arrows. Footer card \"Want the full map?\" → `setPersona('seasoned')`.\n\n`ArchitectureGraph` `mode==='guided'` differences vs operator: `fixedViewport=true` (pan/zoom/scroll/pinch/dblclick disabled, `Controls` hidden), toolbar/operator panel suppressed, drag persistence inert.\n\n## Keyboard / a11y\n\n- `g a` → `/arch` (global chord, `useKeymap`).\n- View toggle buttons `arch-view-{code,systems}`; graph container `architecture-graph` / `systems-view`.\n- Route wrapped in `PanelErrorBoundary`. Node handles + dots are `aria-hidden`; close buttons are `aria-label`ed.\n\n## Persisted files (in the open project)\n\n- `.refringence-console/arch-graph-cache.json` — auto-graph speed cache (dir-mtime signature + flags + `GRAPH_VERSION`). Disposable.\n- `.refringence-console/architecture.json` — user overlay: `positions`, `tierOverrides`, `notes`, `hidden`. Read/written independently of the cache so curation survives `Recompute`. (Both live under the gitignored `.refringence-console/` dir by default.)"
  },
  {
    "key": "pipeline-and-release",
    "title": "Pipeline and Release",
    "guided": "# Pipeline and Release\n\nThis page covers the two panels that answer one question: **is your project safe to put in front of real users yet, and what happens when you push code?**\n\n- **Pipeline** shows the automated checks that run every time you push code (this is called **CI**).\n- **Release** shows a checklist of safety gates that decide whether a build is **ready to ship**, plus a one-click way to **deploy to Vercel**.\n\nYou reach them from the sidebar: **Ship it → Connect & deploy / Release / Monitor**, with **Pipeline** living under the **More** disclosure. The keyboard shortcuts are `g p` (Pipeline) and `g l` (Release).\n\n---\n\n## Pipeline\n\n> [!INFO] **What is CI?**\n> CI stands for *Continuous Integration*. The idea: every time you push code to GitHub, a robot automatically runs a set of checks on it — building the project, running the tests, checking the formatting — *before* the code is considered \"good.\" Instead of you remembering to run things by hand, the robot does it on every change and tells you red or green. Console's Pipeline panel reads those robot instructions and draws them for you.\n\n### Where the data comes from\n\nConsole does not invent the steps. It reads the **workflow files** in your repo at `.github/workflows/`.\n\n> [!INFO] **What is a workflow file?**\n> A workflow file is a small text file (ending in `.yml` or `.yaml`) that lives in the hidden `.github/workflows/` folder of your project. It tells GitHub: \"when *this* happens (a push, a pull request, a schedule), run *these* jobs.\" One file = one workflow. A repo can have several — one for tests, one for linting, one for deploying, and so on.\n\nIf you have not picked a project yet, the panel asks you to **Pick a folder**. If your project has no `.github/workflows/` folder, you will see *\"No workflows found\"* — that simply means nobody has set up CI for this repo yet, and that is completely normal for a young project.\n\n### What each workflow card tells you\n\nIn Guided mode, Pipeline shows **one card per workflow** (up to six), written in plain English. Each card has three parts:\n\n1. **A title** — the workflow's name, cleaned up to read nicely (e.g. `ci.yml` becomes \"CI\").\n2. **A plain-English sentence** describing *when* it runs and *what* it does. For example: *\"On every push and on pull requests, it builds and tests the repo across Lint and Test.\"* Console writes this sentence from the workflow's real triggers and real job names, so two different workflows never read the same.\n3. **A status pill** (only when there has been a run): **Passing**, **Failing**, or **Running**.\n\n> [!INFO] **Reading the trigger phrase**\n> The \"when\" half of the sentence is the most useful part to learn from. It is built from the workflow's real triggers:\n> - *on every push* — runs whenever anyone pushes code.\n> - *on pull requests* — runs when someone proposes a change for review (a \"pull request\").\n> - *on a schedule* — runs on a timer (e.g. nightly), even if nobody pushed.\n> - *when a release is published* — runs only when you cut a release.\n> - *when started by hand* — someone can press a button to run it (this is the trigger Console uses too — see below).\n> - *when another workflow calls it* — it is a reusable piece used by other workflows.\n\n> [!INFO] **What does \"the status pill\" actually mean?**\n> The pill reflects the **most recent run** of that workflow on GitHub:\n> - **Passing** (green) — the last run finished and everything succeeded.\n> - **Failing** (red) — the last run finished but something broke. Open the workflow on GitHub to read the logs and see which step failed.\n> - **Running** (amber) — a run is happening right now.\n>\n> If there is **no pill at all**, it means Console could not confidently classify the latest run (or there hasn't been one). Console deliberately shows *nothing* rather than a fake grey \"idle\" dot — a blank is honest; a made-up status is not.\n\nThe status comes from GitHub itself, read through the **GitHub CLI** (`gh`). Console refreshes it about once a minute, so the pills stay close to live without you reloading.\n\n> [!INFO] **The \"as of\" time**\n> Because the status is fetched and then cached for a short while, the Operator view stamps it with an *\"as of 14:32\"* time so you always know how fresh the reading is. If the GitHub CLI is not installed or you are not signed in, Console will tell you *\"Live run status unavailable\"* and just show the workflow shapes without live pills.\n\n### Seeing more detail\n\nEach card has a **See details** link that flips you into the **Operator** Pipeline view. There, the same workflows are drawn as a left-to-right **stage graph**:\n\n- A **Source** node on the left (this represents your `git push`).\n- One node per **job**, arranged in the order they actually run.\n- An optional **Deploy** node on the right — it only appears if Console finds a `vercel.json` or `netlify.toml`, which signals the project deploys to Vercel or Netlify.\n\n> [!INFO] **What is a \"job\" and what does the arrangement mean?**\n> A workflow is made of **jobs** — independent units of work (e.g. \"install dependencies\", \"run tests\", \"build\"). Some jobs depend on others: the \"test\" job might wait for the \"build\" job to finish first. The graph reads those dependencies and places jobs in columns so that anything to the *right* runs *after* the thing it points back to. Reading the graph left-to-right is reading the order your checks happen in.\n\n### Running a workflow yourself\n\nIn the Operator view, every workflow has a **Trigger** button. Pressing it asks GitHub to start that workflow right now (the same as the *\"when started by hand\"* trigger).\n\n- This only works if you have the **GitHub CLI** installed and your repo actually has CI workflows; otherwise the button is greyed out with a tooltip telling you why.\n- Console fires the request once and shows you a toast like *\"CI dispatched.\"* It will not let you double-fire the same workflow while a trigger is already in flight.\n- A moment later, the workflow's status pill should flip to **Running** as GitHub picks up the job.\n\n> [!INFO] **Why trigger by hand?**\n> Most of the time CI runs on its own when you push. But sometimes you want to re-run checks without making a new commit — for example, a test failed for a flaky reason and you just want to try again, or you fixed something on GitHub's side. The Trigger button is that \"run it again now\" button.\n\n---\n\n## Release\n\n> [!INFO] **What is a \"release\" and why does it need gates?**\n> A **release** is a specific, named version of your project that you've decided is good enough to give to users (e.g. `v0.1`). Before you hand something to real people, you usually want a short checklist: are the tests passing? Is there a security scan? Did someone review it? Each item on that checklist is called a **gate** — because it *gates* (blocks) the release until it's satisfied. Console's Release panel is that checklist, made visible.\n\n### Where the data comes from\n\nRelease reads a checklist file from your repo at `docs/release-checklists/<version>.yaml`. Each version (like `v0.1`) is one file, and each file lists its gates. If you have never created one, you'll see *\"No release pipeline yet\"* with a note telling you where to add it.\n\n> [!INFO] **You write the gates; Console reads them**\n> Console does not decide what \"ready\" means for your project — you do, by listing gates in that YAML file. Console's job is to read the file, total up the statuses, and present it calmly so you always know where you stand.\n\n### The readiness ring and the ship verdict\n\nAt the top, Release shows a **donut ring** and a **plain-English verdict**.\n\n> [!INFO] **What is the readiness ring (the donut)?**\n> The ring is a *part-of-whole* picture. The whole circle is \"all your gates.\" The colored slices show how many are **passing** (green), **blocked/failing** (red), and **pending** (amber). The big number in the middle is how many are passing out of the total — for example **5 / 7**. One glance tells you how close you are without reading every row.\n\nThe verdict next to it is one honest sentence:\n\n- **\"Ready to ship\"** — every gate is passing. Nothing is in your way.\n- **\"Blocked from ship\"** — one or more gates are failing or blocked. These must be resolved first.\n- **\"Awaiting checks\"** — nothing is failing, but some gates still need a human to review and sign off.\n\nBelow that is a quiet count line: how many are **green**, **amber**, **red**, **blocked**, and the total.\n\n### Reading the gates\n\nEach gate row shows:\n\n- A **status icon and badge** — **Passing**, **Pending** (needs review), **Failing** (blocked), or **Waiting**.\n- The **gate's name** in plain words.\n- The **file** it points at (its \"artifact\" — the evidence behind the check, like a test report).\n- A **What this means** sentence explaining the gate in beginner terms, plus a blocker note if something is wrong.\n- A **Show file** link (and in Operator, a **View** button) that opens that evidence file.\n\n> [!INFO] **The four gate states, in plain words**\n> - **Passing (green)** — this check is satisfied. Nothing for you to do.\n> - **Pending / Needs review (amber)** — the work is done, but a person still has to confirm and sign off before the release can go out.\n> - **Failing (red)** — something is actually broken. Fix the underlying problem before shipping.\n> - **Blocked / Waiting (grey)** — this is waiting on something outside your build (e.g. an external approval). Check back later.\n\nGuided mode focuses you on the **top few gates that need attention** so you're not staring at a wall of green checks — it surfaces the things between you and shipping.\n\n> [!INFO] **Why is \"Cut release\" greyed out?**\n> Console can *tell* you a build is ready, but actually tagging and publishing the release (\"Cut release\") and \"Run all checks\" are marked **Planned** — not wired up yet. For now, Console is your dashboard and second opinion; you cut the tag yourself with your normal tools. The panel is honest about this rather than showing a button that does nothing.\n\n### Environment variables\n\nRelease also lists the **variable names** found in your local `.env` files (only the names, never the secret values).\n\n> [!INFO] **What is an environment variable?**\n> An environment variable is a setting your app reads at runtime — things like `DATABASE_URL` or `STRIPE_KEY`. They live in a `.env` file locally and are kept *out* of your code so secrets don't end up on GitHub. Before a release, it's worth checking that every variable your app needs locally is also set on the server you deploy to — a missing one is a classic \"works on my machine, breaks in production\" bug. Console lists the names here so you have a checklist; once you connect Vercel, it can compare them against the host.\n\n> [!INFO] **Privacy note**\n> Console reads only the *names* of your variables (the left side of the `=`), never the values. Your actual secrets never leave the safe part of the app and are never shown back to you or sent to the AI assistant.\n\n---\n\n## Deploying to Vercel (zero-config)\n\n> [!INFO] **What does \"deploy\" mean?**\n> Deploying means taking the code on your machine and putting a running copy of it on the internet so other people can use it at a real URL. **Vercel** is a hosting service that's popular for web apps. \"Zero-config\" means you don't have to write any setup files — Console figures out the settings for you.\n\nDeploying lives in the **Services** panel (under **Connect & deploy**), because you first connect your Vercel account there. Here's the flow:\n\n1. **Connect Vercel.** In Services, paste a Vercel access token. Console stores it safely (encrypted, in your operating system's keychain) and never shows it back to you.\n2. **Press Deploy.** Console inspects your project and *detects* the right settings on its own — what framework you're using, what the build command is, and which folder holds the finished site.\n\n   > [!INFO] **What is \"the framework\" and why detect it?**\n   > A framework is the toolkit your app is built with (React/Next.js, Vite, plain HTML, etc.). Vercel needs to know which one so it can build the project correctly. Normally you'd write a config file to tell it; Console reads your project and works it out, so you don't have to. If there's no build step at all, it deploys your files as a plain static site.\n\n3. **Review the dialog.** A small window shows the **project name** (you can edit it) and the **detected framework and output folder**. No `vercel.json` needed.\n4. **Deploy and watch.** Console uploads your project to Vercel and then **polls** the build every few seconds, updating the status (*Uploading… → Building… → READY*).\n\n   > [!INFO] **What is \"polling\"?**\n   > Building a site takes a little time. Rather than freeze, Console keeps quietly asking Vercel \"are you done yet?\" every few seconds and updates the status line each time. When Vercel reports **READY**, your site is live and Console shows you the URL to click.\n\n5. **Done.** When the build reaches **READY**, Console saves this as your project's last deploy and shows the live URL. That same URL then appears on the **Overview** panel's deployment cell and on the Services card, so you can always jump back to your live site.\n\n> [!INFO] **If a deploy fails**\n> If Vercel reports `ERROR` or `CANCELED`, Console tells you with a toast and stops. Open the Vercel dashboard to read the build logs — the same way you'd debug a failing CI run. A failed deploy never silently looks like a success.\n\n---\n\n## How Pipeline and Release fit together\n\nThink of it as a pipeline of trust:\n\n1. You **push code** → **Pipeline** (CI) automatically checks it.\n2. Those checks (plus human sign-offs and security scans) become **Release gates**.\n3. When every gate is green, the readiness ring fills and the verdict reads **\"Ready to ship.\"**\n4. You **deploy to Vercel**, and the live URL shows up on your Overview.\n\nEach panel is honest about what it knows: if there's no CI, Pipeline says so; if there are no gates, Release says so; if a status can't be trusted, Console shows a blank rather than a guess. That honesty is the point — you're learning to ship by watching real signals, not a green light that doesn't mean anything.",
    "operator": "# Pipeline and Release\n\nTwo panels, both project-scoped to the active window's project (`?wid=`). Pipeline visualizes CI workflows and live run status; Release reads release-gate checklists and the deploy posture. Deploy itself is initiated from the Services panel (Vercel, zero-config).\n\nRoutes: `/pipeline`, `/release`. Chords: `g p`, `g l`. Sidebar: Operator → Project group (Pipeline) / Project group (Release); Guided → Pipeline under **More**, Release under **Ship it**. Both panels branch `isNewbie ? <Newbie/> : <Seasoned/>`.\n\n---\n\n## Pipeline (`/pipeline`, `views/pipeline/`)\n\n`PipelinePanel.tsx` dispatches to `PipelineSeasoned` (Operator) or `PipelineNewbie` (Guided). Header icon `Cable`, title \"Pipeline\", subtitle \"CI/CD stage graph from your workflow files\" (Operator) / \"CI workflows that run on every push\" (Guided).\n\n### Data sources\n\n- `usePipelineDetect(projectRoot)` → `bridge.pipeline.detect(projectRoot)`. staleTime 60s.\n  - Returns `PipelineDetect`: `{ project_root, workflows: PipelineWorkflow[], vercelDetected, netlifyDetected }`.\n  - `PipelineWorkflow`: `{ name, path, triggers: string[], jobs: PipelineJob[] }`.\n  - `PipelineJob`: `{ id, name, runs_on, needs: string[], steps_count }`.\n  - Source: scans `.github/workflows/`.\n- `usePipelineRuns(projectRoot)` → `bridge.pipeline.runs(projectRoot)`. staleTime 30s, **refetchInterval 60s**, enabled only when `projectRoot` set.\n  - Returns `PipelineRuns`: `{ available, reason?, latestByWorkflow: Record<string, WorkflowRun> }` — keyed by **workflow `name`** (not path).\n  - `WorkflowRun`: `{ workflowName, status, conclusion: string|null, createdAt, displayTitle, databaseId, headBranch }`. gh-backed (latest run per workflow).\n  - `available: false` → `reason` rendered as \"Live run status unavailable: {reason}.\"\n- `useProjectCapabilities(projectRoot)` → `ProjectCapabilities.hasCiWorkflows` gates the Trigger buttons.\n\n### Operator view (`PipelineSeasoned`)\n\nHeader right cluster:\n- Project-root `<code>` chip (truncated, `title` = full path; hidden `<sm`).\n- **\"as of HH:MM\"** freshness stamp (`pipeline-as-of`), only when `runs.data.available` — from `asOf(runs.dataUpdatedAt)` (local hours:minutes).\n- **Change / Pick a folder** button (`pipeline-pick-folder`) → `bridge.project.pickFolder()` → `setProject`. Spinner while picking.\n\nStates:\n- Loading skeleton (3 bars) while `detect.isLoading && projectRoot` (`pipeline-loading`).\n- Error → EmptyState \"Failed to read workflows.\" + **Retry** (`pipeline-error-retry` → `detect.refetch()`).\n- No root (`!projectRoot || (!loading && (!data || !data.project_root))`) → EmptyState \"No project connected\" + **Pick a folder** (`pipeline-empty-pick`).\n- Has root, `workflows.length === 0` → EmptyState \"No workflows found yet.\" (add `.github/workflows/<name>.yml`).\n- `runsUnavailable` banner (`pipeline-runs-unavailable`) above the workflow list when runs query reports unavailable.\n\nPer-workflow section (`pipeline-wf-<path>`):\n- Header: humanized name (`humanizeSlug`, `title` = raw name), **status pill**, raw `workflow.path` (mono), and a right-aligned **Trigger** button.\n  - **Status pill** (`pipeline-wf-pill-<path>`): from `pillFor(run)` → `'passing' | 'failing' | 'running' | null`. Mapping (`runStatus.ts`):\n    - `conclusion === 'success'` → Passing (emerald).\n    - `conclusion === 'failure'` → Failing (rose).\n    - `conclusion === null && status ∈ {in_progress, queued}` → Running (amber).\n    - else → `null` → **no pill rendered** (deliberate; avoids a grey \"broken telemetry\" state).\n  - Pill `title` = `pillTooltip(run)` = `\"<displayTitle> - <relativeTime(createdAt)>\"`.\n  - **Trigger** button (`pipeline-wf-trigger-<path>`): `disabled = !hasCiWorkflows || triggering`. Tooltip when disabled: \"GitHub CLI or CI workflows not available\". On click → `useRunner().start({ kind: 'gh', args: ['workflow','run', wf.path], cwd: projectRoot, label: 'Trigger <name>' })`.\n    - **Single-fire guard**: re-press ignored while that workflow's run is `running`. Tracks per-`wf.path` runId in local state.\n    - On runner completion: toast `success` (\"`<label>` dispatched.\") or `error` (\"… failed to dispatch.\"); invalidates `['pipeline','runs',projectRoot]` so the dispatched run surfaces. Notified-set prevents duplicate toasts.\n    - Button label/icon: `Play` → \"Trigger\"; while in flight `Loader2` spin → \"Triggering.\".\n- **Triggers row**: each `workflow.triggers[]` rendered as a humanized chip (`title` = raw event key).\n- **Stage graph** (per workflow with jobs) — hand-rolled SVG inside an `overflow-x-auto` Card:\n  - **Source** endpoint node (left, `GitBranch` icon, sub \"git push\"), vertically centered.\n  - **Job nodes** (`pipeline-job-<id>`, `NODE_W=256 × NODE_H=72`): column = `1 + max(col of needs)` (jobs with no in-repo `needs` → column 0; iterated to fixpoint). Stable row assignment per column. Label = `humanizeSlug(name)` wrapped to ≤2 lines (~34 chars/line, `<title>` = raw name); sub-line = `runs_on` (truncated 38). **No status dot is drawn** — the `JobNode` `status` prop is never plumbed in, so job nodes render with a neutral `stroke-border` and no per-job dot (status lives only at workflow level). `data-status` defaults to `'unknown'`.\n  - **Deploy** endpoint node (right, `Rocket`) rendered only when `data.vercelDetected` (sub \"vercel.json\") or `data.netlifyDetected` (sub \"netlify.toml\"); edges drawn from **terminal jobs** (jobs no other job `needs`) → Deploy.\n  - Edges = static cubic-bezier hairlines (`Beam`, `strokeOpacity 0.22`); no animation (working-surface rule). Edge sets: Source→col-0 jobs, job→dependent job (intra-repo `needs` only), terminal→Deploy.\n  - Workflow with **no jobs** → \"No jobs declared in this workflow.\" (no graph).\n- `humanizeSlug` (exported, reused by Newbie): splits on `-`/`_` and ` -- `/`:`, Title-cases, preserves acronyms (`ci, cd, pr, sbom, drc, erc, cli, api, sdk, ui, qa, e2e, url, yaml, json, pcb, mcp, os, vm`), lowercases connectors (`and, or, to, of, the, a, an, on, in, for, with`).\n\n### Guided view (`PipelineNewbie`)\n\n`pipeline-newbie`. Single column, max-width 820px, **first 6 workflows only** (`slice(0,6)`). No SVG, no YAML paths, no Trigger button.\n- Header subtitle \"CI workflows that run on every push\".\n- Per-workflow Card (`pipeline-newbie-wf-<path>`): humanized name + status pill (same `pillFor` mapping) + a plain-English **explainer** sentence + **See details** (sets persona to `seasoned`).\n- **Explainer** = `\"<capitalized when-clause>, it <does>.\"` built from real data:\n  - When-clause from triggers: `push`→\"on every push\", `pull_request`/`pull_request_target`→\"on pull requests\", `schedule`→\"on a schedule\", `release`→\"when a release is published\", `workflow_dispatch`→\"when started by hand\", `workflow_call`→\"when another workflow calls it\" (comma/and-joined).\n  - Does-clause = verb (nudged by name/path keywords: compliance→\"scans for secrets and license issues\", release/publish→\"cuts a release\", deploy→\"deploys the app\", lint/format→\"checks code style\", docs→\"builds the documentation\", qa→\"runs the UI grading checks\", test→\"runs the test suite\", `\\bci\\b`/`ci.yml`→\"builds and tests the repo\") + first 2 humanized job names (\"across X and Y\"); falls back to \"runs N jobs\".\n- Empty/no-root states mirror Operator with plain copy. Footer **Switch to Operator view** link (`pipeline-newbie-switch-power`).\n\n---\n\n## Release (`/release`, `views/release/`)\n\n`ReleasePanel.tsx` dispatches to `ReleaseSeasoned` (Operator) or `ReleaseNewbie` (Guided). Header icon `Rocket`, title \"Release\", subtitle \"Compliance posture + release readiness\".\n\n### Data sources\n\n- `useReleaseList()` → `bridge.release.list(root)`. Returns `ReleaseChecklist[]` summaries `{ version, status: 'in-progress'|'shipped'|'cancelled', ... }`. enabled when `root` set; staleTime 30s. Active version = `selectedVersion ?? list.data[0].version`.\n- `useReleaseChecklist(version)` → `bridge.release.get(root)` (returns full `ReleaseChecklist` incl. `gates: ReleaseGate[]`). enabled when version+root set; staleTime 30s.\n- `useReleaseSummary(version)` → `bridge.release.summary(root)` → `ReleaseSummary { version, overall_status: GateStatus, green, amber, red, blocked, gate_count }`. staleTime 30s.\n- `useEnvLocalNames(projectRoot)` for the env-vars card.\n- Source files: `docs/release-checklists/<version>.yaml`.\n- `ReleaseGate`: `{ id, label, artifact, status: GateStatus, notes?, blocker? }`. `GateStatus = 'green'|'amber'|'red'|'blocked'`.\n\n### Status taxonomy\n\n| `GateStatus` | Operator badge | Guided badge | Icon |\n|---|---|---|---|\n| `green` | Passing (success) | Passing (success) | `CheckCircle2` |\n| `amber` | Pending (warning) | Needs review (warning) | `AlertCircle` |\n| `red` | Failing (danger) | Blocked (danger) | `XCircle` |\n| `blocked` | Blocked (secondary) | Waiting (secondary) | `MinusCircle` |\n\n`aggregateGateStatus`: any red/blocked → `blocked`; else any amber → `pending`; else `green`. Empty gates → `pending`.\n\n### Operator view (`ReleaseSeasoned`) — `release-panel`\n\nHeader right cluster:\n- **Version `<select>`** (`release-version-select`), options `\"<version> (<status>)\"`, shown only when `list.data.length > 0`. Drives `selectedVersion`.\n- **Run all checks** (`release-run-checks`): **disabled**, `Planned` badge. Tooltip: \"Runs the QA harness against every gate. Not wired up yet; run checks from the CLI for now.\"\n\nList-level states: loading text; error EmptyState \"Could not read release checklists\" (`release-list-error`, prints error); empty EmptyState \"No release checklists yet\" (`release-list-empty`, points at `docs/release-checklists/<version>.yaml`).\n\n**Focal row** (`summary.data` present), 2-col on `lg`:\n- **ShipCard** (`release-ship-card`): `shipVerdict(summary, version)` →\n  - `open === 0` → headline \"Ready to ship `<version>`.\", detail \"All N release gates are passing.\", `ready: true`. Action = **Review N gates** (`release-hero-green`, filters `all`) + a disabled **Cut release** `Planned` button (`release-cut-planned`, tooltip \"Tags and publishes the release. Not wired up yet; cut the tag manually for now.\").\n  - `red+blocked > 0` → headline \"Blocked from ship.\", action **Open N blockers** (`release-hero-blocked`, filters `blocked`).\n  - else → headline \"Awaiting checks.\", action **View N pending** (`release-hero-pending`, filters `pending`).\n  - `open = amber+red+blocked`; `blocked = red+blocked`. Headline accompanied by `GateBadge(overall_status)`.\n- **GateDonutCard** (`release-gate-donut`): `Donut` size 84, segments `[green→emerald, (red+blocked)→rose, amber→amber]`, center = `green / gate_count`. Legend: passing / blocked / (pending if amber>0). Sub-line \"X blocked · Y pending\".\n- **SummaryCounts** (`release-summary`): `green / amber / red / blocked` counts + \"N gates total\".\n\n**Environment variables** (`EnvVarsCard`, `release-env-card`): `KeyRound` header + total-names badge (`release-env-count`). Per `.env` file (with `names.length > 0`): filename (mono) + variable-**name** badges (`release-env-name-<name>`). Note: \"Connect Vercel and link a project to check which of these are missing on the host.\" States: no project (`release-env-no-project`), read error (`release-env-error`), no files (`release-env-empty`). Names only — values never read into the renderer.\n\n**Gates** section (`SectionLabel` \"Gates\", shown when `gates.length > 0`):\n- `GateList` with filter `'all'|'blocked'|'pending'` (set by ShipCard actions). `matchesFilter`: `blocked` = red||blocked; `pending` = amber.\n- Filter chip (`release-gates-filter-chip`) \"Showing `<filter>` (N)\" + **Clear** (`release-gates-filter-clear`).\n- Per gate row (`release-gate-<id>`): status icon + `cleanCopy(label)` + `GateBadge` + mono `artifact` (`title` = full) + amber-without-notes skeleton (`release-gate-skeleton-<id>`) + `notes` + `blocker` (warning text) + **View** button (`release-gate-view-<id>`, only when `artifact` set) → `bridge.openExternal(g.artifact)`.\n- Empty-gates EmptyState \"No gates configured\" (points at `.refringence-qa/`).\n\n### Guided view (`ReleaseNewbie`) — `release-newbie`\n\nSingle column, max-width 820px. Header \"Release\" / \"Is this build ready to ship?\". Active version = `list.data[0].version` (no version picker).\n- **ShipHeroNewbie** (`release-newbie-ship-hero`): same donut (size 88) + headline (\"Ready to ship `<v>`\" / \"Blocked from ship\" / \"Awaiting checks\") + `StatusBadge(overall_status)` + detail sentence. Action **Review the checks** (`release-newbie-hero-green`) / **Open blockers** (`release-newbie-hero-blocked`) / **View pending** (`release-newbie-hero-pending`) → smooth-scroll to gates. Disabled **Cut release** `Planned` (`release-newbie-cut-planned`, tooltip \"This tags and publishes the release. It is not wired up yet.\").\n- **ShipReadyHero** (`release-newbie-hero`): \"How releases work\" explainer card + version badge.\n- **GateListNewbie** (`release-newbie-gates`): **top 3 gates only** (`slice(0,3)`). Each row (`release-newbie-gate-<id>`): icon + `cleanCopy(label)` + `StatusBadge` + \"What this means: `<plainEnglish(gate)>`\" + blocker (warning) + per-row **Show file** toggle revealing the mono `artifact`. `plainEnglish` prefers `gate.notes`, else status-default sentence. amber-without-notes → skeleton (`release-newbie-gate-skeleton-<id>`).\n- Empty EmptyState \"No release pipeline yet\" (`release-newbie-empty`).\n\n### Planned (disabled) affordances\n\n\"Run all checks\" and \"Cut release\" are intentionally disabled with a `Planned` badge in both personas; release cutting is done outside Console (manual tag). No artifact runner is wired to Release.\n\n---\n\n## Deploy to Vercel (zero-config)\n\nInitiated from **Services** (`views/services/ServicesPanel.tsx`, Vercel `ConnectionCard`), not from Pipeline/Release. Tokens are encrypted in the OS keychain and never returned to the renderer; only boolean/redacted status crosses the bridge.\n\nFlow:\n1. **Connect** — paste a Vercel token → `bridge.connections.vercel.connect(token)`. Card then shows `Connected as <user> · N projects` and exposes **Deploy** / **Disconnect**. **Deploy** is disabled until a project is open (`!projectPath`).\n2. **Detect** — `openDeploy()` → `bridge.connections.vercel.detectDeploy(projectRoot)` → `{ ok, settings?: VercelDetectedSettings, error? }`.\n   - `VercelDetectedSettings`: `{ framework, buildCommand, outputDirectory, installCommand, hasPackageJson, hasBuildScript, isStatic, suggestedName }`.\n   - On failure: toast `error`. On success: opens dialog (`vercel-deploy-dialog`), seeds editable **Project name** from `suggestedName`.\n3. **Dialog** — description branches on `detected.isStatic` (\"No build step detected — Console will deploy this as a static site.\") vs `\"Detected <framework> — Vercel will build and deploy it. No vercel.json needed.\"`. Shows `Framework: <framework|static> · Output: <outputDirectory|(auto)>`. Dialog is non-dismissable while `deploying`.\n4. **Deploy** — `runDeploy()` → `bridge.connections.vercel.deploy(projectRoot, { name, framework, buildCommand, outputDirectory, installCommand, target: 'production' })` → `{ ok, deployment?: VercelDeployResult { id, url, inspectorUrl, state }, error? }`. Sets status \"Uploading…\" → \"Building…\". On failure: toast + reset.\n5. **Poll** — `pollState(id)` → `bridge.connections.vercel.deployState(id)` every **3000ms** → `{ ok, state?, url? }`.\n   - Updates status line with `state`; surfaces `https://<url>` as a clickable link (`bridge.openExternal`).\n   - `state === 'READY'` → `setLastDeploy(projectPath, { url, name, id, at: Date.now() })` (localStorage key `refringence-console-deploy:<projectPath>`), toast success, stop, `onChanged()`.\n   - `state === 'ERROR' | 'CANCELED'` → toast error, stop.\n6. **Surfacing** — `getLastDeploy(projectPath)` (per-project, from `deployStore.ts`) feeds the **Overview** Deployment cell (state badge + live URL) and the Services Vercel card. Console shows only what *it* deployed for that project — never the account's global latest.\n\n`bridge.connections.vercel` also exposes `projects`, `deployments`, `redeploy` (used elsewhere on Services/Overview).\n\n---\n\n## Cross-references\n\n- Overview vitals: **Build** cell reads CI status (`metrics.ci`) and links to `/pipeline`; **Deployment** cell appears only when Vercel connected. Sidebar warning dot on `/release` when the release summary aggregates to blocked/red.\n- Pipeline Deploy node and Services deploy both key off `vercel.json` / `netlify.toml` detection (`vercelDetected` / `netlifyDetected`).\n- Live CI run status and the workflow Trigger both require the GitHub CLI (`gh`) installed and authenticated; absence degrades to shapes-only (Pipeline) or a disabled Trigger."
  },
  {
    "key": "services-connections-deploy",
    "title": "Services, connections and deploy",
    "guided": "# Services, connections and deploy\n\nThis page is about the **Services** panel — the place where you plug Console into the outside tools your project already uses (or will use), and where you can push your project live to the internet.\n\nYou'll learn what a \"service connection\" actually is, why Console handles your secret keys the careful way it does, and how to deploy your project to Vercel without configuring anything by hand.\n\n> [!INFO] **What is a \"service\"?**\n> Your project rarely lives alone. The code is on **GitHub**. It might be hosted on **Vercel**. Crashes get reported to **Sentry**. Your team talks in **Slack**. Each of those is a separate company with its own website and its own login. A *service connection* is just Console proving to one of those companies, \"I'm allowed to read this account,\" so it can pull your data in and show it next to your code. Nothing more magical than that.\n\n---\n\n## Where to find it\n\nOpen **Connect & deploy** in the sidebar (Operator mode calls the same panel **Services**). In Guided mode you see a short, curated list of four common starters: **GitHub**, **Vercel**, **Sentry**, and **Stripe**. If you want the full grid of every supported service, click **See all services** at the bottom — that flips you into Operator mode where the complete catalog lives.\n\n---\n\n## The four starters\n\nEach card explains, in one line, what connecting it gets you:\n\n- **GitHub** — connect your repo so Console can show issues, commits, and pull requests.\n- **Vercel** — see your deploy status and projects, and deploy your project, without leaving Console.\n- **Sentry** — surface the latest errors on your Workboard so nothing slips through.\n- **Stripe** — show live revenue and subscription state alongside the rest of your project.\n\nA green **Connected** badge appears on a card once it's wired up. That badge is never decorative — it reflects the real connection state that Console checks each time you open the panel.\n\n---\n\n## Connecting GitHub\n\nGitHub is the easiest one, and it's the one to do first, because most of Console (your Workboard, your release checks, your repo report) is built on top of it.\n\nClick **Connect** on the GitHub card. Console doesn't ask you to paste anything. Instead it reuses a tool you may already have installed: the **GitHub CLI** (`gh`).\n\n> [!INFO] **What is the GitHub CLI?**\n> `gh` is a small program from GitHub that you run in a terminal. Once you log into it once (with `gh auth login`), it remembers who you are. Lots of developer tools lean on that saved login instead of inventing their own. Console does exactly this: when you click Connect, it asks `gh` \"who is logged in?\" and records the answer. **Console never stores a GitHub password or token of its own** — it's borrowing the login `gh` already has.\n\nIf you haven't logged into `gh` yet, Console will tell you: **Run: gh auth login**. Open a terminal, run that command, follow the prompts, then come back and click Connect again. Once connected, the card shows **Connected as `<your-username>`**.\n\nDisconnecting just makes Console forget — your `gh` login stays exactly as it was.\n\n---\n\n## Connecting Vercel\n\nVercel is where you'll host your project (more on deploying below). Connecting it needs one thing GitHub didn't: a **token**.\n\n> [!INFO] **What is a token?**\n> A token is a long, random string of characters that acts like a password *for programs* instead of for humans. You generate it on the service's website, hand it to a tool, and the tool uses it to act on your behalf. The advantage over a real password: you can create several, give each a name, and revoke any one of them later without changing your actual login. Think of it as a single-purpose key you can cut copies of and throw away.\n\nClick **Connect** on the Vercel card. A dialog opens asking for an **Access token**. There's a **Create a token** link that opens the right page on Vercel's site. Generate one there, copy it, paste it into the dialog, and press **Connect**.\n\nConsole immediately checks the token against Vercel to confirm it's real, then shows **Connected as `<your-username>` · N projects**.\n\n> [!INFO] **Where does the token go? (This matters.)**\n> The token does **not** sit in a plain text file, and it is **never sent back to the screen** or to any AI. Console hands it to your operating system's built-in secret vault — the **Keychain** on macOS, **DPAPI** on Windows, the system keyring on Linux — which encrypts it before it ever touches your disk. From then on the secret lives only inside Console's background process: it's pulled out to talk to Vercel and put straight back. The part of Console you're looking at (the window) only ever learns *yes/no, connected* and your username. This split — secrets in the locked drawer, the UI only sees a green light — is the single most important idea on this page.\n\n---\n\n## Connecting Sentry\n\nSentry watches your *running* app and records crashes. Connecting it puts those real production errors right on your Workboard and Observability panel, so a bug your users hit shows up next to the code.\n\nSentry needs **two** things:\n\n1. An **auth token** (same idea as Vercel's — generate it on Sentry, there's a **Create a token** link).\n2. Your **organization slug** — the short name for your Sentry org, the bit that appears in your Sentry URL (e.g. `my-org`).\n\n> [!INFO] **Why a \"slug\" and not the full name?**\n> A *slug* is the URL-safe version of a name: lowercase, no spaces, dashes instead. Sentry can host many organizations, so the token alone isn't enough — Console needs to know *which* org's errors to fetch. The slug is how you point at it.\n\nFill both in, press **Connect**, and Console verifies the token with Sentry before saving. Once connected, your latest unresolved errors from the last 24 hours show up on the **Observability** panel, and a red dot appears in the sidebar when new errors arrive. The token is stored in your OS secret vault, exactly like Vercel's.\n\n---\n\n## Connecting Slack (Operator grid)\n\nIf your team reports bugs in Slack, you can pull those messages in as Workboard items. This card lives in the full Operator grid (**See all services**).\n\nSlack needs a **bot token**, which starts with `xoxb-`. You create it by making a small Slack \"app\" and installing it to your workspace — the dialog links you to the page. The bot needs permission to read channels and their history. Once connected, a **Map channels** button lets you tag each channel as **Tech**, **Non-tech**, or **Test** so the right messages land on your Workboard. (Console only sees channels the bot has been invited to.)\n\n---\n\n## A faster path: connect straight from your `.env`\n\nMost projects keep their secret keys in a file called `.env`. If Console finds keys it recognizes there, it offers a shortcut at the top of the panel under **From your .env**.\n\n> [!INFO] **What is a `.env` file?**\n> `.env` is a plain text file in your project where you store secrets and settings as `NAME=value` lines — like `VERCEL_TOKEN=abc123`. Your code reads from it at runtime. It's kept out of version control (it should be in your `.gitignore`) precisely *because* it holds secrets. It's the standard place a project stashes the very keys Console wants to connect with.\n\nConsole scans your `.env` and lists any services it can wire up — for example a `VERCEL_TOKEN` or `SENTRY_AUTH_TOKEN` it spotted. Each row shows the service name and the **key name** it found. Click **Link** and Console connects that service for you.\n\n> [!INFO] **It reads the names, not the values — until you ask.**\n> When Console *scans*, it only ever looks at the **names** of your keys (the `VERCEL_TOKEN` part), never the secret value after the `=`. Only the names cross into the window. The moment you click **Link**, the background process reads that one value just long enough to test it and lock it in the OS vault, then drops it. It's never shown back to you and never sent to AI. The on-screen note says exactly this — it's a promise the code keeps.\n\nThis works for OpenAI, Anthropic, and Google AI keys (which power the Assistant) as well as Vercel, Sentry, and Slack.\n\n---\n\n## Deploying your project to Vercel\n\nHere's the part that turns \"code on my laptop\" into \"a live website anyone can visit.\" Once Vercel is connected, the Vercel card grows a **Deploy** button.\n\n> [!INFO] **What does \"deploy\" mean?**\n> Deploying is taking your project's files, putting them on a server somewhere on the internet, and getting back a public web address (a URL) where your app now runs. Before deploying, only you can see your project. After, the whole world can. \"Zero-config\" means you don't have to write any setup files or answer a list of questions first — Console and Vercel figure out the sensible defaults for you.\n\n### How it works, step by step\n\n1. **Click Deploy.** Console reads your project and tries to recognize what it is — a Next.js app, a Vite app, an Astro site, a plain static site, and so on. It does this by peeking at your `package.json` (the file that lists your project's tools).\n\n   > [!INFO] **What is `package.json`?**\n   > It's the manifest for a JavaScript/TypeScript project: its name, and the list of libraries it depends on, and the commands to build and run it. Console reads the dependency list to guess your **framework** — if it sees `next`, it knows it's a Next.js app; if it sees `vite`, a Vite app — so it can tell Vercel how to build it.\n\n2. **A dialog opens** showing what Console detected: a **Project name** (you can edit it), the **framework**, and the **output directory** (where your built files will land). If Console found no build step at all, it says it'll deploy your project as a plain static site — that's fine, lots of projects are.\n\n3. **Press \"Deploy to production.\"** Console uploads your project's source files to Vercel.\n\n   > [!INFO] **What does Console *not* upload?**\n   > It deliberately skips things that shouldn't travel: your `node_modules` folder (the downloaded libraries — Vercel re-downloads them fresh), any build output (Vercel rebuilds it), your `.git` history, and — importantly — your **`.env` files and any keys or certificates**. Your secrets stay on your machine. This skip-list is built into the deploy, not something you have to remember.\n\n4. **Watch the status.** The dialog shows the deploy moving through stages — *Uploading → Building → READY*. Console checks Vercel every few seconds and updates the line for you. When it reaches **READY**, you get a clickable public URL. Click it to see your project live.\n\nThe first time you deploy, Vercel **creates the project for you** from the name you chose — so a single Deploy click handles both \"make a new project\" and \"ship it.\" Console also remembers this deploy for *this specific project folder*, so its live URL shows up on your **Overview** afterward.\n\n> [!INFO] **Why does the URL sometimes look different from the build URL?**\n> Vercel gives every deploy a long unique address, but it also assigns a short, clean public one (like `your-project.vercel.app`). Some of those long unique addresses are locked behind a login wall by Vercel's protection settings. Console prefers the clean public URL so the link you click actually opens for everyone.\n\n---\n\n## The privacy rule, in one sentence\n\nEvery secret you give Console — every token, every key — is encrypted by your operating system's vault and lives only in Console's background process. The window you look at, and any AI you chat with, only ever learn **connected: yes/no** and harmless labels like your username. That's the whole design.\n\n---\n\n## Quick reference\n\n| Service | What you provide | What it powers |\n|---|---|---|\n| **GitHub** | Nothing — reuses your `gh` CLI login | Workboard, releases, repo report |\n| **Vercel** | Access token (in a dialog) | Project list, deploy status, one-click deploy |\n| **Sentry** | Auth token + org slug | Live production errors on Workboard/Observability |\n| **Slack** | Bot token (`xoxb-...`) | Channel messages pulled into the Workboard |\n| **From `.env`** | Just click **Link** | Fast-connects any of the above it finds |\n| **PostHog, Stripe, others** | — | \"Open docs\" for now — real connection coming later |\n\n> [!INFO] **A note on the \"Open docs\" services**\n> Some cards (PostHog, Plausible, Netlify, Fly.io, Railway, Supabase, Stripe) don't have a real Console connection yet — clicking them simply opens that provider's setup page in your browser. They're placeholders for connections being built. GitHub, Vercel, Sentry, and Slack are the four that genuinely wire in today.",
    "operator": "# Services, connections and deploy\n\nReference for the **Services** panel (`/services`, `views/services/`). Connects Console to external providers and runs zero-config Vercel deploys. Persona-branched: `ServicesSeasoned` (Operator) and `ServicesNewbie` (Guided). Sidebar label is **Services** (Operator) / **Connect & deploy** (Guided). Default persona = `seasoned`.\n\nAll connection state flows through the `connections` bridge namespace (`window.refringenceConsole.connections.*`) and the `env` namespace. **Tokens never cross the IPC boundary.** The renderer receives only metadata: `{ connected: boolean, login?/user?/org?/team? }`. Backed by `console-electron/src/main/ipc/connections.ts`, `connections.ts` (store), `envConnect.ts`, and `ipc/vercelDeploy.ts`.\n\n---\n\n## 1. Real vs. docs-only services\n\nFour providers have a real connection backend. Everything else is a **DocsCard** that opens the provider's token page via `bridge.openExternal` and never stores anything.\n\n| Provider | Backend | Token type | Stored as |\n|---|---|---|---|\n| **GitHub** | `gh` CLI auth reuse | none stored | meta flag only |\n| **Vercel** | Vercel REST API | personal access token | safeStorage |\n| **Sentry** | Sentry REST API | auth token + org slug | safeStorage |\n| **Slack** | Slack Web API | bot token `xoxb-` | safeStorage |\n| Netlify, Fly.io, Railway, PostHog, Plausible, Supabase, Stripe | none | — | **DocsCard only** |\n\n**PostHog is docs-only.** Its card (`connectUrl: https://app.posthog.com/me/settings`) opens PostHog's settings page in the browser on click and toggles a local `opened` flag; there is no PostHog IPC, no stored token, no status check. The same is true of Plausible, Netlify, Fly.io, Railway, Supabase, and Stripe. The Overview's \"live activity counters\" copy for PostHog is aspirational — not wired.\n\n---\n\n## 2. Operator layout (`ServicesSeasoned`)\n\n`PanelHeader` (icon `Plug`, title \"Services\", subtitle \"Connect hosting, observability, payments, and storage\"). Header actions:\n- **CompareServicesButton** (`ServiceComparison.tsx`) — capability + 2026-pricing comparison, budget cap from `localStorage['refringence-console-budget-cap']`.\n- **Add MCP server** — `disabled`, `title=\"Coming soon\"`.\n\nBody order:\n1. **GitHub hero** (`services-hero`) — rendered only when `githubStatus === 'disconnected'`. One-click **Connect GitHub** (`services-hero-connect`) → `bridge.connections.github.connect()` → invalidates `['connections']`.\n2. **EnvConnectCard** (`From your .env`) — see §6.\n3. **Category sections** from `CATEGORIES`: Hosting (`host`), Observability, Messaging, Analytics, Database, Payment, Auth. Each renders the matching `SERVICES` cards in a 1/2/3-col grid.\n\n`SERVICES` catalog (id · category · blurb): `github` (host), `vercel` (host), `netlify` (host), `fly` (host), `railway` (host), `sentry` (observability), `slack` (messaging), `posthog` (analytics), `plausible` (analytics), `supabase` (database), `stripe` (payment). Card dispatch: `github`→`GithubCard`, `vercel`→`VercelCard`, `sentry`→`SentryCard`, `slack`→`SlackCard`, all others→`DocsCard`.\n\n### Status resolution (`ConnStatus`)\n`statusFor(connected)` collapses the `useConnections()` query into `loading | error | connected | disconnected`:\n- `connections.isError` → `error` → badge **\"Couldn't check connections\"**.\n- `isLoading || data === undefined` → `loading` → badge **\"Checking…\"**.\n- settled → `connected ? 'connected' : 'disconnected'` → **Connected** (green, check icon) / **Not connected**.\n\nThis deliberately avoids flashing \"Not connected\" before the query settles and surfaces backend failures honestly. `ConnectionCard` adds a `border-ring` highlight while connected.\n\n### Data reads\n- `useConnections()` → `bridge.connections.list()`, queryKey `['connections','list']`, `staleTime 30s`. Returns `ConnectionMeta { github, vercel, sentry, slack }`, each `{ connected, login?/user?/org?/team?, connectedAt? }`.\n- `useVercelProjects(enabled)` → `bridge.connections.vercel.projects()`, queryKey `['connections','vercel','projects']`, `staleTime 60s`, enabled only when `vercel.connected`. Gives the project count on the Vercel card.\n- `useActiveProject()` → `project.path` (the deploy target).\n\n---\n\n## 3. GitHub card\n\n`bridge.connections.github.connect()` / `.disconnect()`. **No token is stored.** The main handler runs `gh auth status`; on non-zero exit returns `{ ok:false, error:'Run: gh auth login' }`. On success it runs `gh api user --jq .login`, records `GithubMeta { connected:true, login, connectedAt }` in `connections.json`. Disconnect clears only the Console-side meta — the `gh` login is untouched.\n\n- Connected state: \"Connected as `<login>`\" + **Disconnect**.\n- Disconnected state: **Connect** (busy label \"Connecting…\").\n- On any result → `onChanged()` invalidates `['connections']`; sonner toast on success/error.\n\nSame gh-reuse pattern as `ipc/issues.ts`. 8s spawn timeout per gh call.\n\n---\n\n## 4. Vercel card\n\n### Connect\n**Connect** opens `vercel-connect-dialog`: password `Input` for the access token (Enter submits), a **Create a token** link → `https://vercel.com/account/tokens`. `bridge.connections.vercel.connect(token.trim())`:\n- Main validates against `GET https://api.vercel.com/v2/user`. On non-2xx → `{ ok:false, error }` (message lifted from the Vercel error body). On success extracts `user.username || user.name`, calls `setToken('vercel', token)` (safeStorage), saves `VercelMeta { connected, user, connectedAt }`.\n- Renderer shows toast, clears the token field, closes dialog, invalidates `['connections']`.\n\nConnected state shows \"Connected as `<user>` · N projects\" (count from `useVercelProjects`), plus **Deploy** and **Disconnect**. Disconnect → `clearToken('vercel')` + `clearMeta('vercel')`.\n\n### Deploy (zero-config) — `VercelCard` deploy flow\n`Deploy` is disabled unless `projectPath` is set (`title` flips to \"Open a project first\").\n\n1. **`openDeploy()`** → `bridge.connections.vercel.detectDeploy(projectRoot)` → `VercelDetectedSettings`. Opens `vercel-deploy-dialog`, seeds `deployName` with `settings.suggestedName`.\n2. Dialog shows: editable **Project name** `Input`; a line \"Framework: `<framework>` · Output: `<outputDirectory ?? (auto)>`\"; the description differs for `isStatic` (\"No build step detected - Console will deploy this as a static site\") vs detected framework (\"Detected `<framework>` - Vercel will build and deploy it. No vercel.json needed.\").\n3. **Deploy to production** → `runDeploy()` → `bridge.connections.vercel.deploy(projectRoot, settings)` with `{ name, framework, buildCommand, outputDirectory, installCommand, target:'production' }`. Status text walks `Uploading… → Building…`.\n4. **`pollState(id)`** loops `bridge.connections.vercel.deployState(id)` every 3000 ms. Updates `deployStatus`; sets `deployedUrl = https://<url>` when present. Terminal handling: `READY` → `setLastDeploy(projectPath, {url,name,id,at})` + success toast + `onChanged()`; `ERROR`/`CANCELED` → error toast, stops.\n5. Dialog cannot be dismissed (`onOpenChange` guarded) while `deploying`. The resolved URL renders as an accent link → `bridge.openExternal`.\n\n### Detection logic (`detectDeploy`, main)\nReads `package.json`, merges `dependencies` + `devDependencies`. Framework inference (first match): `next`→`nextjs`; `@sveltejs/kit`→`sveltekit`; `nuxt`→`nuxtjs`; `astro`→`astro` (out `dist`); `@angular/core`→`angular`; `gatsby`→`gatsby`; `vite`→`vite` (out `dist`); `react-scripts`→`create-react-app` (out `build`). `buildCommand`/`installCommand` returned `null` (Vercel uses framework defaults / `npm run build`). `isStatic = !pkg || !hasBuildScript`. `suggestedName = sanitizeName(pkg.name || basename(root))` (lowercase, alnum+`._-`, strips npm scope, ≤100 chars).\n\n### Upload logic (`deployProject`, main)\n`walkProject(root, maxFiles=4000)` SHA-1s each file. **Excluded dirs**: `node_modules .git dist build out .next .nuxt .vercel .svelte-kit .astro coverage .cache .turbo .parcel-cache .vite .output`. **Skipped files**: `.DS_Store`, `Thumbs.db`, `*.log`, `.env` and `.env.*`, `*.pem/.key/.p12/.pfx/.crt`, `id_rsa*`, `id_ed25519*`, `.npmrc`. Uploads via `POST /v2/files` (concurrency 6, `x-vercel-digest` = sha). Creates the deployment via `POST /v13/deployments` with `{ name, files, projectSettings, target }` — **the `name` field creates the Vercel project on first deploy** (single flow = create + deploy). Errors throw the Vercel body message.\n\n### State resolution (`deploymentState`, main)\n`GET /v13/deployments/:id`. Prefers the **shortest production alias** (clean public `<project>.vercel.app`) over the deployment-specific `url`, which Vercel deployment protection may gate behind a login wall. Returns `{ state: readyState ?? state ?? 'UNKNOWN', url }`.\n\n### deployStore (`lib/deployStore.ts`)\nPer-project-path record `StoredDeploy { url, name, id, at }` in `localStorage['refringence-console-deploy:<projectPath>']`. Read by the Overview Deployment cell so a freshly opened folder shows \"not deployed\" rather than an unrelated project's URL. `useVercelDeployments` (queries.connections, `refetchInterval 30s`) tracks BUILDING→READY for the Overview live URL.\n\n### Other Vercel bridge methods\n`vercel.deployments(projectId?)` → `GET /v6/deployments?limit=10[&projectId]`. `vercel.redeploy(projectId, deploymentId)` → `POST /v13/deployments` with `{ name, deploymentId, target:'production' }` (defensive; least-certain shape).\n\n---\n\n## 5. Sentry & Slack cards\n\n### Sentry (`SentryCard`)\n`sentry-connect-dialog`: **Auth token** (password) + **Organization slug** `Input`s, **Create a token** → `https://sentry.io/settings/account/api/auth-tokens/`. Both fields required (client-side toast guards). `bridge.connections.sentry.connect(token, org)`:\n- Main validates `GET https://sentry.io/api/0/` with the bearer token; on success `setToken('sentry', token)` + `SentryMeta { connected, user: org||'sentry', org, connectedAt }`.\n- Connected state: \"Connected: `<org ?? sentry>`\" + **Disconnect**.\n- `sentry.issues()` (used by Observability, not this panel) → `GET /api/0/organizations/<org>/issues/?query=is:unresolved&statsPeriod=24h&limit=10`; requires a stored org or returns \"no organization on record; reconnect Sentry\". Polled 60s via `useSentryIssues`.\n\n### Slack (`SlackCard`)\n`slack-connect-dialog`: **Bot token** (password, must start `xoxb-`), **Create a Slack app** → `https://api.slack.com/apps`. Description names required scopes: `channels:read`, `channels:history`, `groups:history` (private). `bridge.connections.slack.connect(token)`:\n- Main rejects non-`xoxb-` before any network call, then validates `GET https://slack.com/api/auth.test`. Slack returns HTTP 200 with `{ ok:false, error }` on app errors, so the body error is the real signal. On success `setToken('slack', token)` + `SlackMeta { connected, team, user, connectedAt, channelTeams (preserved) }`.\n- Connected state: \"Connected: `<team>`\" + **Map channels** (`slack-map-channels`) + **Disconnect**.\n- **Map channels** dialog (`slack-map-dialog`): `useSlackChannels(connected)` lists channels the bot can see; each has a `Select` (`slack-channel-team-<id>`) → none / tech / nontech / test → `bridge.slack.setChannelTeam(id, value)`, invalidating `['slack','channels']` + `['slack','issues']`. \"No channels visible\" hint instructs inviting the bot. Channel → team mapping drives the Workboard's Slack source.\n\n---\n\n## 6. EnvConnectCard (`From your .env`)\n\nRendered only when an active project path exists. `useQuery(['env','connectable', root])` → `bridge.env.scanConnectable(root)`, `staleTime 30s`. States: scanning (`services-env-scanning`), error + Retry (`services-env-error`), populated (`services-env-connect`), or hidden when empty.\n\nMain (`envConnect.ts`) parses `.env`, `.env.local`, `.env.development`, `.env.production` (first file wins per name; strips `export`, quotes). Connectors and key names:\n- `openai` ← `OPENAI_API_KEY`\n- `anthropic` ← `ANTHROPIC_API_KEY`\n- `google` ← `GOOGLE_API_KEY` / `GEMINI_API_KEY` / `GOOGLE_GENERATIVE_AI_API_KEY`\n- `vercel` ← `VERCEL_TOKEN` / `VERCEL_API_TOKEN`\n- `sentry` ← `SENTRY_AUTH_TOKEN` (org taken from `SENTRY_ORG`)\n- `slack` ← `SLACK_BOT_TOKEN` / `SLACK_TOKEN`\n\n**Names-only across IPC.** `scanConnectable` returns `EnvConnectable { id, name, category, keyName }` — `keyName` is the NAME only, never the value. **Link** → `bridge.env.connect(root, id)`: main reads the value **transiently in-process**, routes AI providers to `setKey` (AI keystore) and Vercel/Sentry/Slack to the same `connect*` validate+store path the manual dialogs use, then drops the value. On success: toast `Linked <name> from .env (<detail>)`, invalidates `['connections']` + `['ai','keyStatus']`, refetches the scan. The on-card note states the contract: names detected, value read transiently to test+store, \"never shown back, never sent to AI.\"\n\n---\n\n## 7. Token storage & encryption (`connections.ts`, store)\n\nTwo state kinds in `<userData>`:\n- **Non-secret metadata** → `<userData>/connections.json` (plain fs + JSON, *not* electron-store — that crashed on an ESM mismatch). Holds `{ connected, login/user/org/team, connectedAt, channelTeams }` per provider.\n- **Secret tokens** → `<userData>/connections/<provider>.token`, one file per provider, encrypted via Electron **`safeStorage`** (OS keychain on macOS, **DPAPI on Windows**, libsecret/keyring on Linux). `chmod 0600` best-effort (no-op on Windows ACLs).\n\n`setToken` throws if `safeStorage.isEncryptionAvailable()` is false — the connect is aborted rather than writing plaintext (surfaced to the user as \"Encryption is unavailable on this system…\"). `getToken` returns `null` (never throws) on missing/undecryptable. Tokens are read back per-request server-side, used to call the provider API, and **never returned to the renderer or logged**. Disconnect = `clearToken` + `clearMeta`.\n\n---\n\n## 8. Guided layout (`ServicesNewbie`)\n\n`PanelHeader` (icon `Plug`, title \"Services\"), intro \"A few services to plug Console into.\", single column `max-w-[820px]`, four `STARTERS`: GitHub, Vercel, Sentry, Stripe. `statusOf(id)` reads the same `useConnections()` data — the Connected pill is real, not cosmetic.\n\n- **GitHub** — connects in place (`bridge.connections.github.connect/disconnect`), filled primary button, label toggles Connect/Disconnect.\n- **Vercel / Sentry** — open an in-place `ServiceTokenDialog` (`service-connect-<id>`) rather than flipping to Operator. Token paste on connect (Sentry also takes org slug); on connect-when-already-connected the dialog shows **Manage** (open provider + destructive **Disconnect**). Same `vercel.connect` / `sentry.connect` bridge calls. Label: Connect / Manage.\n- **Stripe** — no backend; **Open docs** → `bridge.openExternal('https://dashboard.stripe.com/apikeys')`.\n- Footer **See all services** → `setPersona('seasoned')` (the Operator grid).\n\nNote: Guided Services has **no Deploy and no Slack** card and **no EnvConnectCard** — those live only in the Operator grid.\n\n---\n\n## 9. Bridge surface (`connections` + `env`)\n\n```\nconnections.list(): ConnectionMeta\nconnections.github.connect(): { ok, login?, error? }\nconnections.github.disconnect(): { ok, error? }\nconnections.vercel.connect(token): { ok, user?, error? }\nconnections.vercel.disconnect(): { ok, error? }\nconnections.vercel.projects(): { ok, projects?: VercelProject[], error? }\nconnections.vercel.deployments(projectId?): { ok, deployments?, error? }\nconnections.vercel.redeploy(projectId, deploymentId): { ok, deployment?, error? }\nconnections.vercel.detectDeploy(projectRoot): { ok, settings?: VercelDetectedSettings, error? }\nconnections.vercel.deploy(projectRoot, settings): { ok, deployment?: VercelDeployResult, error? }\nconnections.vercel.deployState(id): { ok, state?, url?, error? }\nconnections.sentry.connect(token, org): { ok, user?, org?, error? }\nconnections.sentry.disconnect(): { ok, error? }\nconnections.sentry.issues(): { ok, issues?: SentryIssue[], error? }\nconnections.slack.connect(token): { ok, team?, user?, error? }\nconnections.slack.disconnect(): { ok, error? }\nenv.localNames(projectRoot): EnvLocalNames\nenv.scanConnectable(projectRoot): EnvConnectable[]   // NAMES only\nenv.connect(projectRoot, serviceId): { ok, detail?, error? }\nslack.channels() / slack.setChannelTeam(id, team) / slack.issues()\n```\n\nIn browser dev (no Electron) the `env`/`connections` namespaces are throwing stubs."
  },
  {
    "key": "observability",
    "title": "Observability",
    "guided": "# Observability\n\nObservability is where you watch your project actually *run*. Two things live here, and it helps to keep them separate in your head:\n\n1. **Your own checks** - tests, evals, smoke checks, and CI - that you run on demand and watch stream live.\n2. **What happens after you ship** - real errors hitting real users, pulled in from Sentry once you connect it.\n\nThe first half answers \"is my code correct *before* I ship?\" The second answers \"is my code OK *now that people are using it*?\"\n\n> [!INFO] **What \"observability\" actually means.** In software engineering, observability is your ability to understand what a running system is doing from the outside - without attaching a debugger or guessing. It is built from three kinds of signal: **logs** (lines a program prints as it works), **metrics** (numbers counted over time, like \"errors in the last 24 hours\"), and **traces** (the path a single request took). This panel gives you a beginner-friendly slice of all three: live log output while a check runs, counters for runs and errors, and a feed of production errors.\n\nOpen it from the sidebar (**Monitor** in Guided mode) or press `g y` from anywhere.\n\n---\n\n## The big button: Run a test\n\nAt the top of the panel there is one primary button: **Run a test**.\n\nPress it. Console runs `npm run smoke` inside your project and shows you a small status line - \"Your test is running. This can take a minute.\" The button changes to **Running.** and is locked so you cannot start a second copy on top of the first.\n\n> [!INFO] **What a \"smoke test\" is.** The name comes from electronics: power on a new circuit board and if smoke comes out, something is badly wrong - no need for finer tests yet. A software smoke test is the same idea. It is a small, fast check that the most important things work at all: the app starts, the home page loads, nothing is on fire. It is not exhaustive. It is the *first* test you run, to decide whether running the slower, deeper tests is even worth it.\n\n> [!INFO] **What `npm run smoke` is doing.** `npm` is the tool that runs scripts defined in your project's `package.json` file. `npm run smoke` looks for a script literally named \"smoke\" and runs it. Console did not invent this command - it is reading the script *you* (or whoever set up the project) already defined. If your project has no `smoke` script, the test will report that the script was not found. That is normal and not a Console bug.\n\nWhen the run finishes you get a small toast notification - \"Test run finished.\" or \"Test run failed.\" - and the result appears below as a card.\n\n---\n\n## Reading the last run\n\nAfter a run completes, the **Last run** card tells you in one plain sentence how it went:\n\n- \"Your last test run finished cleanly.\" - everything passed.\n- \"Your last test run had 2 problems.\" - something failed; the number is how many failing artifacts were found.\n- \"Your last test run finished, but the result is unclear.\" - the run produced no files Console could read a pass/fail from.\n\nIt carries a colored badge - **Passing** (green), **Failed** (red), or **Unclear** (grey) - and tells you *when* it ran (\"12 min ago\", \"3:42 PM\").\n\n> [!INFO] **Why a result can be \"unclear\".** Console figures out pass/fail by looking at the *files a run leaves behind* in a folder. If a run wrote no files, or wrote files Console doesn't recognise, it honestly says \"unclear\" rather than guessing green. This is a deliberate engineering value: a monitoring tool that lies green when it doesn't actually know is worse than useless - you would trust it and ship a broken build. \"Unclear\" means \"go look yourself.\"\n\n### Open the report\n\nTwo buttons let you go deeper:\n\n- **See the report folder** (on the card) and **Open last test report** (below it) both open the folder on disk where the run saved its output - logs, screenshots, JSON, whatever the test produced. This opens in your file explorer, not inside Console.\n\n> [!INFO] **What an \"artifact\" is.** An artifact is any file a build or test run produces and saves for you to inspect later - a log file, a screenshot of a failing page, a coverage report, a JSON results file. The word just means \"a thing the run made.\" Console keeps every run's artifacts in its own folder so you can always go back and see exactly what happened, even weeks later.\n\n---\n\n## Recent runs\n\nBelow the last run, you see up to three more recent runs as a short list. Each row is one sentence - what kind of run it was, when, a status badge, and an **Open** button to reach that run's folder. This is your short-term history: \"did the last few checks pass?\"\n\nIf you have never run anything, you get an empty state: *\"No test runs yet. Press Run a test above, or open a terminal and run `npm run smoke`.\"*\n\n---\n\n## Switching to the full Operator view\n\nGuided mode keeps Observability deliberately simple - one button, one last-run card, a few recent runs. The Operator view adds far more: separate buttons for evals / end-to-end / smoke / CI, live streaming consoles, eval pass-rates, raw counters, the full runs table, and the production-error feed from Sentry.\n\nPress **Switch to Operator view** in the header any time you want the full surface. You can switch back from Settings. Nothing is hidden from you - it is just paced differently.\n\n> [!INFO] **The other kinds of checks you'll meet in Operator view.** A quick glossary so they aren't intimidating:\n> - **Eval** - short for *evaluation*. When your app uses an AI model, an eval is a test for the *AI's* answers: you give it known inputs and check the outputs are good. Console runs these with a tool called promptfoo. Evals are how you stop an AI feature from silently getting worse.\n> - **End-to-end (e2e)** - a test that drives the *whole* app the way a real user would: open the page, click the button, check the result appeared. \"End to end\" means from the very front (the click) to the very back (the database) and back again.\n> - **CI** - *Continuous Integration*. A robot that runs your tests automatically every time you push code, on a clean machine in the cloud, so \"works on my computer\" can't hide a broken build. Console's **Run CI** button triggers that robot on demand.\n\n---\n\n## After you ship: production errors\n\nThe bottom of the (Operator) panel is about what happens *after* your code is live and real people are using it. This is fed by **Sentry**, a service that catches errors thrown in your running app and reports them back to you.\n\n> [!INFO] **Why production errors are a different thing from test failures.** A test failure happens on *your* machine, before anyone sees it - you caught the problem early, which is the whole point. A production error happens on a *user's* machine, *after* you shipped - it is a problem that escaped every test and is hurting someone right now. That is why it gets its own section, its own red counter, and (in the sidebar) a red dot that pulls your attention. Both matter; they just live at different stages of the journey.\n\nUntil you connect Sentry, this section simply says *\"Connect Sentry in Services to see production errors.\"* Go to **Connect & deploy** (Services) to wire it up. Once connected, each unresolved error shows its title, where it came from, a severity badge, how many times it has happened, and an **Open** button to view it on Sentry's own site.\n\n> [!INFO] **The red dot in the sidebar.** When your project has errors in the last 24 hours, Console puts a small red dot on the Monitor item in the sidebar, and pops one desktop notification the first time the count crosses from zero. It is a gentle \"something needs your eyes\" - not an alarm. The number it watches is the same \"Errors / 24h\" counter you'll see in the Operator view.\n\n---\n\n## What to take away\n\n- **Run a test** is the one action that matters here - it runs your project's smoke check live and tells you, in a sentence, whether it passed.\n- A result can honestly be **unclear**; that is the tool refusing to fake confidence.\n- Every run keeps its files in a folder you can open and inspect.\n- **Before shipping**, you watch tests. **After shipping**, you watch production errors from Sentry. Same panel, two halves of the same question: *is my software actually working?*",
    "operator": "# Observability\n\nRoute `/observability` · sidebar `g y` · `views/observability/`. Persona branch in `ObservabilityPanel.tsx`: `isNewbie → <ObservabilityNewbie/>`, else `<ObservabilitySeasoned/>`. This page documents the Operator (`seasoned`) surface; the Guided variant is summarised at the end.\n\nHeader (`PanelHeader`): icon `Activity`, title **Observability**, subtitle \"QA run artifacts, counters, and trace logs\". Right slot renders an **as-of** freshness stamp (`obs-freshness`) = `max(runs.dataUpdatedAt, counters.dataUpdatedAt)` formatted `HH:MM`, shown only once data has loaded.\n\nLayout: single vertical scroll column (`observability-panel`), six `SectionDivider`-labelled sections in order: **Run → Evals → Counters → Production errors (Sentry) → Runs → Artifact log**.\n\n## Data sources\n\n| Hook | Bridge call | Notes |\n|---|---|---|\n| `useObsCounters()` | `bridge.obs.counters(root)` | `staleTime 30s`, `refetchInterval 60s`. Enabled only when a project is active. |\n| `useObsRuns()` | `bridge.obs.runs(root)` | `staleTime 30s`. Returns `RunEntry[]`. |\n| `useObsRunDetail(runId)` | `bridge.obs.runDetail(root, runId)` | Lazy; enabled only when a row is expanded (`runId !== null`). |\n| `useConnections()` | `connections.list` | `sentryConnected = data.sentry.connected`. |\n| `useSentryIssues(connected)` | `connections.sentry.issues` | Gated on `sentryConnected`. |\n| `useEvalsHealth()` | `bridge.evals.health(root)` | Used for `promptfooOutputPresent`. |\n| `usePromptfooSummary()` | `bridge.evals.promptfooSummary(root)` | `PromptfooSummary \\| null`. |\n| `useProjectCapabilities(root)` | `bridge.repoIntrospect.capabilities` | `hasEvals` / `hasTests` / `hasCiWorkflows` gate Run buttons. |\n| `usePipelineDetect(root)` | `bridge.pipeline.detect` | `workflows[0]` drives the `Run CI` invocation. |\n| `useRunner()` | `runner.start/stop` + `onOutput/onComplete` subscription | Streaming process runner (see below). |\n\n`root` is the active project's path (`useActiveProject`); all run/counter queries are no-ops with no project mounted. Runs and counters are scoped per project.\n\n`RunEntry` = `{ runId, startedAt(ISO), artifactKinds[], totalFiles, totalBytes }`. `ObsCounters` = `{ runs, runs_last_24h, errors, errors_last_24h }`. `RunDetail` = `{ runId, files: {relPath, kind, sizeBytes}[], status: 'ok'|'failed'|'unknown', statusSource: 'manifest'|'heuristic' }`. Runs are read from `.refringence-qa/runs/<runId>/` (artifact root; the Open buttons resolve paths relative to that).\n\n---\n\n## 1. Run section (`obs-run-surface`)\n\nToolbar (`obs-run-toolbar`) of four `RunButton`s. Each owns a stable `RunKey` (`evals` | `e2e` | `smoke` | `ci`) so it can self-disable while its own run is live (single-fire guard) and label the spawned process. Buttons use `variant=\"secondary\"`, `size=\"sm\"`.\n\n| Button | testid | Invocation (`runner.start`) | Enabled when | Disabled tooltip |\n|---|---|---|---|---|\n| **Run evals** (`FlaskConical`) | `obs-run-evals` | `kind:'npm'`, args `['run','eval:promptfoo']` | `capabilities.hasEvals` | \"No eval config found\" |\n| **Run e2e** (`TestTube`) | `obs-run-e2e` | `kind:'npm'`, args `['run','qa:e2e']` | `capabilities.hasTests` | \"No tests found\" |\n| **Run smoke** (`Zap`) | `obs-run-smoke` | `kind:'npm'`, args `['run','smoke']` | always | — |\n| **Run CI** (`GitBranch`) | `obs-run-ci` | `kind:'gh'`, args `['workflow','run',<firstWorkflow.path>]` | `hasCiWorkflows && firstWorkflow` | \"No CI workflows found\" / \"No workflow to trigger\" |\n\n`RunnerKind` ∈ `'npm' | 'gh' | 'playwright' | 'node'`. All four launch with `cwd = projectRoot || undefined` and `label = RUN_LABELS[key]`. A button shows `PlayCircle` (pulsing) + text **\"Running.\"** while its run is active and is `disabled` (the guard returns early if `isActive(key)`); `Run CI` no-ops if `firstWorkflow` is undefined.\n\n### Live output\n\nFor each `RunKey` with a tracked run, a block (`obs-run-live-<key>`) renders:\n- While `status==='running'`: a **`RunningIndicator`** — `Loader2` spinner (accent), label, live `mm:ss` elapsed ticking client-side from `startedAt`, and a **Cancel** button (`X`) calling `stop(runId)` → `runner.stop`.\n- A **`LiveConsole`** of accumulated `lines` (stdout/stderr, timestamped) for the run — streamed line-by-line via `runner.onOutput`.\n\n### Runner mechanics (`useRunner`)\n\nOne renderer-wide subscription to `runner.onOutput` (appends `RunLine{line,stream,ts}` to the matching `runId`) and `runner.onComplete` (`RunnerComplete{exitCode, durationMs, killed}` → `RunStatus`). `start(opts)` returns a `runId`; status mapping: `killed → 'killed'`, `exitCode===0 → 'done'`, else `'failed'`.\n\nCompletion side-effects (in `RunSurface`, via a `notified` ref set so each runId fires once across re-renders):\n- `done` → `toast.success(\"<label> finished.\")`\n- `failed` → `toast.error(\"<label> failed (exit <code>).\")`\n- `killed` → `toast.message(\"<label> cancelled.\")`\n- Always invalidates `['obs','runs']` and `['obs','counters']` so a fresh artifact run appears in the table and the counters refresh.\n\n---\n\n## 2. Evals section — `EvalsSummary`\n\nDriven by `usePromptfooSummary()` + `useEvalsHealth().promptfooOutputPresent`.\n\n- **Empty** (`obs-evals-empty`): when no promptfoo output present or no summary. Title \"No eval results yet.\" Subline branches on `hasEvals`: \"Use Run evals above to populate this card.\" vs \"No eval config found in this project.\"\n- **Summary** (`obs-evals-summary`): a row with a status `Badge` — **All passing** (`success`) when `failed===0 && errors===0`, else **Failures** (`danger`) — then three `EvalTally` figures: `passed` (CheckCircle2, success), `failed` (XCircle, danger when >0 else muted), `errors` (AlertCircle, warning when >0 else muted). Right-aligned \"Last run `<timestamp>`\" when present.\n\n`PromptfooSummary` also carries `total`, `totalCostUsd`, `durationMs`, and per-row `results[]` (not surfaced on this card). Source is `bridge.evals.promptfooSummary` reading the project's promptfoo output JSON.\n\n---\n\n## 3. Counters section (`obs-counters`)\n\nFour `CounterTile`s in a `grid-cols-2 sm:grid-cols-4`, values `toLocaleString()`, `'-'` while loading, zero rendered muted:\n\n- **Runs total** = `counters.runs`\n- **Runs / 24h** = `counters.runs_last_24h`\n- **Errors total** = `counters.errors`\n- **Errors / 24h** = `counters.errors_last_24h`, `tone='rose'` (danger text) when `> 0`.\n\n`Errors / 24h > 0` is the same signal that drives the sidebar red dot on `/observability` and the once-per-session OS notification (fired from the Sidebar, not this panel) when the 24h error count crosses 0→positive.\n\n---\n\n## 4. Production errors (Sentry) — `SentryErrors`\n\nStates:\n- **Not connected** (`obs-sentry-disconnected`): \"Connect Sentry in Services to see production errors.\" (no link element; go to `/services`).\n- **Loading**: \"Loading Sentry issues.\"\n- **No issues** (`obs-sentry-empty`): \"No unresolved errors.\" / \"No production errors in the last 24 hours.\"\n- **Issues** (`obs-sentry-issues`): one `Card` per `SentryIssue` (`obs-sentry-issue-<id>`): `title`; subline `culprit || 'unknown'` + relative `lastSeen`; level `Badge` (`danger` when level is `fatal`/`error`, else `warning`); `count×` (tabular); **Open** button → `bridge.openExternal(permalink)`, disabled when no `permalink`.\n\n`SentryIssue` = `{ id, title, culprit, level, count, lastSeen, permalink }`. Connection + issue fetch come from `connections.sentry.*`; the token lives in the OS keychain main-side and is never returned to the renderer.\n\n---\n\n## 5. Runs table — `RunsTable` / `RunRow`\n\nReads `useObsRuns()`. Rows are partitioned in `ObservabilitySeasoned`:\n- `realRuns` = `totalBytes > 0 && totalFiles > 0`\n- `emptyRuns` = `totalBytes === 0 || totalFiles === 0`\n\nStates: loading → \"Scanning .refringence-qa/runs.\"; both empty → `obs-runs-empty` (\"No QA runs recorded.\" + `npm run smoke` / `npm run eval:regression` hint).\n\nTable columns: **Run · Started · Status · Files (right) · Size (right)**. Header is uppercase muted. Each `RunRow` (`obs-run-<runId[:24]>`):\n- **Run** cell: chevron (expand state) + `humanizeRunLabel(run).title` (never the raw `runId`; raw id is the `title=` hover) + subline `<N> file(s)`.\n- **Started**: `label.when` — relative for recent (`just now`, `12 min ago`), clock time within 24h, `N days ago` < 7d, else `Mon D`.\n- **Status**: `StatusPill` — `ok` (success), `failed` (danger), `unknown`→\"empty\" (secondary). Status resolution: prefer manifest-backed `RunDetail.status` once a row is expanded; otherwise the `deriveStatus` heuristic = `unknown` if `totalFiles===0`, else `failed` if any `artifactKind` matches `/fail|error/i`, else `ok`. An **inferred** label (with explanatory `title=`) is shown when there is no detail yet or `statusSource==='heuristic'`.\n- **Files** / **Size**: counts; `formatSize` → B / KB / MB.\n\nRow click toggles expansion → `RunDetailFiles` (lazy `useObsRunDetail`): \"Reading artifacts.\" while loading; \"No artifact files in this run.\" when empty; else one row per file — `FileText` icon, monospace `relPath`, `kind`, `formatSize(sizeBytes)`, and an **Open** button → `bridge.openPath('.refringence-qa/runs/<runId>/<relPath>')` (error toast on failure; `stopPropagation` so it doesn't collapse the row).\n\nEmpty runs are hidden by default behind a toggle (`obs-toggle-empty-runs`, \"Show/Hide empty runs (N)\"); when shown they appear under an \"Empty runs (no artifacts)\" sub-header. If `realRuns` is empty but empty runs exist, the body shows \"No completed runs yet. Empty runs are hidden below.\"\n\n---\n\n## 6. Artifact log — `ArtifactLog`\n\nUp to **5** most recent `realRuns` (`realRuns.slice(0,5)`). Empty: \"Artifact paths appear here once the first run completes.\" Each row (`Card`): `FolderOpen` icon, `humanizeRunLabel(run).title` (raw id on hover), subline `<when> : <summarizeArtifactKinds(artifactKinds)>` (lists up to 3 humanized kinds, else \"N artifacts\"), and **Open folder** (`obs-open-<runId[:24]>`) → `bridge.openPath('.refringence-qa/runs/<runId>')`.\n\n---\n\n## Label humanization (`humanizeRun.ts`)\n\nRaw `runId`s are never rendered. `humanizeRunLabel` strips a trailing ISO-ish timestamp, splits on `-`/`_`, and maps lead tokens via `KIND_MAP` (`smoke→Smoke run`, `eval/evaluation→Eval run`, `regression→Regression run`, `e2e→End-to-end run`, `unit→Unit tests`, `perf→Performance run`, `a11y→Accessibility run`, `visual/snapshot→Visual snapshot`, `trace→Trace capture`, etc.). Phase-style tokens (`q0→Q0`, `e7→E7`) and acronyms (`qa, ci, cd, e2e, a11y, pr, ui, sbom, drc`) keep case. `summarizeArtifactKinds` drops a `critique-` prefix and Title-cases kinds.\n\n---\n\n## Keyboard / navigation\n\n- `g y` → this panel (global g-chord, `lib/useKeymap.ts`); yields while typing in inputs or while the palette is open.\n- Command palette action **\"Run eval\"** routes here.\n- No panel-local shortcuts beyond the global set.\n\n---\n\n## Guided variant (`ObservabilityNewbie`)\n\n`observability-newbie`. Single 820px column. Header subtitle \"Quick status of your recent test runs.\" with a **Switch to Operator view** button (`observability-newbie-switch-power` → `setPersona('seasoned')`).\n\n- One primary **Run a test** button (`observability-newbie-run-test`) → `runner.start({kind:'npm', args:['run','smoke'], label:'Run a test'})`, single-fire (`running` guard), shows \"Running.\" + a \"Your test is running. This can take a minute.\" status line. On completion: `toast.success('Test run finished.')` / `toast.error('Test run failed.')` and invalidates `['obs','runs']` (counters are not invalidated here).\n- **Last run** card (`newbie-last-run`): plain-English headline from `deriveStatus` + `countErrors` (\"finished cleanly\" / \"had N problems\" / \"result is unclear\"), `SimpleStatus` badge (Passing/Failed/Unclear), and **See the report folder** / **Open last test report** → `openPath('.refringence-qa/runs/<runId>')`.\n- **Recent runs** (`newbie-recent-runs`): up to 3 `realRows` as sentence rows with status badge + **Open**.\n- Empty: `newbie-runs-empty` (\"No test runs yet\" + `npm run smoke` hint).\n- No counters, evals card, Sentry feed, runs table, artifact log, or per-run file drill — those are Operator-only."
  },
  {
    "key": "repo-library-prompts",
    "title": "Repo, Library and Prompts",
    "guided": "# Repo, Library and Prompts\n\nThese three panels are where Console hands you the project itself. **Repo** orients you: what the project is, how to run it, what files it's missing, and how the pieces fit together by role. **Library** lets you read the project's own writing — its README, docs, and config files — rendered like a book. **Prompts** is your saved-prompt drawer: reusable instructions for an AI, with fill-in-the-blank slots.\n\nYou don't have to read them in order, but this is a natural path: orient (Repo) → read (Library) → act (Prompts).\n\n> [!INFO] What a \"repo\" is\n> \"Repo\" is short for **repository** — the folder that holds a software project and its history. When you point Console at a folder, that folder is the repo. Everything in these three panels reads from the folder you have open; nothing is uploaded anywhere.\n\n---\n\n## Repo — get your bearings\n\nOpen Repo from the sidebar (or press `g r`). At the top you'll see the project's name and a one-line shape: its main language, its size, and how many separate pieces it has. Next to that:\n\n- A **start command** chip (like `npm run dev`) when Console can tell how the project is meant to run.\n- A small coloured **status dot**: green \"runnable\" when there's a clear way to start it, amber \"no start command\" when Console found code but no obvious launch command, and \"reading\" while it's still looking.\n- A **Set up for Console** button (more on that below).\n\n> [!INFO] What \"runnable\" means\n> A project is *runnable* when it declares a way to start itself — usually a \"start\" or \"dev\" script in a file like `package.json`. That command is how a developer turns source code into a running program. If Console can't find one, it doesn't mean the project is broken; it just means the launch command isn't written down where Console looks.\n\n### The two tabs\n\n**Structure** (the default) is the orientation view. **Files by size** is a more technical view that ranks files by how many lines they contain — useful later, ignore it for now.\n\n### How to run it\n\nIf the project is runnable, the Structure tab leads with the start command in a card with a **copy** button. Console can't run a terminal for you, so copying is the action — paste it into your own terminal, in the project folder, to start the project.\n\n### The health checks\n\nBelow that, three checks tell you whether the project is set up the way a healthy one should be. Each runs automatically and reads only your local files (the dependency check makes read-only lookups to public databases — it never installs or changes anything).\n\n**Project setup** lists the housekeeping files a healthy project carries, with a progress bar and a count of how many are in place:\n\n> [!INFO] The files a healthy project keeps\n> - **README** — the front-page explanation of what the project is and how to run it.\n> - **LICENSE** — the legal terms for using the code. Without one, others technically can't reuse it.\n> - **.gitignore** — a list of files Git should *not* track (build output, secrets, junk). Keeps the history clean.\n> - **CI** — \"continuous integration\": an automated checker that runs your tests every time code changes. (See the Pipeline panel.)\n> - **lockfile** — pins the exact versions of every dependency so the project installs the same way on every machine.\n> - **tests** — automated checks that prove the code still works.\n> - **.env.example** — a template listing the *names* of the secret values the project needs, with no real values filled in.\n>\n> Each missing item shows a one-line suggestion and a **Fix** button.\n\n**Dependency health** checks the outside code your project pulls in:\n\n> [!INFO] What dependencies are\n> A *dependency* is someone else's code your project relies on. Most projects have dozens. Console checks them two ways: against **OSV.dev** (a public database of known security holes) for *vulnerable* packages, and against the package registry for *outdated* ones (a newer version exists). \"Vulnerable\" is the urgent one — it means a known weakness is sitting in your project. A **Re-check** button forces a fresh look.\n\n**Exposed secrets** scans your source code for keys and tokens that got pasted directly into a file:\n\n> [!INFO] Why a secret in source is dangerous\n> A *secret* is a password-like value — an API key, an access token — that proves your project is allowed to use a paid or private service. If one is written into a normal source file and that file goes into Git history, anyone who sees the code can use your account. The fix is always the same: move it into a `.env` file (which `.gitignore` keeps out of history) and read it from there. Console only ever shows you a blurred preview of what it found, never the full secret. (It deliberately skips your `.env` files — secrets are *supposed* to live there.)\n\n### The Fix button\n\nEvery finding — a missing file, a vulnerable dependency, an exposed key — has a **Fix** button. It doesn't change your code. Instead it writes a clear, project-specific instruction you can hand to an AI coding tool. The instruction already knows your stack and your start command, so it's specific, not generic. From the dialog you can:\n\n- **Copy** it to paste anywhere.\n- **Save to library** — stores it in the Prompts panel under a \"Setup\" category so you can reuse it.\n- **Cursor rules** — appends it to a file the Cursor editor reads (only shown if Cursor is installed).\n- **Run in Claude Code** — sends it straight to the Claude command-line tool (only shown if that tool is installed).\n\n> [!INFO] What \"hand it to an AI tool\" means\n> Console's job here is to *describe* the fix precisely; a separate coding agent (Cursor, Claude Code) does the actual editing. This split is on purpose — you stay in control, reviewing the instruction before any code changes.\n\n### Structure — the project by role\n\nThe rest of the Structure tab groups the project's pieces by **what each one does**, not by name or size. The groups read top-to-bottom in a \"start here\" order:\n\n- **App shells** — the runnable entry points you actually launch.\n- **User interface** — the screens and components people see.\n- **Services** — backend or background code.\n- **Libraries** — shared code other pieces import.\n- **Tool wrappers** — typed wrappers around bundled command-line tools.\n- **Build and scripts** — programs that build, package, and release.\n- **Tests** — automated checks.\n- **Docs** — written guides and decisions.\n- **Other** — everything else.\n\n> [!INFO] Why grouping by role helps\n> A big project can have twenty folders and no obvious place to start. Grouping by role answers \"where does the program actually begin?\" (App shells), \"where's the screen I'm looking at?\" (User interface), and so on. It's a map keyed to *purpose*, which is what you need when you're new to a codebase.\n\nOn a wide screen, a side panel called the **Newcomer guide** appears on the right. It's a reading aid built from the real project:\n\n- **Project summary** — the project's name, description, languages, license, and run commands.\n- **Hot files** — the files changed most often in the last 30 days. Frequently-changed files are usually the ones that matter most.\n- **Recommended reading order** — the files most other files depend on, ranked. Reading these first gives you the backbone of the project fastest.\n\n### Set up for Console\n\nThe **Set up for Console** button (top right) does the reverse favour: it writes a short guideline *into your project* telling any AI coding agent how to shape the project so Console reads it cleanly. You preview the exact text first, then choose where it lands — **AGENTS.md** or **.cursorrules**. It writes into a clearly-marked managed block, so your own notes in those files are left untouched.\n\n---\n\n## Library — read the project like a book\n\nOpen Library from the sidebar (or press `g b`). Library finds the project's *written* files — READMEs, docs, config, workflow files, the license, the changelog — and lets you read them in a clean reading pane instead of a code editor.\n\n> [!INFO] Why a separate reader\n> The most valuable explanation of a project is usually already written down in it — but it's scattered across folders and mixed in with code. Library gathers just the readable files and renders them like web pages, so you can *read* the project before you try to change it. This is the single fastest way to understand a new codebase.\n\n### Browsing\n\nThe left sidebar is a **folder navigator**. It shows one level at a time: the folders and files directly inside wherever you are. Click a folder to drill in; the count next to each folder tells you how many readable files it holds. **Back** climbs up a level, and a breadcrumb trail across the top lets you jump straight back to any parent. You can collapse the whole sidebar to give the reading pane the full width.\n\n### Reading\n\nClick a file and it opens on the right:\n\n- **Markdown** files (`.md`, like a README) render as formatted text — headings, lists, links — exactly as intended. A small badge shows the word count and an estimated reading time.\n- **Config** files (`.json`, `.yml`, `.yaml`, `.toml`) render with syntax colouring so they're easy to scan.\n- Anything else shows as plain text.\n\n> [!INFO] What \"Markdown\" is\n> *Markdown* is a plain-text way of writing formatted documents: `# Title` becomes a heading, `- item` becomes a bullet. Almost every project's README is written in it. Library renders it the way it's meant to look, so you read the prose, not the symbols.\n\nTwo actions sit in the header of the reading pane:\n\n- **Copy path** — copies the file's full location to your clipboard.\n- **Open in editor** — opens the file in VS Code (or your default editor) if you want to edit it.\n\n> [!INFO] About the 256 KB limit\n> Very large files are cut off at 256 KB with a note. This keeps the reader fast — documentation files are almost always far smaller. If you hit the limit, **Open in editor** shows you the whole thing.\n\n---\n\n## Prompts — your reusable instructions\n\nOpen Prompts from the sidebar. A **prompt** is just a piece of text you give to an AI: a question, a task, an instruction. The Prompts panel is a place to save the ones you use often, with *blanks* you fill in each time.\n\n> [!INFO] Why save prompts at all\n> When you find a wording that gets good results from an AI — say, \"explain this error in plain words\" — you don't want to retype and re-perfect it every time. A saved prompt with a blank for the error lets you reuse the good wording and just drop in today's specifics. Prompts are stored **per project**, in a `.refringence-console` folder inside the project, so each project keeps its own set.\n\n### The layout\n\nThe left column lists your saved prompts with a search box and filter chips: **Favorites**, plus a chip per category and per tag. The right side is where you fill in and use the selected prompt.\n\n### Filling a prompt\n\nA prompt's text can contain **blanks** written as `{{name}}`. When you select a prompt, Console shows one input per blank and a **live preview** of the finished text that updates as you type. Blanks come in three kinds:\n\n- **Text** — a single line.\n- **Multiline** — a box that grows, for pasting something long like an error message.\n- **Select** — a dropdown with fixed choices.\n\n> [!INFO] Why blanks instead of editing the text directly\n> The blanks keep the *good part* of the prompt fixed and let you change only the specifics. The preview shows you the exact text that will be sent, so there's no guessing. Nothing is sent automatically — you always see the final result first.\n\n### Using a prompt\n\nOnce it's filled, you have two plain actions:\n\n- **Copy** — puts the finished text on your clipboard to paste anywhere.\n- **Send to your AI** — drops the finished text into Console's built-in AI assistant (it opens with the text waiting in the box). It does *not* send by itself — you read it, then press send. That deliberate pause is so you're never surprised by what goes out.\n\nIn Guided mode the panel walks you through this as three calm steps — **Pick a prompt → Fill the blanks → Copy or send** — with a Back/Next bar and a line telling you what comes next.\n\n### Making your own\n\n**New prompt** (top right) opens an editor. Type your text, and anywhere you write `{{something}}` Console treats it as a blank you can then describe (give it a label, choose its kind, set options for a dropdown, set a default). Give the prompt a title, a category, and optional tags, and save. You can edit, favourite, or delete any prompt later.\n\n### Skills\n\nThe **Skills** button opens a small catalogue of curated *agent skills* — reusable abilities you can install into the project for a coding agent (Claude Code or Codex). Installing one writes a file inside the project's own `.claude/skills` or `.codex/skills` folder; nothing on your wider system is touched.\n\n> [!INFO] What an \"agent skill\" is\n> A *skill* is a packaged instruction set that teaches an AI coding agent how to do a specific job well. Installing one into your project means that, next time you use that agent here, it already knows the technique — you don't have to explain it each time.\n\n---\n\n## How the three fit together\n\nA common loop: **Repo** flags something (\"missing tests\", \"a vulnerable dependency\"), its **Fix** button turns that into a precise instruction and can **Save to library** — which is the **Prompts** panel — and meanwhile **Library** lets you read the project's own docs to understand what you're changing before you change it. Orient, read, act.\n",
    "operator": "# Repo, Library and Prompts\n\nThree project-grounded panels. **Repo** (`/repo`, `g r`) is the orientation and health surface. **Library** (`/library`, `g b`) is an in-app reader for the project's text files. **Prompts** (`/prompts`) is the per-project prompt template store with a dev-tool handoff router. All three branch on persona at their root (`if (isNewbie) return <…Newbie/>; return <…Seasoned/>`); this reference documents the Operator variants and notes Guided deltas where they matter.\n\n---\n\n## Repo (`/repo`)\n\n`RepoPanel.tsx` → `RepoSeasoned`. Reads `useRepoSummary` (`repo.summary`), `useProjectShape` (`repoIntrospect.shape`), `useProjectCapabilities` (`repoIntrospect.capabilities`). `root` resolves to `activeProject.path ?? repo.data.repo_root`.\n\n### Header\n\n- **Title/subtitle**: \"Repo\" + the project shape line (`shapeSubtitle(shape, total_loc)`, run through `cleanCopy`). Falls back to `\"<N> packages, <LOC> LOC\"` when shape isn't ready, else \"Computing repo summary\".\n- **Start-command chip** (`repo-run-hint`): rendered only when `shape.runnable && shape.startCommand`. Mono text, Play icon.\n- **RunnableDot** (`repo-health-dot`): tri-state — muted \"reading\" (no shape yet), green \"runnable\" (`shape.runnable`), amber \"no start command\" (shape present, not runnable). Appends \" · CI\" when `caps.hasCiWorkflows`.\n- **GuidelineSetup** button (`guideline-setup-open`) — see below.\n\n### Tabs\n\nTwo tabs (`repo-tab-structure`, `repo-tab-files`), active tab gets a 2px accent underline.\n\n#### Structure tab (`repo-structure`)\n\nConstrained to `max-w-3xl`. Order:\n\n1. **How to run it** (`repo-how-to-run`) — only when `shape.runnable && shape.startCommand`. Play icon + mono `startCommand` + optional `entryPoint` (right-aligned, titled) + copy button (`repo-how-to-run-copy`, 1.5 s check state). Copy is the only affordance — the panel cannot spawn a shell.\n2. **HygieneCheck** — `bridge.hygiene.scan(root)`, `staleTime` 5 min, auto-run, no refetch-on-focus. Renders nothing if `items.length === 0`.\n3. **DependencyHealth** — `bridge.deps.scan(root)`, `staleTime` 10 min. Renders nothing if `total === 0`.\n4. **SecretsCheck** — `bridge.secrets.scan(root)`, `staleTime` 10 min.\n5. **Structure** — packages grouped by inferred role.\n\n**HygieneCheck** (`repo-hygiene`): \"Project setup\" section. Each `item` = `{id, label, present, detail?, suggestion?}`. Header tile shows a check (all present) or the present count; subtitle \"`<present>` of `<total>` essentials in place\" / \"Setup complete\". Progress bar = `present/total`. Present items render as green chips (license chip appends `· <detail>` when detail ≠ \"present\"). Each missing item (`repo-hygiene-missing-<id>`) shows \"Missing `<label>`\" + suggestion + a **FixButton** with kind `missing-<id>` (revealed on row hover/focus). IDs: `readme`, `license`, `gitignore`, `ci`, `lockfile`, `tests`, `env-example`.\n\n**DependencyHealth** (`repo-deps-health`): \"Dependency health\" + **Re-check** (`repo-deps-recheck`, \"Checking…\" while fetching). Body: `Checked <n>`, `Vulnerable <n>` (red when >0), `Outdated <n>`. OSV.dev + npm registry, read-only network, never installs/writes/runs.\n- Vulnerable list (first 6, `repo-vuln-<name>`): `name@version` + advisory-count link → `bridge.openExternal('https://osv.dev/vulnerability/<vulns[0].id>')`; \"+N more vulnerable\"; then `FixButton kind=\"vulnerable-deps\"` with detail = comma-joined `name@version`.\n- Outdated list (first 12 chips `name current→latest`; \"+N more\"); `FixButton kind=\"outdated-deps\"` detail = `name current->latest`, comma-joined.\n- `d.error` renders as \"Partial result: …\".\n\n**SecretsCheck** (`repo-secrets-check`): \"Exposed secrets\" + **Re-scan**. Scans source + config for well-known token formats + private keys; renderer only ever receives a redacted `preview`. `.env` files are skipped by design. Clean: \"No keys found in source. Scanned `<n>` files.\" Findings (first 8, `repo-secret-finding`): `file:line` + `type` badge + mono `preview`; \"+N more\"; `FixButton kind=\"exposed-secret\"` detail = comma-joined `file:line`.\n\n**Structure section**: `groupByRole(packages)` buckets each package via `inferPackageRole(name)` (name/path regex), sorts each bucket by `total_loc` desc, and emits groups in `ROLE_ORDER`: `app` (App shells), `ui` (User interface), `service` (Services), `library` (Libraries), `tool-wrapper` (Tool wrappers), `tooling` (Build and scripts), `tests` (Tests), `docs` (Docs), `other` (Other). Each group: label + blurb (`ROLE_META`); rows (`repo-struct-pkg-<name>`) show package name (mono, titled with path), `packageEntryLabel(role)` (\"launches the app\", \"renders the interface\", \"runs in the background\", \"shared code\", …), and right-aligned `<loc> loc`.\n\nStates: loading \"Scanning packages.\", error \"Failed to read repo summary: …\", empty \"No packages detected. Pick a project folder from the top bar.\"\n\n#### Files by size tab (`repo-tree` + article)\n\nLOC-ranked dump (kept intact, demoted behind the tab). Left nav (`repo-pkg-<name>`): every package with `<file_count> files, <total_loc> loc`; selection defaults to `selectedPkg ?? packages[0]`. Right article for the active package:\n- Header: `path` (mono, `repo-active-pkg`) + `total_bytes` in KB.\n- **Languages** (`repo-languages`): top 12 of `activePkg.languages` (loc desc), each `<Lang> <loc>`, language label via `sentenceCaseLang` (HTML/CSS/XML/JSON/YAML/TOML/SQL/TSX/JSX upper-cased).\n- **Top files by line count** (`repo-files`): `sample_files.slice(0,30)` as a `File | Language | Lines | Size(KB)` grid. Empty: \"No source files indexed yet. Run a scan to populate.\"\n\n### Aside — NewcomerGuide (`repo-newcomer-aside`, `xl:` only)\n\n`NewcomerGuide` (`newcomer-guide`), three cards:\n- **Project summary** (`newcomer-summary`) — `repoIntrospect.summary`: name, cleaned description, top-8 languages with counts, license, run commands. Error: \"Failed to read package.json, pyproject.toml or Cargo.toml.\"\n- **Hot files (last 30 days)** (`newcomer-hotfiles`) — `repoIntrospect.hotFiles(root, 30)`: ranked path + lines changed + commit count. Error: not a git repo / git not on PATH.\n- **Recommended reading order** (`newcomer-reading`) — `repoIntrospect.readingOrder`: TS/JS files ranked by inbound import count (`<score> in`). Error: no TS/JS files found.\n\n### GuidelineSetup (`guideline-setup-open`)\n\n\"Set up for Console\". Icon flips to a check when `status.agentsMd || status.cursorRules` (`bridge.guidelines.status`). Dialog (`guideline-setup-dialog`): read-only preview of `bridge.guidelines.generate().content` (lazy, `enabled: open`, 5-min stale); target toggle **AGENTS.md** (`agents-md`, default) / **.cursorrules** (`cursorrules`); **Write guideline** (`guideline-setup-write`) calls `bridge.guidelines.write(root, target)`, writes a managed block (user notes preserved), toasts, closes, refetches status.\n\n### FixWithPrompt (the health-loop close)\n\n`FixButton kind=<FixKind>` opens `FixDialog` (`fix-dialog`). `FixKind` ∈ `missing-readme | missing-license | missing-gitignore | missing-ci | missing-lockfile | missing-tests | missing-env-example | vulnerable-deps | outdated-deps | exposed-secret`. `buildFixPrompt` is **deterministic** (no model call), parameterised by `useStackDetect(root).stacks`, `shape.startCommand` (only when runnable), and the finding `detail`; Python stacks switch the test framework to `pytest`, else `vitest`. Dialog body shows the generated prompt (`fix-prompt-body`). Actions:\n- **Copy** → clipboard.\n- **Save to library** → `bridge.prompts.create(root, {title, body, variables:[], category:'Setup', tags:['setup', kind]})`.\n- **Cursor rules** (only when `devhandoff.detect().cursor`) → `bridge.devhandoff.writeCursorRules(root, body, 'append')`.\n- **Run in Claude Code** (when `detect().claudeCli`) → `bridge.devhandoff.runClaude({prompt, cwd:root})`, closes on success; otherwise the primary button degrades to **Copy prompt**.\n\n### Guided deltas (`RepoNewbie`)\n\nSingle-column `max-w-[820px]`. Plain-English lede (`describeShape`). \"How to run it\" card with copy. Packages grouped by the same roles but listed as `prettyPackageLabel(name)` + mono raw name (no LOC, no hygiene/deps/secrets blocks, no NewcomerGuide). \"Show developer commands\" disclosure lists `summary.runCommands` (each with copy). \"See the full code map\" / \"Switch to Operator view\" flips persona to `seasoned`.\n\n---\n\n## Library (`/library`)\n\n`LibraryPanel.tsx` → `LibrarySeasoned`. Reads `useLibraryList(root)` (`library.list`, 60 s stale) and `useLibraryFile(root, relPath)` (`library.read`, 30 s stale). `root = activeProject.path`. No project → `EmptyPickFolder` (picks a folder via `bridge.project.pickFolder`, writes `refringence-console-active-project` + dispatches `console-active-project-change`).\n\n### Data shape\n\n`library.list` returns `LibraryEntry[]`: `{path, relPath, ext, size, mtimeMs, category}` where `category ∈ docs | config | data | workflow | license | readme | changelog | other`. The IPC scans only readable doc/config/workflow/license/readme/changelog/data files (not arbitrary source). `library.read` returns `LibraryFile`: `{content, mime, truncated}`. Subtitle: \"Scanning `<name>`…\" while loading, else \"`<N>` files in `<name>`\".\n\n### Sidebar — folder drill-down (`LibraryFolderNav`, `library-folder-nav`)\n\nThe flat `LibraryEntry[]` is treated as a virtual tree keyed on posix `relPath` segments; the nav shows exactly **one level** under `currentPath` (`'' = root`). `levelEntries` aggregates each folder's recursive file count for its badge.\n- Header: **Back** (`library-folder-back`, disabled at root, climbs to `parentOf`), current folder name (`library-folder-current`), **collapse** (`library-sidebar-collapse`). Collapsed → a thin rail with **expand** (`library-sidebar-expand`).\n- Breadcrumb (`library-folder-breadcrumb`, hidden at root): clickable crumbs back to root, last crumb inert. Folder/file display names run through `humanizeTitle`.\n- Folder rows (`library-folder-dir-…`): folder icon, humanized name, recursive file count, chevron → `onEnterFolder`. File rows (`library-folder-file-…`): file icon, humanized name, `ext` badge (suppressed in Guided's `calm` mode). Active file gets `bg-secondary`.\n- States: shimmer skeleton while loading; \"This folder is empty.\" when a level has no folders/files.\n- Project change resets `currentPath` and `activeRel` to root/none.\n\n### Reading pane (`RightPane`)\n\nNo file selected → EmptyState \"Nothing open yet\". With a file:\n- Header: `<projectName>/<relPath>` (mono relPath). For `.md`, a `library-read-stats` badge = `<words> words, <minutes> min read` (code fences and markdown punctuation stripped; `minutes = max(1, round(words/200))`). Actions: **Copy path** (`<root>/<relPath>` to clipboard) and **Open in editor** (`bridge.openExternal('vscode://file/<absPosix>')`).\n- Body (`library-body`), `max-w-[72ch]`, by extension:\n  - `.md` → `marked.parse(content, {async:false})`, rendered HTML.\n  - `.json` / `.yml` / `.yaml` / `.toml` → `highlight.js` (`hljs.highlight`, falling back to `highlightAuto`), `langFromExt` mapping.\n  - else → plain `<pre><code>`.\n  - `truncated` → TruncatedNote: \"File truncated at 256 KB. Open in editor to see the rest.\"\n- States: \"Reading file from disk.\" / \"This file could not be read. It may have been moved or deleted.\"\n\nRender payloads (`FileBody`) are computed unconditionally via `useMemo` then selected (Rules-of-Hooks fix; previously crashed on file-kind change).\n\n### Guided deltas (`LibraryNewbie`)\n\nSame `LibraryFolderNav` in `calm` mode (drops the ext badge) with a footer **Switch to Operator view** (flips persona). Reader is a back-navigable `Reader` (`library-newbie-reader`) with a **Back** button, a title derived from the file's first H1 (`.md`) or `humanizeTitle(basename)`, the same read-stats and **Open in editor**. Non-text extensions (anything outside `.md .yml .yaml .json .toml .txt` and extensionless) show a \"cannot be previewed in app\" card (`library-newbie-binary`) routing to the OS handler via `bridge.openPath`. Same 256 KB truncation.\n\n---\n\n## Prompts (`/prompts`)\n\n`PromptLibraryPanel.tsx`. Storage: per-project `.refringence-console/prompts.json` via `bridge.prompts.*`, wrapped by TanStack hooks in `lib/queries/prompts.ts` (`usePrompts` 30 s stale; `useCreate/Update/Delete/ToggleFavorite` all invalidate the single list key). No project → EmptyState \"Pick a project first\" (prompts are per-project).\n\n`PromptEntry`: `{id, title, body, variables: PromptVariable[], category, tags, favorite}`. `PromptVariable`: `{name, type, label?, options?, default?}`, `type ∈ text | multiline | select`.\n\n### Header\n\n`SkillsButton` (`prompts-skills-open`) + **New prompt** (`prompt-new`).\n\n### List column (360 px)\n\n- **Search** (`prompt-search`): case-insensitive substring over `title + body + tags + category`.\n- **Filter chips**: **Favorites** toggle, one chip per distinct `category`, one per distinct `tag` (prefixed `#`). Category/tag chips are single-select toggles.\n- **List** (`prompt-list`, items `prompt-item-<id>`): each row = title + favorite star (filled when `favorite`) + category badge + first 2 tags. Selected row gets `bg-accent-subtle`. On load with nothing selected, the panel auto-selects `list[0]` (so the right pane opens on the fill form, not the empty state). Empty library vs. empty filter results show distinct copy.\n\n`selectPrompt` seeds `values` from each variable's `default`.\n\n### Detail — Operator (`PromptDetailOperator`, `prompt-detail`)\n\nDense cockpit:\n- Header: title + category + first 4 tags; action buttons **favorite** (`prompt-favorite`, `bridge.prompts.toggleFavorite`), **edit** (`prompt-edit`), **delete** (`prompt-delete`, `bridge.prompts.delete`).\n- Keyboard hints row: `↑`/`↓` navigate, `⌘C` copy filled.\n- **PromptVariableForm** (`prompt-variable-form`): one typed input per variable — `text` → Input, `multiline` → auto-growing textarea (56–180 px then scrolls), `select` → dropdown of `options`. Controlled by the parent `values` map. Live **Preview** card (`prompt-preview`) = `interpolate(body, values)`, trimmed; \"Nothing to preview yet.\" when empty.\n- **Template source** (`prompt-template-source`): raw `body` in a mono `<pre>`, plus the **variable schema** (`prompt-variable-schema`): one `{{name}} <type>` badge per variable. Guided never shows this.\n- Actions: **Copy** (`prompt-copy`, filled+trimmed to clipboard, toast) · **Send to AI** (`prompt-send-ai`) → `setAiPrefill(filled.trim())` then `navigate('/ai')` — seeds the assistant composer, does **not** auto-send. Then the **HandoffBar**.\n\n### HandoffBar (`handoff-bar`)\n\nDev-tool router gated by `bridge.devhandoff.detect()` (`{cursor, windsurf, claudeCli}`, 60 s stale). `filled` is trimmed; `root = readActiveProject().path`.\n- **Copy** (`handoff-copy`) — always, enabled when text present.\n- **Write .cursorrules** (`handoff-cursorrules`) — `writeCursorRules(root, text, 'replace')`; enabled with text + root; tooltip if Cursor/Windsurf not on PATH (\"file is still written\").\n- **Write AGENTS.md** (`handoff-agentsmd`) — `writeAgentsMd(root, text, 'replace')`; enabled with text + root (always \"detected\").\n- **Run in Claude Code** (`handoff-claude`) — `runClaude({prompt:text, cwd:root})`; enabled only when `claudeCli` detected; tooltip otherwise.\n- **Open in Cursor** (`handoff-open-cursor`) — `openInCursor(root)`; enabled with root; tooltip falls back to OS folder handler when the Cursor CLI is absent.\n\nEach button shows \"Working...\" while its action is in flight; not-detected tools keep the button and explain the gap on hover.\n\n### Detail — Guided (`PromptDetailGuided`, `prompt-detail`)\n\nCalm 3-step `Stepper` — **Pick a prompt → Fill the blanks → Copy or send** — opening on step 1 (Fill). No template source, no schema. Step 1 renders `PromptVariableForm` (or a \"no blanks\" card when `variables` is empty); step 2 shows the finished preview + **Copy** / **Send to your AI**. Back/Next bar (`prompt-guided-back`/`-next`) with a next-step hint line; `allFilled(body, values)` drives the \"All filled\" copy.\n\n### Editor (`PromptEditor`, dialog `prompt-editor`)\n\nOpened by **New prompt** or **edit**. Fields: title (`prompt-title-input`), category (default \"General\"), comma-separated tags, body (`prompt-body-input`, mono textarea). `{{name}}` references in the body (`extractVariableNames`) drive the **Variables** section: per name, a type select (Text/Multiline/Select), a label, select-options (comma-separated, when type=select), and an optional default. On save, only currently-referenced names are emitted (stale vars dropped). Save disabled until title and body are non-empty. **Create prompt** / **Save changes** via `useCreate/UpdatePrompt`.\n\n### Skills (`SkillsButton` → `SkillsDialog`, `skills-dialog`)\n\nCurated agent-skill catalogue (`bridge.skills.list`, cached `Infinity`). Tool toggle **Claude Code** (`claude`) / **Codex** (`codex`) (`skills-tool-<t>`). Installed set per `(root, tool)` from `bridge.skills.installed`. Each skill (`skill-<id>`): name, description, tag badges; **Install** (`skill-install-<id>`) → `bridge.skills.install(root, id, tool)` writes `<.claude|.codex>/skills/<id>/SKILL.md` **inside the project** (nothing global), toasts the path, invalidates the installed query; already-installed shows an \"installed\" badge. Requires an open project.\n\n### Cross-panel integration\n\nRepo's `FixButton` \"Save to library\" lands prompts here under category **Setup** with tags `['setup', <kind>]`. \"Send to AI\" (both detail variants) is the only path from a prompt into the `/ai` assistant, and it always prefills-then-waits rather than auto-firing.\n"
  },
  {
    "key": "ai-assistant",
    "title": "AI Assistant",
    "guided": "# The AI Assistant\n\nConsole has a chat assistant built in. You ask it questions in plain language; it answers, and when you have a project open, it can actually read your code before it replies. This page explains what it is, where to find it, how to turn it on, what it can and cannot do, and the one idea that makes Console's AI trustworthy: it answers on top of facts Console already measured, not instead of them.\n\n> [!INFO] What \"an LLM\" is\n> The assistant is powered by a *large language model* (LLM) - a program trained to predict text. You give it a prompt (your question plus some context), and it produces a reply one piece at a time. It is good at explaining code, drafting text, and summarising. It is not a database and it does not \"know\" your project until you give it access. That last part matters, and Console handles it for you.\n\n## Where the assistant lives\n\nThere are two doors to the same assistant:\n\n- **The chat dock** - a panel that slides in on the right-hand side of the window. Open it with the **Assistant** button in the top bar, or with the **Open assistant** action in the command palette (press `Ctrl+K`). When it opens, Console widens the window a little to make room.\n- **The assistant page** (`/ai`). In Guided mode this is a four-step wizard that walks you from \"connect a provider\" to \"read the answer.\"\n\nBoth use the same engine underneath. The dock is for quick questions while you work; the wizard is for your very first time.\n\n## Bring your own key\n\nConsole does not sell you AI access and does not have a Console account. Instead you connect your own AI provider. This is called **bring-your-own-key**.\n\n> [!INFO] What \"a provider\" and \"an API key\" are\n> A *provider* is a company that runs LLMs you can call over the internet - or a program that runs one on your own machine. An *API key* is a long secret password that provider gives you so their servers know the requests are yours (and bill you, if the provider charges). You paste that key into Console once; from then on Console uses it to talk to the provider on your behalf.\n\nConsole supports five providers:\n\n| Provider | Needs a key? | Where it runs |\n| --- | --- | --- |\n| **Anthropic** (Claude) | Yes | The provider's cloud |\n| **OpenAI** (GPT) | Yes | The provider's cloud |\n| **Google** (Gemini) | Yes | The provider's cloud |\n| **Kimi** | Yes | The provider's cloud |\n| **Ollama** | **No** | Your own computer |\n\n> [!INFO] Cloud vs. on-device\n> A *cloud* model runs on the provider's servers: you send your question over the internet, they run it and send the answer back. It is usually the smartest option, and it costs money per use. **Ollama** is different - it runs a model *on your own machine*. Nothing you type leaves your computer, and it is free, but it needs a capable computer and the models are generally smaller. If you have Ollama running, Console gives you a working chat with no key at all.\n\n### How to connect (the four-step wizard)\n\nOpen the assistant page in Guided mode and you get a stepper:\n\n1. **Connect your AI** - pick a provider. For a cloud provider, paste your key into the field and press **Save key**. Console checks the key against the provider before saving it, so a typo is caught immediately. For Ollama, press **Detect Ollama**; if it is running, you will see how many local models you have.\n2. **What to do** - type what you want in plain words, or start from one of your saved prompt templates.\n3. **Review** - Console shows you the exact text that will be sent. Step back if you want to change it.\n4. **Send** - the answer streams in, word by word. You can **Ask again** or **Stop**.\n\nYou only do this setup once. After that, just open the dock and type.\n\n## Where your keys are stored\n\nThis is worth understanding, because it is the question people worry about most.\n\n> [!INFO] Why this is a security decision\n> An API key is a live secret - anyone who has it can spend your money or read on your behalf. So *where* it is kept, and *who* can read it back, is the whole game. A careless app stores keys in plain text where any script can grab them. Console does not.\n\nWhen you save a key, here is what happens:\n\n- The key is handed to the **main process** (the trusted, behind-the-scenes part of the Console app), never kept in the visible web page.\n- It is **encrypted** using your operating system's own secure storage - the Windows Credential Manager, the macOS Keychain, or the Linux secret service - and written to a file only your user account can open.\n- The key is **never shown back to you**, **never written to a log**, and **never sent to the AI**. Console reads it back only in that trusted main process, only to make the actual call to the provider.\n- The visible part of the app only ever learns one thing about your key: a yes/no **\"Connected\"** badge. The secret itself never travels back into the page.\n\nSo if you ever want to confirm a key is set, you look for the **Connected** badge in Settings - you will never see the key again, and that is by design. To remove a key, press **Clear**; the encrypted file is deleted.\n\n> [!INFO] What \"the model picker\" shows you\n> Once a key is connected, the **model picker** (top of the chat) lists the specific models you can use - grouped by provider, each with its *context window* (how much text it can consider at once, like \"200k\") and a one-line note on what it is good for. Console only shows models your stored keys can actually reach. If you have only an OpenAI key, you will not see Claude or Gemini in the list, so you can never pick a model that would fail. A model named like \"Claude Sonnet 4.5\" or \"GPT-4\" is just one specific model offered by that provider.\n\n## What the assistant *can* do\n\nWhen you have a project open in Console, the assistant is not guessing about your code - it can read it. Console gives the model three **read-only tools**:\n\n- **List files** - see what files and folders are in a directory.\n- **Read file** - open a specific file and read its contents (up to about 400 lines at a time).\n- **Search code** - find a piece of text across the whole project.\n\n> [!INFO] What \"tools\" mean here, and what you'll see\n> A modern assistant can do more than reply with text - it can *call tools*: small actions the app exposes to it. When the assistant decides it needs to read `package.json` before answering, it calls the **read file** tool, and you see a little card in the chat showing the tool name and the file, with a spinner that turns into a checkmark when it finishes. This is the assistant *checking the real code* instead of making something up. Watching those cards is how you can tell its answer is grounded in your actual project.\n\nConsole also quietly tells the assistant which project you have open - its name, its description, the scripts it can run, and its top-level folders - so its very first answer already has its bearings.\n\nGood things to ask:\n\n- \"Explain what this repo does.\"\n- \"Where is the login handled in this code?\"\n- \"Draft a release note from the latest commits.\"\n- \"Why might this build be failing?\"\n\n## What the assistant *cannot* do\n\nKnowing the limits keeps you out of trouble:\n\n- **It cannot change your files.** The tools are *read-only*. The assistant can look but never edit, delete, or run anything in your project. (If you want AI that edits code, Console can hand a prompt off to a separate coding tool like Cursor or Claude Code - but that is a deliberate, separate action you take, not something the chat does on its own.)\n- **It cannot read outside your project.** The file tools are locked to the folder you opened. The assistant cannot wander up into the rest of your disk.\n- **It does not remember past conversations.** Right now the chat keeps **one conversation in memory** while the panel is open. Press **New chat** (or close the app) and the history is gone - there is no saved session yet. So if a thread matters, copy the useful part out.\n- **It never sends without you.** When you send a prompt from the Prompts library to the assistant, it lands in the message box ready for you to read - it is *not* fired automatically. You always press Send.\n- **It cannot see your keys** - the secret never reaches the model, only your messages and whatever code it reads with the tools do.\n\n## The important idea: AI sits *on top of* facts\n\nThis is the heart of how Console is designed, and it is different from a plain chatbot.\n\n> [!INFO] What \"deterministic\" means\n> *Deterministic* means \"the same input always gives the same output, with no guessing.\" When Console counts your lines of code, lists your dependencies, scans for exposed secrets, reads your CI status, or maps which package imports which - those are deterministic measurements. They are facts, repeatable and exact. An LLM is the opposite: ask it twice and you may get two different phrasings, and it can be confidently wrong.\n\nConsole measures first and lets AI talk second:\n\n- The **Report**, **Repo**, **Architecture**, **Pipeline**, and **Observability** panels are built from real measurements of your project - no AI required. The numbers are true whether or not you ever connect a model.\n- AI is then layered *over* those facts as an optional enrichment: a plain-English summary in the Report, a semantic map in Architecture, an explanation in the chat. The AI explains and narrates; the deterministic scan supplies the ground truth.\n- And when you ask the chat about your project, it does not invent answers - it uses the read tools to go look at the real files first.\n\n> [!INFO] Why this order matters to you as a beginner\n> If a tool *only* had a chatbot, you could never be sure whether an answer was measured or imagined. Console flips it: trust the panels' numbers as facts, and treat the AI as a knowledgeable colleague who read those same files and is explaining them to you. When the AI says something surprising, you can always go check it against the deterministic panel that measured it. That habit - verify the story against the facts - is exactly how experienced engineers work.\n\n## If you see \"Connect an AI provider\"\n\nThat empty state means no model is available yet - either no key is connected, or Ollama is not running. Press **Open Settings**, add a provider key (or start Ollama), and come back. The chat lights up as soon as one usable model exists.\n\n## Quick recap\n\n- Open it from the **Assistant** button in the top bar, or `Ctrl+K` → Open assistant.\n- Connect your own provider once; the key is **encrypted in your OS keychain** and never shown back.\n- With a project open, the assistant **reads your real code** (read-only) before answering, and shows you the tool cards while it does.\n- It does **not** edit files, remember old chats, or send anything without you pressing Send.\n- Treat the panels' measurements as facts and the AI as the colleague who explains them.",
    "operator": "# AI Assistant\n\nMulti-provider chat assistant, bring-your-own-key, with read-only project grounding. Same backend, two front doors: the right-hand **chat dock** (`ConsoleChatDock` → `ChatPanel embedded`) and the `/ai` route (`AiPanel` dispatches on persona: Operator → `ChatPanel`, Guided → `AiWizard`). Keys are held by the Electron main process; only booleans, redacted status, and model metadata cross the bridge.\n\n## Surfaces & entry points\n\n| Surface | Open via | Notes |\n| --- | --- | --- |\n| Chat dock (360px right column) | TopBar **Assistant** toggle (`topbar-ai`) · palette **Open assistant** | `consoleLayout.chatOpen` owns state; opening also calls `bridge.window.growForDock(360)`. Header \"Assistant\" + close (`console-chat-dock-close`). Embeds `<ChatPanel embedded/>`. |\n| `/ai` Operator | direct nav · prefill handoff from Prompts | `ChatPanel` (non-embedded): full `PanelHeader` \"Assistant\" / \"Chat with your AI provider\". |\n| `/ai` Guided | persona = Guided | `AiWizard`: 4-step stepper (Connect → Describe → Review → Send). |\n\nEmbedded vs. full: `embedded` drops the PanelHeader title row and renders model picker + New-chat in an 11px controls strip so the dock shows one title, not two.\n\n## Providers\n\n`AiProviderId = 'openai' | 'anthropic' | 'google' | 'ollama' | 'kimi'`. Registry display order (`ai/registry.ts ORDER`): `anthropic, openai, google, kimi, ollama` (cloud defaults first, local last).\n\n| Provider | Keyed | Credential check | Models |\n| --- | --- | --- | --- |\n| Anthropic | yes | `hasKey('anthropic')` | static: `claude-sonnet-4-5` (200k), `claude-opus-4-1` (200k), `claude-haiku-4-5` (200k) |\n| OpenAI | yes | `hasKey('openai')` | static catalogue |\n| Google | yes | `hasKey('google')` | static catalogue |\n| Kimi | yes | `hasKey('kimi')` | static catalogue |\n| Ollama | **no** | live probe (`localhost:11434`) | live local tags |\n\n`KEYED_PROVIDERS = ['openai','anthropic','google','kimi']`. Ollama is local + keyless and is not part of the keystore surface; a running Ollama yields a usable chat with no key.\n\n## Model picker (`ModelPicker.tsx`, `ai-model-picker`)\n\nDropdown grouped by provider (`groupModelsByProvider`, order = first model seen per provider). Per item: label + context window rendered as `Math.round(context/1000)k` (e.g. \"200k\") + one-line `description`. Trigger label = current model label, truncated 180px; disabled when 0 models.\n\n- **Source of truth = available models, not raw keys.** `useAiModels` → `bridge.ai.availableModels()` → `registry.listAvailableModels()`, which contributes only providers passing `hasCredentials()`, each yielding its `listAvailableModels?.()` (catalogue ∩ key-reachable) or `listModels()`, or live local tags. A user with only an OpenAI key never sees Claude/Gemini entries that would fail on send.\n- `bridge.ai.listModels()` / `registry.listAllModels()` = full catalogue (static cloud lists + live Ollama), used internally to resolve a model id → provider; not what the picker shows.\n- `ChatPanel` defaults `modelId` to `list[0].id` once models arrive and re-pins to a valid selection if the list changes.\n\n## Key storage (where keys live)\n\nMain process only. `ai/keystore.ts`:\n\n- One encrypted file per provider at `<userData>/ai-keys/<id>.key` via Electron `safeStorage` (OS keychain — Windows DPAPI / macOS Keychain / Linux secret service). `chmod 0o600` best-effort (no-op on Windows ACLs).\n- `setKey` returns `false` (never throws) when `safeStorage.isEncryptionAvailable()` is false — surfaces \"Secure storage is unavailable… key was not saved\" rather than writing plaintext.\n- `getKey` is **main-process internal only** — never returned over IPC. Raw key never logged.\n- The same encrypted store also holds non-AI service keys by string id (e.g. `langsmith`); the typed `KEYED_PROVIDERS` surface stays AI-only.\n\nIPC (`ipc/ai.ts`):\n\n- `console:ai.setKey` — **validate-before-store**: `validateKey(id, key)` probes the provider; on `valid:false` returns `{ok:false, valid:false, error}` and **never lands the key on disk**. On success encrypts + stores. Returns `{ok, valid?, error?}` — never the key.\n- `console:ai.getKeyStatus` → `keyStatus()` = `Record<provider, boolean>` (`hasKey` per `KEYED_PROVIDERS`).\n- `console:ai.clearKey` — deletes the file; total (never throws).\n\nSettings → AI write surface: masked write-only `<input>` per `CLOUD_PROVIDERS` (OpenAI `sk-…`, Anthropic `sk-ant-…`, Google `AIza…`), Set/Replace/Clear, Connected badge driven by `useAiKeyStatus`. **Legacy migration** (`useAiKeyMigration`, one-shot, flag `refringence-console-ai-keys-migrated`): moves localStorage `refringence-console-anthropic-key` into the keystore via `bridge.ai.setKey('anthropic',…)`, then removes the legacy key.\n\n## Tier (separate from the chat backend)\n\nSettings → AI **Tier** radios write localStorage `refringence-console-ai-tier`: `on-device` (default — all AI local via Ollama, no data leaves machine) / `on-device-optional-cloud` / `cloud-first`. This drives the **on-device capability layer** (`lib/ai/registry.ts`: Transformers.js BGE-small embeddings in a worker; rule-based `suggestNext`/`explain`/`categorize`; cloud refinement stubbed \"T4 not wired\"), collapsed internally to `on-device-only | allow-cloud`. **The chat Assistant does not consult the tier** — `ChatPanel` gates purely on `availableModels` and routes by model id through the main-process providers regardless of tier.\n\n## Streaming chat (`lib/ai/useAiChat.ts`)\n\n`bridge.ai.chat.start({ model, messages, system?, projectRoot? })` returns `{chatId}` synchronously; renderer subscribes once to `onDelta / onToolCall / onToolResult / onDone / onError`, filtering by `activeChatId`. Surface: `{ messages, streaming, error, send, stop, reset }`.\n\n- **History across turns = role + text only**; within-turn tool detail (`ChatToolUse`) stays local to the message.\n- `stop` → `bridge.ai.chat.cancel(chatId)` (main aborts via per-chat `AbortController`).\n- `reset` (**New chat**, `ai-new-chat`) cancels any live chat + clears messages.\n- One conversation in component state. **No session persistence.**\n- Main side (`ipc/ai.ts`): resolves model→provider via `listAllModels`; unknown model → error+done. One `AbortController` per chat in a `chats` map; every handler total (never throws across IPC).\n\n## Project grounding (tools + context)\n\nPassed `projectRoot` (= `useActiveProject().project.path`) ⇒ assistant gets read-only tools + an injected context block. `ipc/ai.ts` builds them only when `projectRoot` is a non-empty string; failure falls back to plain chat. System prompt = `buildProjectContext(root)` prepended to any caller `system`.\n\n**Tools** (`ai/tools.ts`, JSON-schema inputs, all scoped to root with `inside()` traversal guard — cannot read outside the project):\n\n| Tool | Input | Behaviour |\n| --- | --- | --- |\n| `list_files` | `dir` (\"\" = root) | dir listing, hides dotfiles (except `.github`) + SKIP set (`node_modules`, `.git`, `dist`, `build`, `.next`, `out`, `.cache`, `.venv`, `__pycache__`, `.refringence-qa`, `.refringence-console`), sorted, capped 200 |\n| `read_file` | `path` | text file, **>200KB rejected**, returns ≤400 lines (then \"… N more lines\") |\n| `search_code` | `query` | literal `git grep -F` (respects .gitignore, 10s/1MB cap); falls back to bounded JS walk (≤60 hits, ≤3000 files scanned) on known source extensions |\n\n**Context block** (`buildProjectContext`): \"You are Console's assistant…\", project root, a `package.json` shape line (name/description/script names) or \"No package.json…\", top-level non-dot/non-SKIP folders (≤20), and an instruction to use the tools to ground answers before responding. Tool cards in the transcript (`ToolCard`) icon by name: `read_file`→FileText, `list_files`→FolderOpen, `search_code`→Search, else Wrench; show the `path`/`dir`/`query` arg + spinner→check.\n\n## Transcript & composer\n\n- **Transcript** (`ChatMessages.tsx`, `ai-transcript`): user = right-aligned card; assistant = markdown (`marked`, `{gfm:true, breaks:true}`) with accent-dot gutter; tool cards above text. Stick-to-bottom (re-pins when within 64px of bottom), \"Scroll to latest\" pill when scrolled away, \"Thinking\" indicator (pulsing accent dot) while streaming with no assistant turn yet.\n- **Composer** (`ChatComposer.tsx`, `ai-composer`): auto-grow textarea (min 40px, max 160px). **Enter sends, Shift+Enter newline.** Send (`ai-send`) disabled until non-empty + enabled; while streaming shows Stop (`ai-stop`). `disabled` mirrors `!modelId`.\n- Errors render a `role=\"alert\"` danger banner above the composer.\n- **Empty states**: no models → `EmptyState` \"Connect an AI provider\" → **Open Settings** (`ai-empty-settings-link`); models present but no messages → \"Ask anything\".\n\n## Prefill handoff (`lib/ai/prefill.ts`)\n\nPrompts panel **Send to AI** stashes text in `sessionStorage` (`refringence-console-ai-prefill`), navigates to `/ai`; `ChatPanel` reads-and-clears it once into the composer `initialText`. **Never auto-fires** — the user reviews and presses Send.\n\n## Guided wizard (`AiWizard.tsx`, `ai-wizard`)\n\nSteps: **Connect** (cloud key field per `openai`/`anthropic`, or Ollama detect; Next gated on `connected`) → **Describe** (free text or pick a saved prompt template, defaults interpolated; Next gated on non-empty) → **Review** (exact send text) → **Send** (auto-fires `chat.send(task)` once on entering step 3; **Stop** / **Ask again**). Same `useAiChat` backend, no `projectRoot` passed in the wizard path.\n\n## Layering AI over deterministic facts (design contract)\n\n- **Deterministic-first.** Report (`intel.profile`), Repo, Architecture (`arch.graph`), Pipeline, Observability, dependency/secrets/hygiene scans are exact measurements with no model call. They hold whether or not a provider is connected.\n- **AI as optional enrichment over those facts**, never the source of truth: Report `onEnrich` (`intel.enrich`) adds tagline/narrative/one-liners on top of the deterministic profile; Architecture **Systems view** generates a semantic map but validates paths against the real repo before they open code; the chat grounds via read tools rather than recall.\n- **Boundaries enforced in code**: read-only tools (no write/edit/run), root-scoped traversal guard, history limited to role+text, prefill never auto-sends, keys never reach the model or the renderer. Overview explicitly forbids a chatbot widget (deterministic `WhatsNextTile` instead).\n\n## Bridge surface (`ai.*`)\n\n`providers()` · `listModels()` · `availableModels()` · `getKeyStatus()` · `setKey(id,key)` · `clearKey(id)` · `chat.start(opts)` · `chat.cancel(chatId)` · `chat.onDelta/onToolCall/onToolResult/onDone/onError`. Browser-dev fallback: all stubs throw; event subscriptions are no-op unsubscribers.\n\n## Limits / not-yet-wired\n\n- No session persistence (one in-memory conversation; New chat / reload discards).\n- Chat path ignores Tier; Tier only drives the on-device registry layer (cloud refinement stubbed).\n- Tools are read-only — code edits route out via `devhandoff.*` (Run in Claude Code / Open in Cursor), a separate explicit action, not the chat.\n- `safeStorage` unavailable ⇒ keys cannot be saved (honest failure, no plaintext fallback)."
  },
  {
    "key": "settings-and-shortcuts",
    "title": "Settings and shortcuts",
    "guided": "# Settings and shortcuts\n\nThis page covers the two ways you control Console: the **Settings** screen (how Console looks and behaves, where AI runs, what services it talks to) and the **keyboard shortcuts** that let you move around without touching the mouse.\n\nYou do not have to learn any of this to use Console. But spending five minutes here will make the rest of the app feel faster and more like *yours*.\n\n> [!INFO] **What \"settings\" actually are.** Almost every setting on this page is just a small piece of remembered preference. Console writes your choices into a tiny local store on your machine (the browser's `localStorage`, plus an encrypted vault for secrets) so they survive a restart. Nothing here is sent to a server. Changing a setting only changes how *your* copy of Console behaves.\n\n---\n\n## Opening Settings\n\nThere are three ways to get there:\n\n1. Click **Settings** at the bottom of the left sidebar.\n2. Press **`g`** then **`,`** (the comma key) anywhere in the app.\n3. Press **`Ctrl + K`**, type \"settings\", and press **Enter**.\n\nIn **Guided mode** (where you probably are if you're reading this), Settings is deliberately short: just **Theme**, **AI mode**, and **About**. Everything else is one click away behind **See full settings**, which switches you to Operator mode and reveals the complete panel. You can switch back any time.\n\n---\n\n## Display: making Console look right\n\n### Theme — light, dark, or match your system\n\nPick how Console looks. **Light** is bright, **Dark** is gentler on the eyes at night, and **Match my system** follows whatever your operating system is set to (so Console goes dark when Windows goes dark in the evening, if you've set that up).\n\nThere is also a quick toggle in the top bar — the small **sun/moon button** in the top-right corner flips between light and dark instantly. The top-bar button only flips light↔dark; the **Match my system** option lives only here in Settings.\n\n> [!INFO] **Why \"match my system\" is usually the right pick.** Your OS already knows whether it's day or night for you (many systems switch automatically). \"Match my system\" means you set it up once and never think about it again. This pattern — an app deferring to a system-wide preference instead of inventing its own — is good engineering practice: fewer places to configure, fewer surprises.\n\n### AI mode — where the AI runs\n\nThis is the most important privacy choice in Console. It decides **where your code goes when you ask the AI a question.** Three options:\n\n- **On-device only** — every AI request runs on your own laptop using a local AI runtime (Ollama). Nothing about your code ever leaves the machine. This is the default and the most private.\n- **On-device with cloud help** — runs locally by default, but asks before sending anything to a cloud provider for harder questions.\n- **Cloud first** — uses a cloud AI provider for every request. Faster and smarter, but it uses your API key and sends your prompt to that provider.\n\n> [!INFO] **\"On-device\" vs \"cloud\" — what's the difference?** An AI model is a large program that has to run *somewhere*. \"On-device\" means that program runs on your computer's own processor — private, free, but limited by your hardware. \"Cloud\" means your question is sent over the internet to a company's servers (OpenAI, Anthropic, Google) which run a much bigger model and send the answer back. The trade-off is always the same: **privacy and cost vs. capability.** Console lets you choose where you sit on that line, per machine.\n\n### About\n\nJust shows what you're running: the app name (**Console**) and the version (**v0.1**). Useful when reporting a problem so you can say which version you're on.\n\n### See full settings\n\nThe link at the bottom of the About card flips you into **Operator mode**, which unlocks the full Settings screen described below (density, AI keys, sidebar layout, project folder, services). Don't worry — switching modes doesn't lose anything; it just shows more controls. You can flip back with the **mode pill** in the top bar.\n\n---\n\n## The full Settings screen (Operator mode)\n\nWhen you switch to the full view, Settings grows a left-hand menu with five sections: **Display, AI, Sidebar, Project, Services.** Here's each one.\n\n### Display (full)\n\nSame **Theme** as above, plus two more controls:\n\n- **Mode** — switch between **Guided** (this teaching view, with explainers and roomier spacing) and **Operator** (a dense, no-explanations cockpit for experienced engineers). This is the same switch as the **mode pill** in the top bar.\n- **Density** — **Compact** packs more onto the screen; **Roomy** adds breathing room. Guided mode defaults to Roomy; Operator defaults to Compact. Purely cosmetic — it changes spacing, not what's shown.\n\n### AI (full)\n\nThis is where you set up cloud AI if you chose a cloud tier. Three parts:\n\n**1. Tier** — the same three choices as \"AI mode\" above (On-device only / On-device with optional cloud / Cloud-first), just with more technical wording.\n\n**2. Provider keys** — paste an API key for **OpenAI**, **Anthropic**, or **Google** to let Console use that provider's models.\n\n> [!INFO] **What is an \"API key\"?** When you sign up with an AI provider, they give you a long secret string — your API key — that identifies your account and bills you for usage. It's like a password for a robot: anything that has your key can spend your money, so it must be kept secret. Console handles this carefully: when you paste a key, it goes straight to the part of Console that runs on your machine (the \"main process\") and is stored in your operating system's **encrypted keychain**. The key is *never shown back to you* and *never sent to the AI*. The screen only ever tells you \"Connected\" or \"Not connected\" — a yes/no, never the secret itself. This is the correct way to handle secrets, and it's worth recognising so you can demand it from every tool you use.\n\nEach provider row has a masked input (the characters show as dots), a **Set** button (which becomes **Replace** once a key is saved), and a **Clear** button to remove it. When you save a key, Console quickly checks it's valid before storing it.\n\n**3. Ollama** — for the on-device tiers. Press **Detect Ollama** and Console checks whether the local AI runtime is installed and running (on `localhost:11434`). If found, it shows the version and how many models you have installed. If not, it links you to **ollama.com** to install it.\n\n> [!INFO] **What is Ollama?** Ollama is a free program that runs open AI models directly on your computer. Console talks to it on a local network address (`localhost:11434` — `localhost` always means \"this same machine,\" and `11434` is the port number Ollama listens on). Because it's local, using it costs nothing and your code never leaves your laptop — which is exactly why it's the default for the privacy-first tiers.\n\n**4. Eval tools — LangSmith.** Optional. Paste a LangSmith key (starts with `lsv2_`) and Console can run a small test of your AI setup and record the results to your LangSmith account. Once connected, a **Run eval** button appears.\n\n> [!INFO] **What is an \"eval\"?** An eval (short for *evaluation*) is a test for AI. Normal software tests check exact outputs — 2 + 2 must equal 4. But AI answers vary, so you can't check them exactly; instead you run the AI over a set of example questions and *score* how good the answers are. **LangSmith** is a service that records each AI run so you can see what was asked, what came back, and how it scored. Connecting it here lets Console run one of these scored tests for you. You'll meet evals again in the Observability panel.\n\n### Sidebar — rearranging the left menu\n\nThis lets you make the left sidebar yours. Drag any row by its handle to **reorder** it, and click the **eye icon** to **hide** a panel you never use.\n\nThree panels — **Overview, Release, and Pipeline** — show a small **lock** instead of an eye. These are core to shipping, so Console keeps them always visible.\n\n> [!INFO] **Hidden doesn't mean gone.** Hiding a panel only removes it from the sidebar to reduce clutter. Every panel is *always* reachable from the command palette (`Ctrl + K`) — so you can hide things aggressively and still get to them in two keystrokes. This is a common UI idea: keep the always-visible surface small, but let power-users reach anything instantly.\n\n**Reset to default** puts the sidebar back the way it started.\n\n### Project — the folder Console is reading\n\nConsole always works on **one active project folder** at a time. This section shows that folder's path and lets you **Pick a folder** / **Change folder** / **Clear** it.\n\n> [!INFO] **What \"the active project\" means.** Console doesn't have a database of your code; it reads a real folder on your disk live. The \"active project\" is simply *which folder* it's pointing at right now. Everything you see — the architecture map, the health score, the file list — is computed by reading that folder. Change the folder and the whole app re-reads. (You can also open a *different* project in a brand-new window from the project chip in the top bar, so you can have several open at once.)\n\n### Services — connecting GitHub, Vercel, and more\n\nThis section is a shortcut. The actual connecting happens in the dedicated **Services** panel — click **Open Services** to go there. Because connecting accounts is a privacy-sensitive thing, it's worth understanding what happens:\n\n- **GitHub** — Console reuses the login you already have through the GitHub command-line tool (`gh`). No password or token to paste.\n- **Vercel** — paste a personal token (from *vercel.com/account/tokens*); Console can then deploy your project. The token is encrypted on your machine.\n- **Sentry** — paste a token and your organisation name to pull live production errors into the app.\n- **Slack** — paste a bot token (starts with `xoxb-`) to pull issue reports from your Slack channels.\n\n> [!INFO] **How Console keeps your tokens safe.** A connection token is another secret — like an API key, it lets Console act on your behalf. Every token you paste is encrypted and stored in your operating system's secure keychain, and like AI keys, **it is never sent back to the screen and never sent to the AI.** Console only ever reports a yes/no \"Connected\" status. There's also a neat trick on the Services panel: it can scan your project's `.env` file for service *names* (never the secret values) and offer to connect them; when you tap Link, the value is read just long enough to test the connection, then dropped.\n\n---\n\n## Keyboard shortcuts\n\nConsole is built so you can fly through it with the keyboard. You don't need these — every shortcut has a button somewhere — but they're worth learning because they make the app feel instant.\n\n> [!INFO] **Why \"g then a letter\"?** Console borrows a navigation style from professional tools like Linear and Gmail: you press **`g`** (think \"**go**\") to start, then a second letter to choose a destination — `g o` = \"go to Overview.\" It's called a **chord** (two keys pressed in sequence). The advantage over a single hotkey is that you don't run out of letters: every panel gets a memorable two-key combo without clashing with normal typing.\n\n### Jump to any panel — press `g`, then:\n\n| Press | Goes to |\n|-------|---------|\n| `g` `o` | Overview |\n| `g` `w` | Workboard |\n| `g` `r` | Repo |\n| `g` `a` | Architecture |\n| `g` `p` | Pipeline |\n| `g` `s` | Services |\n| `g` `b` | Library |\n| `g` `l` | Release |\n| `g` `y` | Observability |\n| `g` `c` | Activity |\n| `g` `t` | Tutorials |\n| `g` `d` | Docs |\n| `g` `,` | Settings |\n\nIf you press `g` and then change your mind, just wait — the chord forgets itself after a moment and nothing happens.\n\n### The two shortcuts to actually remember\n\n| Press | Does |\n|-------|------|\n| **`Ctrl + K`** | Opens the **command palette** — a search box for *everything*: jump to a panel, run an action (toggle theme, connect a service, deploy, run an eval), or open a doc. If you only learn one shortcut, learn this. |\n| **`?`** | Opens the **keyboard shortcuts help** — a pop-up listing every chord, so you never have to memorise this page. |\n\n> [!INFO] **What a \"command palette\" is, and why every serious app has one.** A command palette is one search box that can run any command in the app, found by typing what you want. It exists because menus don't scale — once an app has dozens of features, hunting through nested menus is slow. Typing \"deploy\" and pressing Enter is faster than remembering which panel the deploy button lives in. Console's palette even re-ranks results by *meaning*, not just spelling, so a rough guess usually finds the right thing.\n\n### In the AI assistant chat\n\n| Press | Does |\n|-------|------|\n| **`Enter`** | Sends your message |\n| **`Shift + Enter`** | Adds a new line *without* sending (for writing a longer, multi-line question) |\n\n### When shortcuts politely step aside\n\nThe `g`-chords and `?` **don't fire while you're typing** in a text box, or while the command palette is open. That's on purpose — you should be able to type the letter \"g\" into a search field or a chat message without Console teleporting you somewhere. `Ctrl + K` always works, because no one types `Ctrl + K` by accident.\n\n---\n\n## A quick tour of the layout presets\n\nOne more thing worth knowing, reachable from the command palette (`Ctrl + K` → type \"layout\"): Console has three **layout presets** that rearrange the whole window in one click.\n\n- **Standard** — full sidebar, no chat. The calm default.\n- **Focus** — sidebar shrinks to a thin icon rail so your content fills the screen.\n- **Chat** — icon rail plus the AI assistant docked on the right, for working alongside the AI.\n\n> [!INFO] **Why presets instead of fiddly toggles.** Rather than make you adjust the sidebar and the chat dock separately every time, presets bundle \"the way I like to work right now\" into one choice. It's the same idea as a \"reading mode\" in a browser: a named bundle of settings you switch between, instead of ten individual knobs. You can still toggle the rail or the chat individually (`Ctrl + K` → \"toggle sidebar rail\" / \"open assistant\") when you want fine control.",
    "operator": "# Settings and shortcuts\n\nComplete reference for the Settings panel (every section, field, and storage key) and the global keyboard map. Settings branches on persona at the top: Guided renders a trimmed three-card view (`SettingsNewbie`); Operator renders the full five-section panel (`SettingsSeasoned`). The mode pill in the top bar and the **Mode** field in Display both write the same persona key — switching is non-destructive.\n\nTwo surfaces also live outside the Settings panel and are documented here for completeness: the **theme toggle** in the top bar, and **service connections** (which the Settings → Services section only links to; the real controls are in the Services panel).\n\n---\n\n## Reaching Settings\n\n- Sidebar → **Settings** (Help group; always shown, never hideable from the rail).\n- Chord **`g` `,`** (global, outside text inputs / palette).\n- Palette (`Ctrl+K`) → \"Settings\" panel item.\n\nRoute: `/settings`. Root testid: `settings-panel` (Operator) / `settings-newbie` (Guided).\n\n---\n\n## Guided settings (`SettingsNewbie`)\n\nThree cards, no left nav. Centered, max-width 820px.\n\n| Card | Control | Values | Storage |\n|------|---------|--------|---------|\n| **Theme** (`newbie-settings-theme`) | native `<select>` | Light / Dark / **Match my system** | `refringence-console-theme` |\n| **AI mode** (`newbie-settings-ai`) | radio group | On-device only / On-device with cloud help / Cloud first | `refringence-console-ai-tier` |\n| **About** (`newbie-settings-about`) | read-only + link | App: Console · Version: v0.1 · **See full settings** (`newbie-settings-see-more`) → sets persona `seasoned` | — |\n\nTheme writes apply immediately: toggles `.dark` on `<html>` (resolving `system` via `prefers-color-scheme`) and broadcasts `console-theme-change`. AI mode writes the tier key only (no side effects). On mount, both Guided and Operator AI sections run a one-shot legacy migration: any `refringence-console-anthropic-key` in localStorage is pushed into the keystore via `bridge.ai.setKey('anthropic', …)`, then the localStorage key is removed and `refringence-console-ai-keys-migrated=1` is set so it runs once.\n\nGuided settings deliberately omit Density, Provider keys, Ollama, Eval tools, Sidebar editor, Project folder, and Services. \"See full settings\" flips to Operator to expose them.\n\n---\n\n## Operator settings (`SettingsSeasoned`)\n\nLeft nav (`w-40 sm:w-48`) with five sections, content pane on the right. Sections (`SECTIONS`): **Display**, **AI**, **Sidebar**, **Project**, **Services**. Default active: `display`.\n\n### Display section\n\nCard 1 — three `SegmentedGroup` fields:\n\n| Field | Options (value → label) | Storage / effect |\n|-------|-------------------------|------------------|\n| **Theme** | `light`→Light, `dark`→Dark, `system`→System | `refringence-console-theme`. `writeTheme()` sets key, toggles `.dark` via `resolveDark()`, dispatches `console-theme-change`. A `system` selection installs a `matchMedia('(prefers-color-scheme: dark)')` change listener that re-toggles `.dark`. |\n| **Mode** | `newbie`→Guided, `seasoned`→Operator | `refringence-console-persona` via `writePersona()`. Same key the top-bar mode pill writes; broadcasts `console-persona-change`. |\n| **Density** | `compact`→Compact, `roomy`→Roomy | `refringence-console-density` via `writeDensity()`. Initial value: existing key if set, else `roomy` when persona=`newbie`, else `compact`. Persisted on first mount so other panels read a stable value. |\n\nCard 2 — **About**: App = `Console`, Version = `v0.1` (static).\n\n> Note: the top-bar `topbar-theme-toggle` only cycles light↔dark and persists `refringence-console-theme` as `light`/`dark` — it cannot select `system`. The `system` option is reachable only from Settings (Operator or Guided).\n\n### AI section\n\nThree `Field`s plus the eval tools row.\n\n**Tier** — radio group, key `refringence-console-ai-tier`. Three tiers:\n\n| id | Title | Body |\n|----|-------|------|\n| `on-device` | On-device only | All AI runs locally via Ollama. No data leaves the machine. **Default.** |\n| `on-device-optional-cloud` | On-device with optional cloud | Local first; cloud calls allowed when you opt in per task. |\n| `cloud-first` | Cloud-first | Cloud provider for every request. Requires an API key. |\n\n**Provider keys** — one `ProviderKeyRow` per cloud provider:\n\n| Provider id | Name | Placeholder |\n|-------------|------|-------------|\n| `openai` | OpenAI | `sk-...` |\n| `anthropic` | Anthropic | `sk-ant-...` |\n| `google` | Google | `AIza...` |\n\nEach row: masked input (`type=password`, eye toggle to reveal), **Set**/**Replace** button (label flips on connected state), **Clear** button (connected only). Save path: `bridge.ai.setKey(id, key)` → on `{ok:false}` or `{valid:false}` shows the returned error, else clears the input and invalidates key status. Clear: `bridge.ai.clearKey(id)`. Connected badge driven by `bridge.ai.getKeyStatus()` (boolean per provider) via `useAiKeyStatus()`. **The raw key never crosses back to the renderer** — input is write-only; status is boolean only; keys live in the OS keychain (main process). Hint text states this verbatim. Note: only OpenAI/Anthropic/Google get key fields here; Ollama needs none and Kimi has no Settings field (both still selectable as chat providers elsewhere via available-models gating).\n\n**Ollama** — **Detect Ollama** button → `useOllamaDetect().refetch()`. On `data.running`: success badge \"Ollama vX, N model(s) installed\". On not running: muted note \"Ollama not detected on localhost:11434\" + link to `https://ollama.com` (via `bridge.openExternal`, window.open fallback). On error: \"Detection failed.\"\n\n**Eval tools — LangSmith** (`EvalToolsRow`):\n- Status via `useLangsmithStatus()` → Connected / Not connected badge.\n- Masked input (placeholder `lsv2_...` or \"Replace key\" when connected) + eye toggle. **Set**/**Replace** → `bridge.evals.setLangsmithKey(key)`; on `{ok:false}` or `{valid:false}` shows error, else clears + invalidates. **Clear** (connected only) → `bridge.evals.clearLangsmithKey()`.\n- When connected: **Run eval** button (`langsmith-run-eval`) → `useRunEval().mutate()`. Runs a small fixed dev dataset through the user's OpenAI model, traced + scored to their LangSmith account. Result renders `passed/total passed` + \"view in LangSmith\" link, or the error string.\n\n### Sidebar section (`SidebarEditor`)\n\nDrag-reorder + show/hide editor over the canonical `GROUPS` item set. Writes through `useSidebarLayout` (`refringence-console-sidebar-layout-v1`, shape `{hidden, order, collapsedGroups}`); rail updates live.\n\n- Rows built from `GROUPS.flatMap` after `orderItems(items, layout.order)`. Each row: grip handle (`@dnd-kit` `useSortable`, 4px activation distance), icon, label + group subtext, and a trailing control.\n- Trailing control: **eye / eye-off** toggle (`setHidden(to, …)`) for hideable panels; a **lock** glyph + \"Always visible\" tooltip for `LOCKED` panels = **Overview, Release, Pipeline** (never hideable).\n- Drag end → `arrayMove` → `setOrder(ids)`.\n- **Reset to default** (`sidebar-editor-reset`) → `reset()`.\n- Testids: root `sidebar-editor`; per-row `sidebar-editor-row-<label>`, `sidebar-editor-toggle-<label>`, `sidebar-editor-lock-<label>`.\n- Note: every panel remains reachable from the palette even when hidden; this only affects the rail.\n\n### Project section\n\n- Active project path rendered in a `<code>` (`settings-project-path`), truncated, `title`=full path or \"No active project\".\n- **Pick a folder** / **Change folder** (`settings-project-pick`) → `bridge.project.pickFolder()`; on `{canceled:false, path}` → `setProject(path)` via `useActiveProject`.\n- **Clear** (when a project is set) → `clear()`.\n- If `project.pickedAt` set: \"Picked <localeString>.\" line.\n- Active project is window-scoped (`?wid=`); recent list (cap 6) is global. (Multi-project: use the top-bar chip → \"New project in a new window\".)\n\n### Services section\n\nStatic link-out only — no connection controls live here. Renders a description + **Open Services** button (`<Link to=\"/services\">`). All real connection management is in the Services panel.\n\n---\n\n## Service connections (in the Services panel, surfaced for completeness)\n\nSettings → Services routes to `/services`. Connection state comes from `bridge.connections.*` via `useConnections()`; status per card is `loading | error | connected | disconnected`. Tokens are encrypted in the OS keychain and **never returned to the renderer** — only boolean `connected` + safe metadata (login, user, project count).\n\n| Service | Connect mechanism | Token shape / source | Disconnect | Notes |\n|---------|-------------------|----------------------|------------|-------|\n| **GitHub** | `bridge.connections.github.connect()` — reuses `gh` CLI auth | none (login reused; source `cli.github.com`) | `github.disconnect()` | Powers Workboard, Repo, Release, Pipeline. Hero banner when disconnected. |\n| **Vercel** | `vercel.connect(token)` | personal token from `vercel.com/account/tokens` | yes | Shows user + project count; enables zero-config **Deploy** (`detectDeploy`→`deploy`→poll `deployState` to READY). |\n| **Sentry** | `sentry.connect(token, org)` | auth token + org slug (`sentry.io/.../auth-tokens/`) | yes | Surfaces production errors on Workboard + Observability. |\n| **Slack** | `slack.connect(token)` | bot token `xoxb-` (`api.slack.com/apps`) | yes | **Map channels** dialog tags channels Tech/Non-tech/Test (`slack.setChannelTeam`). |\n| Netlify, Fly.io, Railway, PostHog, Plausible, Supabase, Stripe | DocsCard only | — | — | \"Open the provider's docs\" via `bridge.openExternal(connectUrl)`; no in-app connect yet. |\n\n**`.env` auto-connect** (`EnvConnectCard`): `bridge.env.scanConnectable(root)` reads `.env` key **names only**; **Link** → `bridge.env.connect(root, id)` reads the value transiently main-side to test+store, then drops it. Never shown back, never sent to AI.\n\n---\n\n## Keyboard map\n\nImplemented in `lib/useKeymap.ts` (`useKeymap({enabled, onShowHelp})`), mounted by `CommandPalette` with `enabled={!paletteOpen}`. Handler is document-level; it **yields** (returns early) when:\n- the event has `metaKey | ctrlKey | altKey` (except the palette's own `Ctrl/Cmd+K`), or\n- the focused target is editable (`INPUT`, `TEXTAREA`, `SELECT`, or `contenteditable`), or\n- the palette is open.\n\n### g-prefix navigation chords (`NAV_CHORDS`)\n\nPress `g`, then the key within **1500 ms** (buffer auto-clears after the timeout; second key is case-insensitive):\n\n| Chord | Route | Label |\n|-------|-------|-------|\n| `g` `o` | `/overview` | Overview |\n| `g` `w` | `/issues` | Workboard |\n| `g` `r` | `/repo` | Repo |\n| `g` `a` | `/arch` | Architecture |\n| `g` `p` | `/pipeline` | Pipeline |\n| `g` `s` | `/services` | Services |\n| `g` `b` | `/library` | Library |\n| `g` `l` | `/release` | Release |\n| `g` `y` | `/observability` | Observability |\n| `g` `c` | `/activity` | Activity |\n| `g` `t` | `/tutorials` | Walk through |\n| `g` `d` | `/docs` | Docs |\n| `g` `,` | `/settings` | Settings |\n\n### Global keys\n\n| Key | Action |\n|-----|--------|\n| `Ctrl+K` / `Cmd+K` | Toggle command palette (`CommandPaletteHotkey`). Works even in inputs. |\n| `?` | Open the shortcuts help dialog (`shortcuts-help`), which lists `Ctrl+K`, `?`, and all `NAV_CHORDS`. |\n\n### Command palette (`Ctrl+K`)\n\n- `shouldFilter={false}` — own substring match on `label + description`, then re-rank by semantic similarity (`rankBySemanticSimilarity`, 150 ms debounce).\n- Footer: **Esc** to close, **Enter** to select.\n- Groups: **Panels** (from `GROUPS`; shows group badge + \"Hidden\" badge for sidebar-hidden panels), **Actions**, **Docs** (from `CONSOLE_DOC_PAGES`).\n- **Action catalogue** (settings/shortcut-relevant subset): Toggle theme; Switch mode (Guided/Operator); Run onboarding again (clears global + per-window onboarded flags → `/welcome`); Connect GitHub / Connect Vercel / Connect repo (→ `/services`); Run eval (→ `/observability`); Deploy (→ `/release`); Open assistant (toggles chat dock); Open prompt library (→ `/prompts`); Toggle sidebar rail; **Layout: Standard / Focus / Chat**; Copy eval command (`npm run eval:promptfoo` → clipboard + toast); Open project folder (`bridge.openPath`, error toast if no active project); Open repo on GitHub (`github.com/Refringence-AI/console`); Keyboard shortcuts (opens `shortcuts-help`).\n\n### Chat composer (`ChatComposer`)\n\n| Key | Action |\n|-----|--------|\n| `Enter` | Send |\n| `Shift+Enter` | Newline (no send) |\n\n### Prompts (Operator) detail\n\n| Key | Action |\n|-----|--------|\n| `↑` / `↓` | Navigate prompt list |\n| `⌘C` | Copy filled prompt |\n\n---\n\n## Layout presets (`lib/consoleLayout.ts`)\n\nTwo bits — `railCollapsed`, `chatOpen` — persisted to `refringence-console-layout-v1`; changes broadcast via `console-layout-change` and mirror across windows via the `storage` event. `useConsoleLayout` is the single owner of the rail bit.\n\n| Preset | railCollapsed | chatOpen |\n|--------|---------------|----------|\n| **Standard** (default) | false | false |\n| **Focus** | true | false |\n| **Chat** | true | true |\n\nSet from the palette (`Layout: …`) or individually: `toggleRail()` / `setRailCollapsed()` / `toggleChat()`. The top-bar **Assistant** button toggles `chatOpen` and, on open only, calls `bridge.window.growForDock(360)` to widen the OS window for the 360px dock. `detectPreset()` returns `custom` when the two bits don't match a named preset.\n\n---\n\n## Storage keys (settings-relevant)\n\n| Key | Owner | Values |\n|-----|-------|--------|\n| `refringence-console-theme` | Display / TopBar | `light` / `dark` / `system` |\n| `refringence-console-persona` | Mode | `newbie` / `seasoned` |\n| `refringence-console-density` | Density | `compact` / `roomy` |\n| `refringence-console-ai-tier` | AI tier | `on-device` / `on-device-optional-cloud` / `cloud-first` |\n| `refringence-console-layout-v1` | Layout presets | `{railCollapsed, chatOpen}` JSON |\n| `refringence-console-sidebar-layout-v1` | Sidebar editor | `{hidden, order, collapsedGroups}` JSON |\n| `refringence-console-ai-keys-migrated` | One-shot migration flag | `1` |\n| `refringence-console-anthropic-key` | **Legacy** (migrated then removed) | — |\n\nCloud API keys, LangSmith keys, and all service tokens are **not** in localStorage — they live in the OS keychain via the main process and are never returned to the renderer."
  }
]